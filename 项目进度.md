# DeskPilot 项目进度

> 本文件是跨会话续接入口。每次完成开发任务后都应更新“已完成、验证结果、遗留问题、下一步”四部分。

## 1. 当前状态

| 项目 | 内容 |
| --- | --- |
| 最后更新 | 2026-08-17 |
| 产品目标 | 本地优先、能力可声明、过程可检查、结果可验证的通用任务 Agent；首个纵向切片为 `research_to_html` |
| 当前阶段 | 阶段 4：多 Agent / 通用任务 Runtime（阶段 74 可证明上下文压缩与重建已完成） |
| 阶段状态 | 当前进入阶段 75 通用任务与多 Agent 对抗发布门禁；CompactionSnapshot 已绑定 source/coverage/parent 并在漂移时 stale，但 Summary 仍不能放宽 Policy 或绕过阶段 71 verified edge |
| 总体完成度（当前扩展口径） | **约 52%～56%**；首个价值门、短期/长期记忆和可证明压缩已完成，但统一前端工作台、用户路径导出、对抗发布门禁和第三方供应链仍在分母内 |
| 多 Agent 专属实现 | **约 48%～52%**；Registry、Contract/Plan、Handoff/Invocation/Model Turn、独立 Verification、verified-edge reducer、Memory、ContextManifest 和 source-bound Compaction 已落地；统一图控制面和发布级对抗证明尚未完成 |
| 通用任务能力实现 | **约 23%～27%**；`research_to_html` 已可生成、验证和交付受控 HTML，且多轮 task-scope 约束可确定性压缩/重建；更多产物、用户路径导出和统一 UI 仍未完成 |
| 后端目录 | `backend/` |
| 前端目录 | `frontend/` |
| 设计文档 | `doc/` |
| 当前处理器 | 磁盘容量任务仍从离线 Fake Provider 派发；结构化 `file_move`/`file_move_saga` 使用 v1 受信线性图，`file_move_dag` 使用通用 v2 受信图；`disk_pressure_guarded_file_move` 已把真实 R0 磁盘结果、内容寻址条件决定、ready/admission、R1 审批/账本/回执与前端演示串成首个可触发业务图。所有写路径均来自受保护结构化请求，已派发 running Tool 仍严禁自动重放并进入对应 unknown 阻断终态 |
| 当前能力缺口 | 真实联网默认关闭；尚无 Conversation/Workspace 统一前端工作台、用户路径导出、多类产物能力和阶段 75 发布级对抗证明；现有本地知识库/MCP 仍未组入同一通用任务链 |

### 1.1 进度估算口径

- 早期“约 79%”只针对扩展多 Agent 范围之前的原始 DeskPilot MVP，现已废止，后续不得再作为当前总进度引用。
- ADR-015 前的“约 50%”也已废止：它没有把联网研究、Artifact 工作区、HTML 生成和浏览器验收作为独立可发布能力计入分母。
- 当前 40%～45% 按实际剩余工程量粗估，不按阶段编号、文档数量、代码行数或勾选项数量平均计算。
- 架构方案、候选决策和 ADR 只有在实现、自动化验收与文档回写都完成后，才增加实现完成度；单独完成讨论或 Markdown 不计为功能完成。
- 多 Agent 专属实现与可复用底座分开统计：底座成熟不能冒充 Agent Registry/Handoff/Verification/Memory 已落地，后者也不抹去已有安全执行、恢复和评测资产。
- 通用任务能力单独统计：本地知识库、固定 MCP、Registry 角色名或“模型理论上可联网”都不能冒充真实 Web Research/Artifact/Browser 闭环。
- 百分比只用于范围管理，不替代每一阶段的验收门；后续更新应同时写明新增完成项、剩余项和验证证据。

## 2. 本次已完成

### 2.1 工程与环境

- [x] 创建独立的 `backend/` 与 `frontend/` 目录。
- [x] 使用工作区 Python 3.12.13 建立 `backend/.venv`。
- [x] 后端生成 `uv.lock`，锁定 Python 依赖解析结果。
- [x] 前端生成 `pnpm-lock.yaml`。
- [x] pnpm 供应链策略只允许 Vite 必需的 `esbuild` 运行安装脚本。
- [x] 创建根目录 `.gitignore`，排除虚拟环境、运行数据库、构建产物和本地密钥。

### 2.2 后端

- [x] FastAPI 应用组合根和配置模型。
- [x] SQLite + SQLAlchemy Async 持久化。
- [x] 引入 Alembic，以 `0001_initial_schema` 管理首版数据库结构。
- [x] 应用启动自动执行 `upgrade head`，不再使用运行时 `create_all`。
- [x] migration 同时支持空库初始化和旧版已有数据库无损接管。
- [x] 新增 `OutboxMessageRecord` 与 `0002_transactional_outbox` migration。
- [x] 任务状态、任务事件和 Outbox 消息在同一事务原子提交。
- [x] `OutboxPublisher` 提交后唤醒，并保留短轮询作为漏通知兜底。
- [x] 发布失败按指数退避重试，错误信息和尝试次数持久化。
- [x] API 重启后自动恢复未发布消息，按任务事件序号投递。
- [x] 使用 at-least-once 语义；WebSocket 继续按 `task_id + seq` 去重。
- [x] `TaskRecord`、`TaskEventRecord` 与任务内单调事件序号。
- [x] 任务创建、任务查询、按 `after_seq` 补拉事件。
- [x] WebSocket 实时事件流，连接后先补历史再接收新事件。
- [x] WebSocket 同时监听 broker 事件和客户端 disconnect；非终态客户端关闭后立即清理等待器与订阅，不阻塞 API 优雅停机。
- [x] 进程内 Event Broker；数据库事件是持久化真值。
- [x] 首版 Fake Task Processor 产生分类、计划、步骤、工具和完成事件，后续已演进为 Model Gateway 驱动的 TaskProcessor。
- [x] 终态和 `task.completed` 在同一事务提交，避免状态/事件竞态。
- [x] SQLite 回读时间统一补为 UTC，消除前后事件 8 小时时差。
- [x] 404 使用稳定错误码 `TASK_NOT_FOUND`。
- [x] 每次 API 启动生成高熵进程级 session token，也支持通过 SecretStr 配置注入。
- [x] 会话引导接口只接受精确白名单 Origin 或可信同源 Fetch Metadata，并禁止缓存响应。
- [x] REST 使用 Bearer token；写请求额外要求可信浏览器来源。
- [x] WebSocket 同时校验精确 Origin 和 `Sec-WebSocket-Protocol` 中的令牌。
- [x] 凭据不进入 WebSocket URL、查询参数或 Uvicorn 访问日志。
- [x] 领域错误、参数错误和框架 HTTP 错误统一为 RFC 9457 风格 Problem Details。
- [x] 定义显式任务状态转换矩阵，终态后禁止继续追加执行事件。
- [x] 新增 `pause/resume/cancel` 命令 API 和可选 `reason` 请求体。
- [x] Pause 只允许 `running -> paused`，在持久化事件之间的安全点生效。
- [x] Resume 从可验证的下一阶段检查点继续，同一 API 进程或跨 API 重启都不重复已完成 Tool 事件。
- [x] Cancel 可终止任意非终态任务，以 `task.cancelled` 结束事件流。
- [x] 重复 pause/cancel 幂等，不增加事件序号或 Outbox 消息。
- [x] 非法转换返回 `409 TASK_TRANSITION_NOT_ALLOWED` 和机器可读状态扩展。
- [x] 密文损坏或无法与持久化真值证明一致的 checkpoint 会 fail closed；进程内也无可用运行时时 Resume 返回 `409 TASK_RUNTIME_UNAVAILABLE`。
- [x] 新增数据化 `ToolContract`，声明版本、风险、副作用、Schema、超时、幂等性、输出上限、资源锁和最小能力。
- [x] Tool Contract 不保存 callable、import path 或命令，只能由受信任应用组合代码注册。
- [x] 新增精确匹配 `name@version` 的 `ToolRegistry`，注册时校验输入/输出 Pydantic 模型与序列化 Contract 一致。
- [x] 新增 `deskpilot.runner.v1` IPC 消息：hello、heartbeat、call、cancel、progress、result。
- [x] IPC 信封使用至少 32 字节会话密钥进行 HMAC-SHA256 签名，完整绑定调用参数、Contract 摘要、时间窗和 Runner 启动 nonce。
- [x] Runner 验证器拒绝未知 key、篡改、旧启动会话、未来签发、过期、超长 TTL 和重复 nonce。
- [x] 新增线程安全的进程内防重放表；命令通过密码学与时间校验后立即消费 nonce。
- [x] 新增严格单帧 NDJSON codec：UTF-8、换行终止、1 MiB 上限、重复 JSON key 和多帧拒绝。
- [x] 新增 Runner 调用授权器：验签后执行精确版本白名单、Contract 摘要、幂等键和输入 Schema 校验。
- [x] 新增 `doc/14-Tool-Contract与Runner-IPC协议.md`，记录信任边界、消息字段、签名范围、错误码、校验顺序和下一阶段接线方案。
- [x] 新增控制面 `RunnerClient`，使用当前 Python 解释器创建隐藏的独立 Runner 子进程。
- [x] 会话密钥、`key_id` 和 `startup_nonce` 每次启动随机生成，只通过 stdin 首个 bootstrap 帧交付，不进入命令行、环境变量或日志。
- [x] Runner 完成签名 hello 握手并持续发送 heartbeat；控制面支持心跳超时、异常退出检测和干净关闭。
- [x] Runner 使用最多 4 个 worker、8 个活动调用的受限并发，并支持 Contract 超时与协作取消。
- [x] 新增静态 `ToolExecutor` handler 白名单；输出在 Runner 和控制面两侧执行 Schema 校验，Runner 额外执行 Contract 大小限制。
- [x] 实现首个真实 R0 工具 `computer.disk_usage@1.0.0`，使用标准库读取现有路径所在磁盘的 total/used/free/百分比。
- [x] 磁盘工具无副作用、禁止网络、Contract capability 为 `filesystem.metadata.read`，默认查询 `DESKPILOT_DISK_USAGE_PATH=.`。
- [x] Fake Processor 调整为 Fake Planner，将 `fake.echo` 替换为独立 Runner 真实调用；任务事件可关联 `call_id`、版本、Contract 摘要、Runner ID 和真实结果。
- [x] 健康接口新增 Runner 状态；正常为 `ready`，恢复中为 `degraded`，并投影 backoff/open/half-open 运行态。
- [x] 覆盖真实独立 PID、heartbeat、磁盘查询、结构化失败、超时、取消、启动失败、意外退出和 API 任务闭环测试。
- [x] 修复 Windows Proactor 子进程 stdout/stderr transport 回收顺序，退出时不遗留管道警告。
- [x] 新增 `doc/15-独立Runner与首个R0工具实现.md`，记录进程生命周期、安全边界、失败语义、配置和验收。
- [x] 新增 `RunnerSupervisor`，每次换代使用全新 `RunnerClient`、HMAC 会话、startup nonce、Runner ID 和冻结 generation lease。
- [x] Runner 启动/退出/心跳/协议故障按指数退避自动恢复；连续失败进入 open，恢复窗口后只创建一个 half-open 代际，稳定窗口后才清零。
- [x] 初次 Runner 启动失败不再阻止 API 启动；健康接口投影 state、generation、失败数、重启次数、下一次重试和稳定错误码。
- [x] 旧代在途调用绝不透明重放；expected runner ID 不一致时快速拒绝并安全收敛。
- [x] Alembic `0006_tool_call_persistence` 新增 `tool_calls`，持久化 call/task/step/attempt、Contract/参数摘要、幂等策略、Runner ID、状态和终态事件关联。
- [x] 原始 arguments 和 idempotency key 只计算 SHA-256，不进入调用账本、TaskEvent 或 Outbox。
- [x] `tool.requested`、`tool.started`、工具终态、任务失败和 Outbox 按调用阶段原子提交；终态重复或迟到结果为 no-op。
- [x] 启动恢复将遗留 requested 收敛为确定失败，将 running 收敛为 `unknown`；重复恢复不增加事件。
- [x] 非重放安全工具执行中 timeout/cancel 返回 `unknown`，不把可能已产生的副作用伪装成普通失败。
- [x] 新增 `doc/27-Runner故障恢复与unknown调用持久化.md`，记录状态机、事务、恢复和剩余安全边界。
- [x] 新增 Provider-neutral `ModelRequest/ModelResponse/ModelUsage/ModelStreamEvent/ProviderHealth` 领域契约。
- [x] 新增不可变 Provider descriptor 和 capability requirements，描述 structured output、strict schema、streaming、tool calling、vision、embeddings 与上下文上限。
- [x] 用 `typing.Protocol` 定义 `ModelProvider` 的 complete、stream、health 和 descriptor port，Processor 不依赖供应商 SDK。
- [x] 实现 Model Gateway Provider 注册、重复/缺失默认配置拒绝、显式 Provider hint 和响应身份校验。
- [x] 实现隐私路由：`local_only` 只走本地；`local_preferred` 未批准云回退时也禁止静默上云。
- [x] 实现能力路由、统一 timeout、连续流式序号与 completed 语义检查。
- [x] 定义认证、限流、不可用、超时、内容过滤、响应无效和流无效等稳定模型错误码。
- [x] 实现无需网络/API Key 的 `FakeModelProvider`，支持分类、计划、流式事件、usage、health、延迟和失败注入。
- [x] 新增严格 `TaskClassification/TaskPlan/PlanStep` Schema，校验步骤唯一性、依赖顺序和工具名称/版本配对。
- [x] TaskProcessor 的 `intent` 与 `planner` 阶段改由 Gateway 结构化输出驱动，不再在 Processor 内写死计划字典。
- [x] 当前只读切片额外限制模型计划必须且只能引用 `computer.disk_usage@1.0.0`；模型输出仍不是工具授权。
- [x] 持久化 `model.started/model.usage/model.failed/task.classified`，记录 Provider/model/角色/usage/延迟但不保存完整 prompt。
- [x] Provider 未知异常转换为稳定、脱敏 `MODEL_PROVIDER_UNAVAILABLE`，原始错误正文不进入任务事件。
- [x] `create_app` 支持从组合根注入 Model Provider，健康接口公开当前默认 Provider ID。
- [x] 新增 `doc/16-Model-Gateway与Fake-Provider实现.md`，记录实际接口、路由、安全边界、错误和下一步。
- [x] 将 `httpx` 提升为后端运行时依赖，实现 `OpenAICompatibleChatProvider`，业务层与 Processor 不接触 HTTP/供应商类型。
- [x] 将统一 messages、temperature、Token 上限和 strict JSON Schema 映射到 `/chat/completions` 共同子集。
- [x] 非流式响应校验字节上限、单 choice、模型身份、refusal、finish reason、usage 合计、JSON object 和应用 Pydantic Schema。
- [x] 实现有界 UTF-8 SSE 解析，可处理任意网络拆包，要求稳定 id/model、最终 usage、finish reason 与 `[DONE]`。
- [x] 将 401/403、普通 429、额度类 429、408/504、5xx、其他 4xx、连接失败和 timeout 映射为稳定脱敏错误。
- [x] 新增 `MODEL_QUOTA_EXCEEDED` 与 `MODEL_REQUEST_REJECTED`，避免把不可恢复额度问题错误标记为可重试限流。
- [x] Provider 使用 `SecretStr` 保存 API Key；默认禁止重定向和环境代理继承，base URL 拒绝内嵌凭据、query 与 fragment。
- [x] 请求不外发 DeskPilot task/request ID、隐私状态、内部 metadata、Tool Contract 或 Runner/session 数据。
- [x] 实现脱敏 `GET /models` Provider health 探测；后续已通过单 Provider 按需公共 API 接入，仍不会在总健康接口中自动探测全部 Provider。
- [x] 新增 17 个完全基于 `httpx.MockTransport` 的 adapter/任务闭环测试，不进行真实 DNS、网络或付费调用。
- [x] 新增 `doc/17-OpenAI-Compatible-Chat-Provider实现.md`，记录 HTTP/SSE 映射、安全边界、错误语义和后续配置方案。
- [x] 新增 discriminated `ProviderConfig` catalog，支持 Fake 与 OpenAI-compatible Chat 配置，最多 32 个且 Provider ID 唯一。
- [x] 支持同一 Gateway 同时注册默认 Fake、本地 Ollama/OpenAI-compatible endpoint 和可选云端 Chat Provider。
- [x] 空 catalog 保持 legacy Fake 配置兼容，不会因升级自动联网或要求 API Key。
- [x] 新增 `CredentialReference` 与 `CredentialResolver` port，配置只能引用 `DESKPILOT_CREDENTIAL_*`，不能保存明文 `api_key`。
- [x] 实现 environment credential backend，返回 `SecretStr`；缺失/空白密钥以脱敏 `CREDENTIAL_NOT_FOUND` fail fast。
- [x] Provider factory 只允许静态 adapter 类型，不支持 import path、动态 Python 或模型生成的 Provider 类。
- [x] 云端 endpoint 强制 HTTPS 和 credential reference；本地 endpoint 默认只允许 loopback。
- [x] 公网地址不能声明为 local；私网/link-local IP 必须显式 `allow_private_network=true`，DNS hostname 不参与本地推断。
- [x] disabled Provider 不解析 credential；默认 Provider 缺失或禁用时在数据库/Runner 启动前失败。
- [x] 新增 16 个 Provider 配置、凭据隔离、endpoint 策略和多 Provider 组合测试，全程不访问模型网络。
- [x] 新增 `doc/18-Provider配置与凭据引用实现.md`，记录 catalog、凭据边界、环境配置和下一步。
- [x] 新增受认证 `GET /api/v1/model-providers`，显示所有 enabled/disabled Provider 的无密钥 descriptor、默认状态和有效缓存，列表本身不触发网络探测。
- [x] 新增 `GET /api/v1/model-providers/{provider_id}/health`，只在用户明确请求时探测单个 enabled Provider。
- [x] 公共健康快照在领域类型层移除上游 `detail`，catalog/API 均不返回 endpoint、credential reference、API Key 或异常正文。
- [x] Provider 健康探测加入默认 15 秒 TTL；成功、降级和不可用结果都会短期缓存，避免故障时形成重试风暴。
- [x] 同 Provider 并发请求通过 single-flight 共享一个内部 Task；单个调用者取消不会取消其他等待者的探测。
- [x] 不同 Provider 探测使用默认全局 4 并发 Semaphore，并有统一 5 秒 timeout；应用关闭时取消遗留探测。
- [x] disabled Provider 无需凭据即可出现在 catalog，health 返回 `409 MODEL_PROVIDER_DISABLED` 且不访问网络；未知 ID 返回 `404 MODEL_PROVIDER_NOT_FOUND`。
- [x] 新增 11 项 Provider API、认证、脱敏、零探测列表、TTL、single-flight、取消隔离、并发限制和 timeout 测试。
- [x] 新增 `doc/19-Provider只读API与健康探测缓存实现.md`，记录接口契约、缓存语义、安全边界、配置与下一步。
- [x] 新增 `0003_provider_catalog` migration，创建 `model_provider_catalog_state` 与 `model_provider_catalog_entries` 两张表。
- [x] 新增严格 `ProviderCatalogDefinition/PersistedProviderCatalog`，持久化入口在类型层只接受公开 descriptor、enabled/default，不接受原始 Settings 或 ProviderConfig。
- [x] 新增 `ProviderCatalogStore` port 与 SQLAlchemy repository，API 的 catalog 和 health 前置检查改为每次读取持久化公开投影。
- [x] 应用启动在 Runner 之前幂等导入公开 catalog；相同内容保持版本与时间，公开内容变化时事务内替换条目并将版本单调递增。
- [x] 导入前按 Provider ID 稳定排序并计算 canonical JSON SHA-256，输入数组顺序变化不会产生虚假版本。
- [x] repository 支持 `expected_version` 条件更新和 `MODEL_PROVIDER_CATALOG_VERSION_CONFLICT`，为未来 ETag/`If-Match` 写接口保留乐观并发基础。
- [x] Provider API 新增 `catalog_version` 和 `imported_at`，前端可判断 catalog 是否在重启或配置导入后发生变化。
- [x] SQLite catalog 不保存 `base_url`、credential identifier、API Key 或 health detail；自动化测试直接扫描数据库文件验证敏感字节不存在。
- [x] 新增 5 项 catalog 持久化、幂等导入、版本冲突、重启续读和密钥隔离测试，并更新迁移/API 断言。
- [x] 新增 `doc/20-Provider-Catalog持久化与启动导入实现.md`，记录公开投影决策、数据模型、版本语义、安全边界和下一步。
- [x] `CredentialReference` 新增 `windows_credential_manager` backend；environment 与 Windows identifier 分别使用独立严格命名空间。
- [x] 新增 `CompositeCredentialResolver`，只按显式 backend 分发，禁止 Windows/environment 之间静默 fallback。
- [x] 新增 ctypes `Win32CredentialApi`，使用 Unicode `CredReadW/CredWriteW/CredDeleteW/CredFree`，不增加第三方依赖。
- [x] Windows 凭据固定使用 `CRED_TYPE_GENERIC`、`CRED_PERSIST_LOCAL_MACHINE` 和 `DeskPilot/ModelProvider/` target namespace。
- [x] 实现 Windows Credential Manager resolve/store/delete；删除不存在条目返回 false，保持幂等。
- [x] Generic credential 按 UTF-8 编码并限制 2560 字节；读取始终 `CredFree`，Python 侧读写 `bytearray` 在 finally 中零覆盖。
- [x] 新增 `CREDENTIAL_BACKEND_UNAVAILABLE/CREDENTIAL_INVALID/CREDENTIAL_BACKEND_OPERATION_FAILED`，错误仅保留稳定 operation/backend/OS code，不泄露 target 或密钥。
- [x] Windows 默认组合同时注册 environment 与 Credential Manager；Provider Factory 和业务层继续只依赖原有 `CredentialResolver` port。
- [x] 新增安全本地 CLI：隐藏输入两次保存、只显示存在状态、删除要求 `--yes`，不支持命令行 secret 参数。
- [x] 新增 14 项 backend 命名、CRUD、字节边界、缓冲区清零、错误脱敏、无 fallback、Factory 接线和 CLI 测试；未操作真实用户凭据库。
- [x] 新增 `doc/21-Windows-Credential-Manager实现.md`，记录官方 API 依据、Win32 映射、CLI、安全边界和下一步。
- [x] 新增严格 `ProviderRuntimeConfigBundle`，统一封装完整 ProviderConfig、payload schema 和固定 `retain` 凭据删除策略；内外 Provider ID 必须一致。
- [x] 新增 `RuntimeConfigProtector` port 与 `ProviderRuntimeConfigCodec`，使用 canonical JSON、256 KiB 明文上限和编解码工作 `bytearray` 清零。
- [x] 新增 ctypes `WindowsDpapiProtector`，使用 current-user `CryptProtectData/CryptUnprotectData`、`CRYPTPROTECT_UI_FORBIDDEN`、Provider 绑定 entropy 和 `LocalFree`。
- [x] DPAPI 不使用 `CRYPTPROTECT_LOCAL_MACHINE`，运行配置通常只能由同一 Windows 用户在同机解密；错误只保留 operation 和 OS code。
- [x] 新增 `0004_provider_runtime_config` migration，创建 `model_provider_runtime_configs` 密文表与 `model_provider_config_audit_events` 审计表。
- [x] 新增 `SqlAlchemyProviderRuntimeConfigRepository`，支持 create/update/get/delete、revision 条件更新、相同内容幂等和审计 sequence 分页。
- [x] 密文变化与审计追加在同一 SQLite 事务；审计只保存字段名和枚举结果，不保存 endpoint、credential identifier、密钥或修改前后 JSON。
- [x] 明确删除联动边界：删除 Provider 配置不会自动删除 environment/Windows 凭据，审计记录 `provider_deleted_credential_retained`；后续凭据删除必须独立确认并检查引用。
- [x] 新增 8 项运行配置/DPAPI/仓储/审计测试；Windows 真实 DPAPI 测试只做内存往返，不写 Credential Manager 或外部文件。
- [x] 新增 `doc/22-Provider运行配置保护与审计模型实现.md`，记录分层、DPAPI 依据、数据模型、并发语义、删除策略和下一步接线方案。
- [x] 新增统一 `ProviderManagementStore` port、`ProviderManagementService` 和 SQLAlchemy 原子仓储，统一协调公开 Catalog、密文运行配置、审计和幂等回执。
- [x] Provider 启动配置改为首次 seed；存在密文配置后，数据库成为 adapter 启动真值，重启不再被 `.env`/Settings 静默覆盖。
- [x] 新增 `0005_provider_management` migration 与 `model_provider_idempotency_records`，只保存 key digest、keyed request fingerprint、公开回执和 24 小时过期时间。
- [x] GET Catalog 新增配置 ETag 和 `Cache-Control: no-store`；写请求强制严格 `If-Match` 与 16～128 位 `Idempotency-Key`。
- [x] 新增 Provider 创建、PUT 完整更新、启用、禁用、默认切换、删除和审计分页 API。
- [x] stale ETag 返回 `412 MODEL_PROVIDER_CATALOG_VERSION_CONFLICT`；幂等成功重试优先于 ETag 检查，返回原回执并标记 `replayed=true`。
- [x] 同 key 不同请求返回 `409 IDEMPOTENCY_KEY_REUSED`；raw key、endpoint、credential identifier 和 API Key 不进入数据库回执。
- [x] 写入前构造完整候选 adapter registry；credential 缺失/配置无效时不提交 Catalog、密文、审计或幂等记录。
- [x] 新增 `ModelGateway.reconfigure` 和 health cache/probe invalidation；提交后新请求立即使用新 Provider/default，进行中的旧调用可继续完成。
- [x] endpoint 等隐藏字段变化也递增聚合 Catalog version；删除默认/最后 Provider、禁用默认 Provider 均被拒绝。
- [x] 新增 8 项写 API、ETag、幂等、动态 Gateway、凭据失败回滚、重启真值、凭据保留和 SQLite 明文扫描测试。
- [x] 新增 `doc/23-Provider管理服务与写API实现.md`，记录事务顺序、API 契约、并发/幂等语义、启动真值和安全边界。
- [x] CORS 显式暴露 `ETag` 响应头，前端使用独立 API Origin 时也能读取 Catalog 并发条件。
- [x] 新增 `ModelGatewayPolicy`、角色级有序 Provider allowlist 和 `priority/latency_aware` 两种策略；隐私、能力与费用硬约束始终先于延迟排序。
- [x] 新增请求级 `ModelExecutionBudget`；应用默认最多 2 次尝试、累计等待 2 秒，整个 fallback 链共享单一 request timeout。
- [x] OpenAI-compatible adapter 解析 429/5xx `Retry-After` delta-seconds/HTTP-date 并限制为 3600 秒；有备用时立即切换，无备用时只在预算内等待。
- [x] 新增整数微美元 Provider 价格、调用前保守费用预留、任务累计结算和可负担 fallback；有限预算下缺少价格时安全拒绝。
- [x] 新增 Provider 延迟 EWMA；延迟感知策略先采样未知候选，再按最低 EWMA 调度。
- [x] 新增 closed/open/half-open 熔断，连续 timeout/不可达/非法响应达到阈值后打开，恢复窗口后仅放行一个半开探针；429 使用独立冷却窗口。
- [x] 新增 `GET /api/v1/model-providers/routing`，只返回角色策略、预算、EWMA、费用、重试和熔断状态，不返回 endpoint、凭据、请求内容或任务 ID。
- [x] 新增 12 项路由/配置/adapter/API 测试，使用虚拟时钟、Fake Provider 和 MockTransport 覆盖角色选择、费用预算、Retry-After、EWMA、熔断、半开单探针与脱敏投影。
- [x] 新增 `doc/25-角色级Provider路由与韧性预算实现.md`，记录排序顺序、计价、重试、状态机、配置和已知边界。
- [x] 新增确定性的 `BuiltinPolicyEngine` 和结构化 `ToolAuthorizationRequest/PolicyDecision`；策略不调用模型，只消费受信任 Contract、规范化参数摘要、capability 和资源事实。
- [x] 建立默认风险矩阵：R0 自动允许；R1/R2 要求一次性审批；R3 默认拒绝，显式启用后仍要求审批；R4 项目级拒绝。
- [x] 新增有效风险上调、builtin 来源限制、capability allowlist、资源 operation/capability 完全绑定和精确资源范围校验，任一事实不匹配均 fail closed。
- [x] 新增 `waiting_approval` 状态，以及 `policy.evaluated`、`approval.required`、`approval.resolved`、`approval.expired`、`approval.invalidated` 事件。
- [x] 新增 `0007_policy_approvals` migration 和持久化 `approvals`；工具调用账本同步保存策略 decision/revision/effect、资源范围摘要、策略事件和 authorization ID。
- [x] 审批 `preview_hash` 精确绑定 task/step/call、工具版本、Contract/参数摘要、策略 decision/revision、资源/capability、后果、数据外发和有效期；决策只支持 `scope=once`。
- [x] 相同审批决定可幂等重放，冲突决定、旧预览和过期审批返回稳定 409；批准、拒绝、过期、任务取消和调用状态都使用 CAS/任务锁收敛。
- [x] 策略/审批、任务与工具调用状态、TaskEvent 和 Outbox 在同一数据库事务原子提交；提交失败不会留下孤立授权或事件。
- [x] 一次性审批仅在 `requested -> running`、Runner IPC 下发前原子写入 `consumed_at`；已消费、过期或绑定不一致的授权不能再次执行。
- [x] Runner IPC 新增 `ToolAuthorizationGrant`，完整绑定 task/step/call、actor/origin、工具/Contract、策略依据、参数/资源、capability、网络/外发、副作用、交互/批量和一次性审批；Runner 在 handler 前拒绝缺失、错配、降级或过期证明。
- [x] Tool Registry 要求可信资源 projector；控制面和 Runner 分别从同一份已验证参数重算规范资源，参数与授权资源不一致时在 handler 前 fail closed。
- [x] 用户 `decision` 与当前授权 `status` 分离；批准后未消费的授权在取消、dispatch 前过期或启动恢复时显式失效，同时保留原批准人、时间和原因审计。
- [x] API 启动时只为通过持久化证明的 pending/approved-but-unconsumed 审批保留续跑资格；缺失或无效 checkpoint 的遗留状态仍安全取消，不猜测也不重放已派发 Tool。
- [x] 非交互、批量和数据外发调用在相应预览/预算/目的地绑定完成前 fail closed；审批 API 成功响应统一使用 `Cache-Control: no-store`。
- [x] 新增 `doc/28-Policy-Approval执行前授权主干.md`，记录策略矩阵、精确预览、一次性消费、Runner 证明、重启语义和 OS 隔离边界。
- [x] 将常驻 Runner 收敛为签名 IPC/授权/调度 broker；工具 handler 改为每次调用在全新一次性 worker 进程执行，不跨调用复用内存、句柄或模块状态。
- [x] 新增 `deskpilot.worker.v1` 私有单帧协议；worker 只接收 call ID、精确工具版本、Contract 摘要和已验证参数，不接收 Runner HMAC 密钥或完整授权证明。
- [x] worker 从受信任固定 factory 重建静态 executor，并再次校验工具版本、Contract 摘要、输入/输出 Schema 与输出上限；生产 factory 不能由模型、用户或 IPC 指定。
- [x] 新增 ctypes Windows 隔离启动器：`CreateRestrictedToken(DISABLE_MAX_PRIVILEGE | LUA_TOKEN)` 删除最大权限，`SetTokenInformation(TokenIntegrityLevel)` 降为 Low Integrity（RID 4096）。
- [x] 每个调用创建独立 Job Object，启用 kill-on-close、未处理异常终止、进程内存上限和活动进程上限；默认 256 MiB、`ActiveProcessLimit=1`。
- [x] worker 以 `CreateProcessAsUserW + CREATE_SUSPENDED` 创建，先 `AssignProcessToJobObject` 再 `ResumeThread`，消除入 Job 前执行/派生的竞态。
- [x] 使用 `STARTUPINFOEXW + PROC_THREAD_ATTRIBUTE_HANDLE_LIST`，worker 只继承私有 stdin/stdout/stderr；父/子管道副本、令牌、Job、进程和线程句柄在所有成功/失败路径关闭。
- [x] worker 环境从静态白名单重建，排除 session token、Provider credential 和任意应用秘密；为保持 `ActiveProcessLimit=1`，直接启动可信基础解释器而非会再派生进程的 venv 重定向器。
- [x] timeout/cancel 现在可通过 `TerminateJobObject` 强制终止调用进程；非重放安全调用仍按既有规则保守收敛为 `unknown`，不会因强杀而伪造未产生副作用。
- [x] bootstrap 新增强制 Windows 沙箱、worker 内存/进程数限制；签名 hello 回报实际 isolation mode 与每调用隔离，要求不满足时 Runner 代际启动失败而非静默降级。
- [x] Windows 真实集成测试验证不同调用 PID、Low Integrity、权限削减、Job 成员、Medium Integrity 写入拒绝、子进程创建拒绝和父进程秘密隔离。
- [x] 新增 `doc/29-Windows-Runner进程隔离与低完整性实现.md`，记录执行链、Win32 映射、配置、失败语义、已强制边界和剩余资源隔离边界。
- [x] 新增 `0008_tool_reconciliation` migration；持久化 `tool_reconciliations`、Reconciliation API 幂等回执和 `key_required` Tool 幂等键占用回执。
- [x] `finish_tool_call(UNKNOWN)` 与启动恢复在 Tool 账本、TaskEvent、任务终态和 Outbox 的原事务中创建 pending reconciliation；回滚不留孤立记录。
- [x] 人工裁决支持 `confirmed_succeeded / confirmed_failed / confirmed_no_effect / accepted_unknown`，必须携带证据摘要，resolved 后不可改写。
- [x] 原 `tool_calls.status=unknown` 永不改写；人工裁决不伪装成 Runner 结果，也不向原 failed task 追加事件。
- [x] 只有 `confirmed_no_effect` 允许创建一个显式新 attempt；其他 outcome 全部 fail closed，区分“失败”与“可证未产生效果”。
- [x] 新 attempt 使用全新 Task/call ID，仅复制 goal/conversation/privacy/constraints，重新规划、Policy/Approval 和 Runner 授权；不复用旧参数、幂等键、预览、审批或 lease。
- [x] `task.created` 写入 `retry_of` lineage，精确关联 reconciliation、原 task/call 和 source attempt；Reconciliation 最多绑定一个直接后继任务。
- [x] Reconciliation 写 API 强制 16～128 位 `Idempotency-Key`；只保存键摘要与规范请求 fingerprint，支持跨 API 重启语义重放与同键异请求 409。
- [x] `key_required` Tool 在 requested 事务内持久化 tool/version/key digest 占用回执；跨任务重用在 Runner 前被拒绝，原始 key/参数不落库。
- [x] 旧 schema 中已有 `unknown` 自动回填 pending reconciliation，旧 `key_required` 调用回填最早占用者；空库、重复升级和保留数据均通过。
- [x] 新增受认证 Reconciliation 列表/详情/裁决/新 attempt API；写接口强制可信 Origin，成功响应统一 `Cache-Control: no-store`。
- [x] 并发同键 create-attempt 只创建一个任务；数据库已提交而响应丢失时，同键重试恢复原 task，仅在它仍为 `created` 且本进程无 runtime 时启动。
- [x] 新增 `doc/30-unknown人工对账与显式新attempt实现.md`，记录状态机、幂等语义、事务边界、API、迁移和不重放不变量。
- [x] 新增父 Runner `ToolResourceBroker`；`filesystem.metadata.read` 先以 `CreateFileW(FILE_READ_ATTRIBUTES)` 打开授权路径，再用最终句柄路径核对签名资源 scope。
- [x] broker 通过 `GetVolumePathNameW/GetDiskFreeSpaceExW` 生成不可变容量 facts；一次性 worker 不再根据原始 `path` 打开文件系统。
- [x] `deskpilot.worker.v1` 新增严格 `BrokeredFilesystemMetadata`；worker 要求 resource operations 并集与 allowlisted Contract capabilities 完全相等，缺失/额外能力在 handler 前拒绝。
- [x] `ToolExecutionContract` 新增 `read_only/brokered` commit protocol；声明副作用的 Contract 必须使用 brokered commit，无 provider 时以 `TOOL_CONTROLLED_COMMIT_UNAVAILABLE` 在 handler 前 fail closed。
- [x] Windows 启动器新增每调用 AppContainer 模式，无任何网络 capability，同时保留 Job Object、挂起启动、精确 stdin/stdout/stderr 句柄白名单和环境重建。
- [x] Runner bootstrap/hello 独立声明 `require_network_isolation` 与 `network_isolation_mode`；强制模式预启动真实 worker，解释器或模块在 AppContainer 中不可访问时整代 Runner 启动失败，不静默退回 Low Integrity。
- [x] Windows loopback 真实测试证明无 capability AppContainer 内 `curl.exe` 无法连接父进程监听端口；连续 32 次资源 broker 核验无句柄泄漏，测试后无残留 AppContainer profile。
- [x] 新增 `doc/31-Contract能力Broker受控提交与Windows禁网边界.md`，记录资源句柄/facts、commit 门槛、AppContainer 姿态、平台探针与发布态剩余边界。
- [x] 新增内容寻址 AppContainer worker bundle，只收集 CPython 标准运行时、Pydantic 闭包与 DeskPilot 包；staging 完成 manifest、ACL 和哈希复核后原子发布。
- [x] bundle manifest 精确绑定目标路径、大小、SHA-256、Python 版本和稳定 capability SID；存在额外文件、reparse point、路径穿越或任意内容篡改时 fail closed，不静默覆盖。
- [x] `DeskPilot.workerRuntime.v1` capability 通过 protected DACL 只获得 bundle read/execute；当前用户、SYSTEM 与 Administrators 保留安装维护权限，不再修改开发解释器/venv/源码 ACL。
- [x] AppContainer worker 环境移除开发 `PYTHONPATH/VIRTUAL_ENV` 与用户 PATH；只绑定专用 `PYTHONHOME`、最小系统 PATH、私有三管道和 profile-local TEMP。
- [x] 控制面在 Runner 进程创建前异步准备 bundle，Runner 独立复核已发布内容并预启动真实 worker；Windows 发布 `.env.example` 已切换为强制 AppContainer 禁网。
- [x] 新增 durable AppContainer profile journal；每次调用先登记 exact moniker，删除成功后注销，下一 Runner 启动会在接受调用前回收异常退出遗留 profile。
- [x] 真实 `computer.disk_usage` 已在 `windows_appcontainer/appcontainer` 下端到端成功；自定义 runtime capability 写 bundle 失败且不能连接父进程 loopback。
- [x] 新增 `doc/32-AppContainer专用Worker运行时与Profile回收.md`，记录运行时闭包、内容寻址发布、capability DACL、profile reaper、配置与验收。
- [x] 实现首个真实可逆写 Tool `file.move@1.0.0`；Contract 为 R1 / `key_required` / `brokered`，限定单普通文件、同卷移动且绝不覆盖目标。
- [x] `BrokeredFileMove` 精确绑定 source/destination 角色 capability、source 内容+文件身份版本和 destination absent；AppContainer worker 只返回无副作用 `FileMovePrepare`。
- [x] 父 Runner 复核 prepare、authorization ID、一次性 approval preview hash、expected resource versions 和幂等键摘要；Windows commit provider 使用不带 replace flag 的 `MoveFileExW`。
- [x] 新增 Runner 自有 SQLite commit journal，持久化 `prepared/committing/committed/no_effect/unknown`、prepare binding 与 before/after 外部版本；原始幂等键不落库。
- [x] Runner 启动回收可从 source/destination 外部状态证明 committed 或 no-effect；签名 `tool.commit_receipt.get/result` IPC 可跨 Runner 换代查询 receipt 且不重放调用。
- [x] timeout/cancel 改为按共享 commit boundary 裁决：提交前可确定失败/取消，committing 无证据时 `unknown`，已有 durable committed receipt 时返回成功。
- [x] 新增 Alembic `0009_tool_commit_receipts`，将成功 receipt 的无路径版本证据与 Tool 终态/事件原子投影到控制面。
- [x] 新增 `doc/33-file.move受控提交与持久化回执.md`，记录信任边界、资源版本、journal、崩溃恢复、timeout/cancel 与已知限制。
- [x] `TaskCreate.tool_request` 新增 strict `file_move` 结构化入口；API 在建任务前只读规范化 source/destination，拒绝缺失源、目标已存在、跨卷、同路径和非普通文件。
- [x] 文件移动不解析自然语言或模型输出；TaskProcessor 使用可信三步计划模板、`local_user` actor、单次高熵幂等键和 source/destination 精确版本预览。
- [x] Policy 新增窄范围动态路径规则，仅允许精确 `local_user + file.move@1.0.0 + 双角色 capability + R1/local/single-call` 事实，并始终要求一次性审批；模型身份或事实扩张 fail closed。
- [x] 现有审批卡已真实展示规范源/目标路径、source 版本、R1、reversible、capability、no-overwrite 与补偿后果；拒绝/过期/取消时不派发 Runner。
- [x] 前端任务表单新增“移动单个文件”模式、双路径字段、完整性禁用和安全边界提示；批准后真实 API/Runner 端到端只移动一次并返回 committed receipt。
- [x] 新增 `doc/34-file.move显式任务入口与一次性审批.md`，记录请求契约、可信参数来源、Policy 规则、审批/前端流程和恢复边界。
- [x] 新增 Alembic `0010_reconciliation_receipt_evidence` 与追加式 `tool_reconciliation_evidence`；按 reconciliation + 内容摘要去重保存 commit-receipt/no-receipt/query-failed 观察。
- [x] 新增 `POST /reconciliations/{id}:refresh-evidence`；通过冻结 Runner lease 执行签名 journal 查询，不派发 Tool、不重放调用，响应与列表/详情均返回 `Cache-Control: no-store`。
- [x] 发现 receipt 时再次核对 call/tool/version/authorization/idempotency digest，并写入无路径 `tool_commit_receipts` 投影；绑定无效只记录稳定失败码。
- [x] no receipt/query failed 均明确为非结论性证据，不开启新 attempt；committed receipt 也只支持人工判断，原 `tool_calls.status=unknown` 和 pending outcome 保持不变。
- [x] 新增 `doc/35-unknown-Runner回执证据采集与前端展示.md`，记录证据模型、内容寻址幂等、API、安全验证、前端三态解释和剩余边界。
- [x] 新增内部 `FileMoveCompensationRequest` 与无 body `POST /reconciliations/{id}:create-compensation`；反向路径只从原审批和已验证 committed receipt 派生，客户端不能声明路径或版本。
- [x] 创建前复核反向 source 的 receipt 版本和反向 destination absent；阶段 4 在 Tool call/审批前再次投影，关闭证据到审批的竞态窗口。
- [x] 新增 Alembic `0011_file_move_compensation`，持久化 compensation task/receipt/创建时间；每条 Reconciliation 只允许一个直接补偿，且与显式新 attempt 互斥。
- [x] 补偿任务生成新 task/call/高熵 Tool 幂等键/Policy decision/一次性 approval，审批后仍经过 prepare/commit/receipt；原 unknown Tool 账本保持不变。
- [x] 新增 `doc/36-file.move回执驱动显式补偿闭环.md`，记录服务端派生、版本竞态、单补偿血缘、新授权/提交、前端交互与验收。
- [x] 新增 `GET /api/v1/tasks?status=&limit=&offset=`；提供 1～100 条有界分页、状态筛选、稳定倒序、总数与 `Cache-Control: no-store`，不聚合事件 payload 或 Tool 参数。
- [x] 收紧显式 attempt：仅精确 `computer.disk_usage@1.0.0` 可由当前可信处理器确定性重建；没有持久化原始请求的 `file.move` 即使裁决为 no-effect 也不会从 goal 猜测路径。
- [x] 新增 `doc/37-任务历史与集中Reconciliation中心.md`，记录历史 API、集中工作台、幂等交互、血缘导航、重试边界与验收。
- [x] 新增 strict/frozen `TaskCheckpointPayload`，持久化结构化 `file_move`/补偿请求、受信分类与计划、规范化参数/资源、Policy 事实、审批绑定与原始 Tool 幂等键。
- [x] 新增 `TaskCheckpointCodec`；使用 canonical JSON、512 KiB 上限、临时缓冲区清零和 task-specific current-user DPAPI context，原始路径/幂等键不以明文进入 SQLite。
- [x] 新增 Alembic `0012_task_runtime_checkpoints`；每任务最多一个受保护 checkpoint，明文仅投影 schema/stage/event-seq/revision/protection/digest 和时间。
- [x] 初始 checkpoint 与 task/event/outbox 原子创建；之后每个已完成阶段保存确切 `next_stage`，pause/resume/approval resolution 在事务内重绑最新事件游标。
- [x] API 启动可重建 created/classifying/pre-dispatch running/paused/waiting-approval 和 approved-but-unconsumed 运行时；非暂停状态在 Runner 启动后自动续跑。
- [x] 恢复强制核对 task event sequence/status、确定性 call ID、Tool call status、Policy request digest 与 approval task/call/consumption 绑定；任一错配均 fail closed。
- [x] 启动 Tool call 恢复仅跳过可证明尚未派发的 `requested` call；遗留 `running` call 仍转 `unknown` 并创建 Reconciliation，不会透明重放。
- [x] 终态任务自动删除 runtime checkpoint；密文损坏或真值绑定落后时写入稳定脱敏失败码，不泄漏 DPAPI 细节或载荷。
- [x] 新增 `doc/38-结构化Tool请求与可证明跨重启检查点.md`，记录受信 9 阶段图、保护模型、启动顺序、证明规则、不重放边界与验收。
- [x] 新增 `deskpilot.tool-effect-graph.v1`，明确 graph/node/edge/attempt/effect/compensation 身份、状态和确定性派生规则。
- [x] 新增结构化 `file_move_saga`：一次仅允许 2～10 个互不重叠的显式单文件移动，任务创建前完成普通文件、同卷、目标不存在和路径去重校验。
- [x] 新增 Alembic `0013_tool_effect_graphs`，持久化 graph、node、success/compensation edge、attempt、receipt-bound effect 与 append-only transition journal。
- [x] 节点 transition 与 TaskEvent/Outbox、graph/node revision、attempt 状态及 effect lineage 同事务提交；启动恢复 running call 为 unknown 时同事务阻断 graph。
- [x] 新增 `GET /tasks/{task_id}/effect-graph`，只返回版本、状态、边与 call/attempt/effect/receipt/transition 身份，不返回原始路径、参数或幂等键。
- [x] 多步 forward 每节点重新执行资源版本投影、Policy、一次性审批、Runner commit 和 receipt；全部成功后 graph/task 才成功。
- [x] 后续节点确定失败时，仅对已有 committed receipt 的前置效果逆序启动 compensation；每次补偿使用新 call/Tool attempt/幂等键/Policy/审批/receipt 并记录 `compensates_effect_id`。
- [x] forward 或 compensation 为 `unknown` 时 graph 进入 `blocked_unknown`，停止全部后继和自动补偿，继续交由 Reconciliation 处理。
- [x] 多步任务可在不同节点审批处连续两次 API 重启并精确续跑，不重复已完成 forward call；补偿完成后原任务以稳定 `SAGA_COMPENSATED` 失败终结。
- [x] 新增 `doc/39-版本化Tool-effect-graph与Saga补偿.md`，记录图身份、原子 transition、补偿顺序、unknown 阻断、验收和后续事务边界。
- [x] 新增 Alembic `0014_graph_lease_recovery`，为 effect graph 增加 owner/heartbeat/expiry/fencing token，并为 Reconciliation 增加 graph recovery 状态、动作、事件外键与时间。
- [x] graph acquire/takeover 使用 revision CAS，接管单调增加 fence；所有正常 graph/node mutation 同时核对 live owner、fencing token 和 node revision，旧实例写入完整回滚。
- [x] Processor 按 TTL/3 续租，lease 丢失后停止 runtime；有效、无效或缺失 checkpoint 的启动恢复均先 claim graph，contender 不处理 live owner 的 task/call/approval。
- [x] Tool request/start/finish Runner 边界已统一提交 Tool ledger、event/Outbox、effect attempt、node/graph transition 与 protected checkpoint；request/terminal checkpoint 故障注入证明整事务回滚。
- [x] `unknown` 任务进入非终态 `waiting_reconciliation`；新增幂等 `POST /reconciliations/{id}:recover-graph`，显式 `continue/terminate` 且永不改写原 unknown call。
- [x] 只有 `confirmed_succeeded` 或 `confirmed_no_effect` 可继续；`confirmed_failed` 与 `accepted_unknown` 不能证明安全推进，只允许显式终止。
- [x] 前端历史/状态与集中 Reconciliation 中心支持等待对账、图恢复状态和二次确认的继续/终止操作。
- [x] 新增 `doc/40-跨实例Graph所有权与图级Reconciliation恢复.md`，记录所有权、统一事务、恢复矩阵、验收与数据库时钟边界。
- [x] 新增 Alembic `0015_database_claims_dag`，为 Outbox 与 Tool effect node 增加 owner/acquired/expiry/fencing token，并持久化内容寻址 DAG ready-set checkpoint。
- [x] graph lease acquire/renew/fence 改用数据库权威 UTC；应用时钟漂移不再影响 claim 所有权。
- [x] Outbox Publisher 使用持久化 instance ID、原子 claim、TTL 和单调 fence；过期可接管，旧 publisher 不能 ack/fail 新 owner 的消息。
- [x] Tool、Reconciliation 与 Provider 管理命令的并发唯一键/CAS 竞争会回滚重读持久化回执，统一归一为 replay 或稳定领域冲突。
- [x] 新增 `deskpilot.tool-effect-graph.v2`，验证依赖存在/唯一/无环，持久化 success 与 compensation-order edge，并生成包含 graph/node/predecessor revision 的 ready-set digest。
- [x] v2 节点子集 claim 在同一事务执行 graph revision CAS、node revision/old-fence CAS、transition/event/Outbox；过期 active node 可跨重启 reclaim，旧 node fence 无法提交终态。
- [x] 新增 `doc/41-数据库原子Claim与DAG并行恢复证明.md`，记录数据库时间、Outbox fencing、幂等归一化、DAG ready-set/join 恢复证明与后续边界。
- [x] 新增 `EffectDagDispatcher`；按持久化 ready-set 批量 claim，有界并行执行根节点，join 仅在前驱完成后进入后续轮次。
- [x] 新增 `RunnerEffectNodeExecutor` 受信 adapter；敏感参数和授权 grant 由 claim 后 resolver 提供，不进入脱敏 DAG 定义。
- [x] node claim 增加数据库时间 heartbeat/续租；dispatcher 同时续租 graph 与每个并行 node，旧 fence 始终不可复活。
- [x] v2 新增 graph `cancelled`、node `skipped/cancelled` 与持久化 cancel intent；reducer 迭代传播依赖阻断并归约 succeeded/failed/cancelled/compensating/blocked 终态。
- [x] 新增内容寻址 `tool_effect_compensation_plans`，以 reverse-topological waves 证明 sink-first 和同 wave 并行安全性。
- [x] Alembic `0016_dag_dispatch_delivery` 增加 graph cancel intent、node heartbeat、compensation plan、Outbox delivery/DLQ 字段与 Inbox 去重表。
- [x] Outbox 每次 claim 生成 delivery envelope，有界重试耗尽后进入 DLQ，支持显式 requeue 和 retention cleanup；Inbox handler 与 logical-message 占用在同一事务提交。
- [x] 新增 `asyncpg` 运行依赖与 PostgreSQL 原生 Outbox/node `FOR UPDATE SKIP LOCKED` + 批量 `UPDATE RETURNING` 路径，方言编译自动化断言锁和 fence 语义。
- [x] 新增 `doc/42-DAG并行Dispatcher与可靠消息投递.md`，记录 dispatcher/reducer/补偿计划、delivery/inbox/DLQ、PostgreSQL 语义、验收与剩余边界。
- [x] 新增受信 `file_move_dag` 结构化入口；拓扑依赖、规范路径和全图路径互斥均由应用层验证，不接受模型生成写路径。
- [x] `TaskProcessor` 已将 v2 ready-set 逐节点绑定 Tool call/effect attempt/Policy/一次性审批/authorization/Runner/effect/commit receipt，终态同时校验 graph 与 node fence。
- [x] 同 ready-set 批量审批只在最后一项批准后恢复任务；任一拒绝/过期会取消其余未消费审批与 call；protected checkpoint 绑定整批审批，三进程重启及过期计时器重建测试已通过。
- [x] 新增 `EffectDagCompensationDispatcher`；消费持久化 reverse-DAG waves，同 wave 有界并行，早期 wave 未全部 compensated 时拒绝跨 barrier claim。
- [x] 每个补偿节点由 forward receipt 派生反向参数和版本，创建新 call/attempt/审批/回执；图终态可收敛到 `compensated/blocked_compensation_failed/blocked_compensation_unknown`。
- [x] Alembic `0017_parallel_compensation` 扩展补偿失败/unknown 图终态约束；新增 `doc/43-v2可信Tool账本与并行补偿执行.md`。
- [x] v2 DAG 新增 `conditional` edge 与不可变 `predecessor_key/decision_key/expected_outcome` 定义；条件依赖与 success edge 共同参与重复前驱和环验证，并保留反向补偿顺序。
- [x] 新增 `tool_effect_branch_decisions` 和 fenced `record_effect_dag_branch_decision`；决策绑定 source success revision/event、受信 evidence digest 和全部条件边定义，`decision_id = tbd_<proof_digest>`。
- [x] 分支决策相同内容重试幂等、改写稳定冲突；graph 查询和 ready-set 会重新计算内容摘要，proof/edge 篡改 fail closed，决策提交前的 ready proof 随 graph revision 推进立即失效。
- [x] `EffectDagDispatcher` 新增可选受信 branch resolver；只有 source 持久化成功后才解析已声明 outcome，先落库 decision 再调度选中路径，resolver 缺失时保持等待而不猜测。
- [x] ready-set v2 同时证明前驱 revision/event 和匹配 branch decision；reducer 区分未决、选中与明确未选，未选路径及普通后代传播 `skipped`，跨重启可复算同一分支。
- [x] 补偿计划将 conditional edge 纳入 reverse-topological 顺序；只有实际成功的选中路径进入补偿集合。
- [x] Alembic `0018_branch_decision_proofs` 增加条件边元数据约束和内容寻址分支决策表；新增 `doc/44-条件边与内容寻址分支决策证明.md`。
- [x] `EffectDagDispatcher.request_cancel` 先用当前 graph owner/fence 持久化 cancel intent，再只向本轮 `node_id + claim_fencing_token` 对应的在途 executor 广播取消；空 ready-set 会重新 reduce，避免返回旧 active 快照。
- [x] `LedgerBoundEffectNodeExecutor` 绑定 claim fence、确定性 call ID 与原 Runner ID；claim 后尚未派发的竞态由进程内 cancel 标记和 `start_tool_call` 事务内 cancel-intent 复核共同封闭。
- [x] `RunnerSupervisor.cancel_call` 新增 expected Runner generation 校验；旧 generation 的取消在 IPC 发送前拒绝，不会误伤新 Runner。
- [x] Task Cancel API 将用户 reason 传入 TaskProcessor；当前实例持有的 v2 DAG 在途并行调用会收到 cancel IPC，idle/审批等待图则在新 graph lease 下落 intent 并运行 reducer。
- [x] cancel 仍以 Runner 终态为准：确定未 commit 为 cancelled，committing 无证据保持 unknown/`blocked_unknown`，receipt-proven success 保留 succeeded/effect；新增 `doc/45-在途Runner取消与Fence语义.md`，本阶段无迁移。
- [x] 新增进程级 `EffectDagAdmissionController`；应用内所有 forward/compensation graph 共享全局、每图、每 Tool 三层配额，graph ring 轮询分配释放的容量且保持 work-conserving。
- [x] admission 严格发生在数据库 node claim 前；容量不足只阻塞进程内 waiter，不创建 ACTIVE claim，协程取消与 graph cancel 均会移除 waiter 且不泄漏容量。
- [x] ready-set checkpoint 升级为 `deskpilot.effect-ready-set.v3` 内容寻址页；membership digest 绑定完整集合，page digest 绑定 cursor/size/total/next/has-more 与页内前驱/分支 proof，跨页或旧 revision claim 会 fail closed。
- [x] `Settings` 新增全局/每图/每 Tool 并发与 ready 页大小配置；单 API 进程默认 8/4/4/64，forward 与补偿共享同一控制器。
- [x] DAG 基础设施边界提升为 1000 节点、单节点 128 前驱、全图 10000 依赖；128 根宽图在 17 节点 proof 页和 8 槽 backpressure 下完成压力验证。
- [x] 新增 `doc/46-DAG公平调度分页与Backpressure.md`；记录 claim 前 admission、公平轮询、页证明、取消竞态、规模边界和下一阶段跨实例控制路由。
- [x] 新增独立 `tool_effect_graph_controls` 持久化控制邮箱；cancel ID 由 graph+command 内容寻址，`(graph_id, command)` 唯一约束保证跨 API 并发/重试只保留一个首次请求事实。
- [x] control router 从数据库 live graph lease 捕获目标 owner/fence；只有 owner ID 与当前 graph fence 同时匹配的 API 能 claim，delivery claim 使用独立单调 fence 和 TTL，旧消费者不能 ack 新投递。
- [x] 非 owner API 的 Task Cancel 会等待远端 fenced ack；live owner 复用 `TaskProcessor.apply_effect_graph_control` 与原 dispatcher，继续先落 graph intent，再按当前 node fence 与原 Runner generation 广播 cancel IPC。
- [x] stale target 会进入 superseded 并重新路由；无 live owner 时新 router 竞争更高 graph fence，只持久化 cancel/reduce 而不猜测或重放遗留 Runner call。同步等待超时返回 `503 EFFECT_GRAPH_CONTROL_PENDING`，durable command 继续投递。
- [x] 新增 Alembic `0019_graph_control_mailbox`、三项 mailbox 配置和 `.env.example`；双 FastAPI 实例集成测试证明请求端零 runtime/零 Runner cancel，远端 owner 的两条 IPC 均发生在 intent 持久化之后。
- [x] 新增 `doc/47-跨实例Graph取消控制邮箱.md`；记录 Outbox 不可替代控制总线的原因、内容寻址消息、两层 fence、接管路径、超时语义和下一阶段集群级 admission 入口。
- [x] 新增数据库支持的 `EffectDagClusterAdmissionController`；forward/compensation 在所有 API 间共享全局、每图、每 Tool 配额，按 graph 最近 grant sequence 做 work-conserving 公平分配。
- [x] admission ticket 使用数据库时间 TTL、心跳、单调 grant sequence 和 fencing token；过期 permit 可由其他实例回收，旧续租/释放与旧 node claim proof 均 fail closed。
- [x] node claim 事务精确 CAS admission ID、graph/node、owner、未过期状态与 fence 后才生成 ACTIVE claim；容量不足或 graph cancel 期间仍保持零 claim、零 Runner IPC。
- [x] 集群容量配置用规范 JSON 摘要绑定；live pending/granted 存在时配置不一致 fail closed，仅在 admission 空闲后允许切换。
- [x] 新增 Alembic `0020_cluster_dag_admission`、租约/轮询配置和 `.env.example`；双连接/双 dispatcher SQLite 集成测试证明严格集群容量与跨 graph 交替 grant。
- [x] 新增 `doc/48-集群级DAG-Admission与容量Fence.md`；记录持久化 ticket、CAS 调度、公平算法、node-claim proof、permit-loss 与下一阶段增量 ready 投影入口。
- [x] 新增每 graph ready state 与每 node 依赖计数投影；新图在定义事务中初始化，node succeeded/离开 succeeded、branch decision、cancel/reducer/compensation 事件在原事务内增量推进 hash-chain。
- [x] ready checkpoint 升级为 v4：数据库 COUNT/索引分页后只加载当页 node、incoming edge、前驱与相关 decision，不再把完整 graph membership materialize 到应用进程。
- [x] claim 使用 checkpoint 数据库时间重查同一页，精确绑定 graph fence/event、projection revision/digest、node revision/claim fence、当页 predecessor/branch proof，再接 admission proof 与 node CAS。
- [x] 旧库投影缺失或 state event/行数漂移时执行一次 O(V+E) lazy rebuild；稳态 node event 只更新直接后继，projection 行摘要或 decision proof 篡改 fail closed。
- [x] 新增 Alembic `0021_incremental_ready`；512 根宽图在禁用全图 loader 后仍完成 7 节点分页、claim 和下一页，证明稳态路径不再依赖全图重算。
- [x] 新增 `doc/49-增量Ready投影与v4分页证明.md`；记录增量算法、v4 截面证明、复杂度变化、修复边界与统一运维面入口。
- [x] 新增统一 `EffectRuntimeOperationsService`；一次受保护查询同时投影 graph-control、cluster admission、ready projection、Outbox/Inbox 的数据库时间指标、告警和有界脱敏样本。
- [x] 运维 snapshot 不返回 control reason、Outbox payload/error 原文或 Tool 参数，只暴露身份、状态、revision/fence、时间和内容摘要；查询不 claim、ack、续租、修复或触发外部网络。
- [x] 新增 metrics 定时采样和 hash-chain 审计；sequence、request/result digest、previous digest 与 state head 连续校验，详情/序号/链头篡改均 fail closed。
- [x] 新增有界 retention scheduler；只清理安全终态图的终态 control/admission/ready 派生记录、已发布 Outbox 和旧 Inbox receipt，删除前内容清单摘要与审计在同一事务提交。
- [x] DLQ 永不自动删除；显式 requeue 要求可信写来源与幂等键，同事务清除错误原文/claim、重置 attempt、提升 Outbox fence 并追加脱敏审计，提交后才唤醒 publisher。
- [x] ready state 新增累计成功 rebuild 次数和最近耗时；运维面检测 live graph 缺失投影、event/行数漂移，并对 control/admission/ready/Outbox 异常产生稳定告警码。
- [x] 新增 Alembic `0022_effect_runtime_ops`、两张运维审计表、五项 scheduler/retention 配置与受认证 operations API。
- [x] 新增 `doc/50-受保护运行时运维面与Retention审计.md`；记录脱敏边界、审计链、retention 安全矩阵、DLQ fence 和下一阶段真实故障注入入口。
- [x] ready checkpoint 升级为 v5 ordinal keyset；cursor 是前页内容寻址 checkpoint ID，继页只从已重算的前页取得冻结数据库时间与 `last_ordinal`。
- [x] ready page 运行时 SQL 改为 `ordinal > after_ordinal ORDER BY ordinal LIMIT page_size+1`，不再使用 `OFFSET`；全局 COUNT 仍绑定 membership proof。
- [x] claim 事务重新验证 cursor checkpoint ID/digest、graph fence/event、projection revision/digest、前页 ordinal 和当页成员摘要；跨图 cursor、持久化 proof/ordinal 篡改均 fail closed。
- [x] 抽取运行时共享 ready query builder，PostgreSQL 方言编译自动断言 keyset/有界 limit/无 offset。
- [x] 新增显式选入 PostgreSQL 验收门禁；只允许 `postgresql+asyncpg`、库名含 `test` 且二次确认的可抛弃库，覆盖 1000-node `EXPLAIN ANALYZE/BUFFERS`、双独立 engine 单获胜 claim、连接池丢失后 TTL/fence 接管和双实例 audit 行锁串行化；阶段 51 当时无配置时明确 skip，阶段 54 已在 Docker 专用测试库实际通过。
- [x] 新增 `doc/51-Ready-v5-Keyset与PostgreSQL验收门禁.md`；记录 v5 内容证明、实库安全门禁、本地验收与真实故障注入剩余边界。
- [x] 将 PostgreSQL 验收 URL 校验收敛到共享 fail-closed guard；必须显式二次确认、精确使用 `postgresql+asyncpg`，且库名中的 `test` 必须是 `_`/`-` 分隔 token，错误不回显凭据。
- [x] 新增真实 PostgreSQL backend terminate 门禁；在 graph revision/node fence 已写但未 commit 时终止 victim session，断言事务全回滚、原 v5 proof 仍可 claim 且首个可见 fence 为 1。
- [x] 新增长事务 graph 行锁与 `lock_timeout` 门禁；竞争事务必须以 SQLSTATE `55P03` 失败，回滚后 graph/node 保持零部分 claim、零幽灵 fence。
- [x] retention 与 DLQ requeue 在查询幂等事件前统一锁 operations audit state；两个独立 service 同键首次并发被串行化为同一 audit event/digest 和 manifest，而不是在唯一约束处失败。
- [x] 新增 `doc/52-PostgreSQL连接终止与多主幂等门禁.md`；记录实库保护、连接终止/锁超时证明、审计锁顺序、本地验收和下一阶段执行入口。
- [x] 前端新增 operations v1 完整公开类型与五个受认证 API client；snapshot/audit 只读禁缓存，retention/requeue 精确携带幂等键。
- [x] 新增 `useEffectRuntimeOperations`；只采信服务端真值，sample 显式提交，retention/DLQ 传输失败后仅在人工重试相同请求时复用原 key，成功后重新读取 snapshot/audit。
- [x] 新增独立“运行时运维”页面；展示 graph-control、cluster admission、ready projection、Outbox/Inbox 四域指标、稳定告警、fence/摘要样本和 hash-chain 审计，不展示 payload/error/Tool 参数原文。
- [x] retention 与 DLQ requeue 均要求二次确认、提交期互斥和响应身份复核；不做乐观删除/重新入队，不自动重放结果不明的 POST。
- [x] 新增 `doc/53-前端受保护运行时运维台.md`；记录前端信任边界、幂等交互、脱敏视图、测试与后续 audit/alert/broker 入口。
- [x] 启动 Docker Desktop 既有 `deskpilot-postgres`，仅连接可抛弃 `deskpilot_test`；PostgreSQL 17.10 上实际通过迁移、1000-node keyset EXPLAIN/BUFFERS、dual-engine 单获胜、pool drop TTL/fence 接管、backend terminate 回滚、SQLSTATE `55P03` 与 retention 多主同键幂等门禁。
- [x] 修复真库暴露的 Alembic `version_num VARCHAR(32)` 长 revision 截断；online migration 对 PostgreSQL 空库创建或无损扩宽为 `VARCHAR(128)`，并新增 revision 容量与真库列长度断言。
- [x] 修复 PostgreSQL 原生 node `UPDATE RETURNING` 的 ORM identity-map 提前同步；claim 显式禁用 session synchronization，继续以返回行验证 revision/fence，保持 claim-before-runner 与事务 fence。
- [x] 收敛任务垂直切片测试的 Windows 时序假设；复用同文件统一 5 秒有界等待，替代硬编码约 1 秒轮询，完整回归第二轮稳定通过。
- [x] 新增 `doc/54-Docker-PostgreSQL真库验收与兼容修复.md`；记录真库环境、两项 SQLite 隐藏缺陷、修复、实际门禁结果和剩余 OS/server/network/broker 边界。
- [x] 新增 PostgreSQL JSON plan baseline v1；保存完整原始 plan、参数化 SQL shape、PostgreSQL 版本、时延、rows、shared buffer hit/read、scan node 和索引名，使用原子 JSON 写入且不保存连接凭据。
- [x] 1000-node 真库门禁默认比较 PostgreSQL 17 版本化基线；严格检查 workload/query shape/major/plan/index，按版本化阈值检查 scan rows、执行时间和 shared blocks，只有显式 `record` 才重写基线。
- [x] 在 PostgreSQL 17.10 上先生成、再用全新 1000-node graph 比较通过；首份摘要为 101 root rows、202 累计 scan rows、311 hit/0 read、0.292 ms execution，使用 ready ordinal 唯一索引和 node 主键。
- [x] 新增 `doc/55-PostgreSQL-JSON-Plan版本化基线.md`；记录 schema、生成/比较命令、阈值、安全边界和阶段 55 后续进程级故障注入顺序。
- [x] 新增可执行 `tests.fault_injection.api_claim_after_commit`；最小 API claimant 使用生产 `Database + TaskService` 提交 graph lease/ready proof/node claim 后才输出无凭据 checkpoint，随后停在 Runner 派发前等待父测试强杀。
- [x] Windows 门禁区分 venv launcher PID 与实际解释器 PID；核对 checkpoint 的 parent/actual PID 后终止实际进程并确认父子均退出，避免只杀 launcher 留下 claimant。
- [x] PostgreSQL 17.10 实测 API kill 后仅一条 committed node claim、graph/node fence=1 且 Tool ledger=0；数据库 TTL 后由新 API 精确接管为 graph/node fence=2，旧 graph owner/fence 与旧 node owner/fence 分别拒绝，拒绝事务零 revision 漂移，当前 fence 可继续提交成功。
- [x] `FileMoveCommitProvider` 新增默认关闭的持久化边界 observer；生产组合不读故障环境变量，测试 Runner 才能在 prepared/committing/外部效果已落地/committed 精确 checkpoint 阻塞。
- [x] 新增真实 Runner OS 进程强杀门禁；prepared 后 kill 重启恢复为 `no_effect`，文件未移动且无 receipt；OS move 已完成但 receipt 未写入时 kill，重启按 destination 版本恢复并持久化 committed receipt。
- [x] 新增 committing 后状态不可证门禁；重启只查询 receipt 不重放 Tool，journal 固定为 `unknown`，真实 Policy/一次性审批/Tool ledger 原子进入 `waiting_reconciliation`，恰好一条 pending reconciliation 且无 `tool.completed`。
- [x] 完整回归收敛两个 Windows harness 竞态：慢 Runner 有界等待统一为 5 秒；SQLite rollback journal 短暂独占时做 2 秒有界重试，持续锁定仍失败且明文 key 不落库断言不变。两个目标用例各连续三次通过后，379 项全量通过。
- [x] 新增额外显式选入的 PostgreSQL 专用容器 restart 门禁；重启前 fail closed 核对固定容器、Compose project/service、loopback 端口、healthy 状态和专用数据卷，测试 URL 和凭据不进入 Docker 命令或日志。
- [x] PostgreSQL 17.10 实测 restart 使旧 backend 连接 invalidated 且 postmaster 启动时间递增；恢复后 graph/node/Outbox 只按数据库 `current_timestamp` 等待 TTL，三域 fence 都从 1 接管为 2。
- [x] restart 接管后旧 graph owner/fence、当前 graph 配旧 node owner/fence、旧 publisher owner/fence/delivery 全拒绝；拒绝零 graph/node revision 漂移、零 Outbox claim identity 漂移、Tool ledger=0，当前 publisher/node fence 可继续完成 ack 与 node success。
- [x] restart 专项为 `1 passed`；与既有 backend terminate/lock timeout、API process kill、1000-node plan/dual-engine/pool-drop 真库门禁合并为 `4 passed, 376 deselected`；新门禁 Ruff 与生产源码+门禁 134 文件 mypy 通过。
- [x] 新增 `test_postgresql_transaction_fault_injection.py`；`statement_timeout=250ms` 在 graph revision 与 node claim 已暂存后精确命中 SQLSTATE `57014`，整事务回滚后 graph/node revision 不变、node pending/owner 空/fence=0、Tool ledger=0，原 ready proof 仍可颁发第一个可见 fence=1。
- [x] 新增两 backend 反序更新两 node 的真实多行 deadlock；恰好一个 SQLSTATE `40P01` 与一个 committed，最终 node 只有胜者 owner，且受害者在互锁前已写入的独占 witness 回到原值，直接证明零部分提交。
- [x] 新增生产 `TaskService.finish_tool_call()` terminal commit 连接中断；测试专用 session 在 transaction commit 前终止自身 backend，connection-invalidated 后无部分 terminal ledger/event/Outbox，startup recovery 将唯一 running call 精确收敛为一次 `unknown`+一条 pending reconciliation，无新 call、无 `tool.completed`、无透明重放。
- [x] 阶段 56 专项 `3 passed`，全部 PostgreSQL marker `7 passed, 376 deselected`；Ruff 全仓、mypy 133 个生产源码/生产+新门禁 134 文件通过，显式启用专用 PostgreSQL 与容器 restart 的后端全量为 `383 passed, 1 warning`、无 skip。
- [x] 新增 `doc/56-PostgreSQL事务超时死锁与连接中断门禁.md`；记录 SQLSTATE、事务 witness、unknown/不重放证据、安全 guard 和真库结果。
- [x] 新增 `aio-pika 9.6.2` 与可选 RabbitMQ transport；默认 `local` 不联网，显式 RabbitMQ 模式使用 durable direct exchange/queue、persistent message、mandatory publish、publisher confirm、固定 prefetch 和 manual ack。
- [x] 生产组合根保留本地 EventBroker 作为 WebSocket 实时扇出；RabbitMQ consumer 先提交事务 Inbox，再扇出并 ack，数据库 replay 继续是真值。
- [x] 新增 fail-closed RabbitMQ guard 与 loopback Docker Compose；真实门禁精确证明 confirm 后响应丢失、新 delivery/fence 重发、旧 publisher ack/fail 全拒绝、ack 前断连后的 `redelivered=true`，以及两个 delivery/一次 Inbox handler/一次实时扇出。
- [x] 真实 transport 停机连续失败进入数据库 DLQ；人工 requeue 清除旧 delivery、提升 fence，RabbitMQ 恢复后以新 delivery/fence 精确处理一次。
- [x] RabbitMQ 3.13.7 专项 `1 passed, 1 warning in 3.72s`；配置守卫、durable Outbox 内容绑定与可靠投递回归 `11 passed`；专用测试容器和 test-only volume 已在验证后删除。
- [x] 阶段 57 最终默认后端全量 `385 passed, 8 skipped, 1 warning in 581.44s`；8 个 skip 均为未注入 URL 的显式外部门禁。Ruff 全仓、mypy 135 个生产源码、`uv lock --check`、Alembic current/check 全通过。
- [x] 新增 `doc/57-RabbitMQ真实Broker重投与Inbox门禁.md`；记录拓扑、confirm/ack 不确定边界、Inbox 证据、DLQ/fence、安全 guard 与未覆盖的 quorum/restart/network 边界。
- [x] 新增 Alembic `0023_ready_membership`：ready state 持久化 membership version、projection row count 与 ready count，ready node 持久化 membership boolean，并增加 membership keyset 与 graph/claim-expiry 索引。
- [x] ready v6 将 claim、node transition、直接 successor、branch decision 和 rebuild 的 membership/count 差量绑定到同一事务与 hash chain；旧 v5 checkpoint、旧 membership version 和非法 count 边界全部 fail closed。
- [x] checkpoint 按数据库时间和 state row lock 接管过期 claim；cursor 固定 database time，接管导致 projection revision 变化时拒绝旧 cursor，跨实例 identity-map 使用 `populate_existing` 防止旧对象掩盖并发更新。
- [x] 删除稳态 ready membership `COUNT` 与 `_ensure` 的 projection/node 双 COUNT；SQL capture 证明完整 checkpoint 零 `COUNT(`，state/row tamper 门禁和 operations 审计分别拒绝或报告 count/membership 漂移。
- [x] PostgreSQL 17.10 真实录制 `ready-v6-membership-1000-nodes` JSON plan：page+sentinel 101 行、scan rows 202、rows removed 0；record 后 compare 通过，双 engine 同 proof/node 恰好一方成功且 state count 精确从 1000 降为 999。
- [x] 真库迁移发现并修复 PostgreSQL boolean 与 integer 比较不兼容；SQLite `head -> 0022 -> head`、PostgreSQL head 升级、开发库 `upgrade/current/check` 均通过且无 metadata 漂移。
- [x] 阶段 58 默认后端全量 `389 passed, 8 skipped, 1 warning`（397 collected）；Ruff 全仓、mypy 136 个生产源码、`uv lock --check` 全通过；另有 PostgreSQL v6 plan/双 engine 专项真实通过。
- [x] 新增 `doc/58-Ready-membership-count投影与漂移门禁.md`；记录计数模型、TTL 接管、证明链、漂移成本边界、PostgreSQL 基线与后续 admission 分片入口。
- [x] 新增 Alembic `0024_admission_shards`：16 个持久化调度 shard、ticket `scheduling_shard`、公平索引和 PostgreSQL 全局 grant sequence；旧 ticket 使用与运行时一致的 SHA-256 规则回填，不集中到 shard 0。
- [x] PostgreSQL admission grant 不再写 singleton global state CAS；graph 确定性落 shard，调度以 `FOR UPDATE SKIP LOCKED` 领取最久未服务 shard，每 turn 最多 grant 一张，避免单热点 shard 吃满全局容量。
- [x] 跨 shard 全局/每 Tool 容量由 PostgreSQL `SERIALIZABLE` SSI predicate conflict 保护；SQLSTATE `40001/40P01` 有界重试，sequence 只保证单调且允许回滚/空 turn 间隙。同 graph 固定 shard 保持 graph 内串行选择。
- [x] permit release 增加数据库 TTL 门禁；真实 PostgreSQL 四 engine 在 global=2、per-graph=1、两个 Tool 各=1 下完成两轮四 shard 公平服务，过期 permit replacement 接管后旧 release/renew fence 全拒绝，global config revision 在 grant 期间不变。
- [x] 新增 16000-ticket PostgreSQL 17.10 JSON plan：16 shard 各 1000 pending，候选 page+sentinel 返回/扫描均为 101 行，使用 shard route index，`Limit + Index Scan` 且无 Sort；record 后 compare 并额外连续 compare 5 次通过。
- [x] 阶段 59 默认后端全量 `391 passed, 9 skipped, 1 warning`（400 collected）；Ruff 全仓、mypy 138 个生产源码、`uv lock --check` 全通过；开发 SQLite 与测试 PostgreSQL 都为 `0024_admission_shards (head)` 且 Alembic check 无漂移。
- [x] 新增 `doc/59-Admission分片与PostgreSQL原生调度.md`；记录 hash/shard 迁移、SSI 容量证明、公平 turn、TTL/fence、候选成本边界、plan 基线和 graph-control 下一入口。
- [x] PostgreSQL graph-control owner claim 收敛为单条 `UPDATE ... RETURNING`；先以 route 复合索引锁定有界有序前缀，再用 graph 主键验证当前 owner、数据库 lease TTL 和 target fencing token，`FOR UPDATE SKIP LOCKED` 使多 engine 不等待且不重复领取；SQLite 保留逐条 CAS 兼容路径。
- [x] PostgreSQL `route_pending` 增加 control 行 `SKIP LOCKED`，避免旧 route pass 覆写并发新 claim；ack、renew、retry 全部增加 `claim_expires_at > database_now`，过期 delivery 即使 owner/fence 尚未被接管也不能回写。
- [x] 新增 Alembic `0025_graph_control_claims`：route 索引从 `(status,target_owner_id,available_at)` 扩展为再含 `(created_at,control_id)`；SQLite 和 `deskpilot_test` 均完成 `0025 -> 0024 -> 0025` 往返，head metadata check 无漂移。
- [x] 真实 PostgreSQL 四 engine 对 12 个 mailbox control 并发领取，合计恰好 12 个且无重复；数据库 TTL 后旧 ack/renew/retry 全拒绝，同 owner claim fence 从 1 接管为 2，graph owner B 接管后旧 owner 无法领取且新 claim fence 为 3。
- [x] 新增 16000-control PostgreSQL 17.10 JSON plan：16 owner 各 1000 pending，batch 101，root rows 101、scan rows 303、rows removed 0；严格使用 route/control PK/graph PK 三个索引，无 Sort、Bitmap Heap Scan 或 Seq Scan，record 基线后 compare 通过。
- [x] 阶段 60 默认后端全量 `393 passed, 11 skipped, 1 warning`（404 collected）；Ruff 全仓规则检查、本阶段文件 format check、mypy 139 个生产源码、`uv lock --check` 全通过；本阶段无前端代码变更。
- [x] 新增 `doc/60-Graph-control-PostgreSQL原生批量Claim.md`；记录单语句 claim、route/claim 竞态、数据库 TTL/fence、索引迁移、16000-control plan、RabbitMQ 非正确性依赖和下一阶段告警/audit 入口。
- [x] 新增 Alembic `0026_alert_notifications`：operations state 增加 alert sequence/digest head，并新增当前 alert lifecycle state 与 append-only notification 表；SQLite/PostgreSQL 均完成 `0026 -> 0025 -> 0026` 往返和 metadata check。
- [x] `metrics.sampled` 事务内按稳定 code 维护 opened/updated/resolved；未变化告警不重复通知，恢复 count 归零，再次出现以更大 revision 重开。通知 hash-chain 绑定 snapshot digest、metrics audit ID/sequence/result digest 与数据库时间。
- [x] 新增告警通知 keyset API；page+sentinel 校验 notification chain，并批量交叉验证关联 audit action/sequence/result digest。事务后 wake callback 失败不回滚数据库真值，生产不依赖 RabbitMQ。
- [x] 普通 operations audit 页先冻结 state head，再查询 `after < sequence <= through`，消除并发 append 导致的尾页假漂移；新增 frozen export ID、opaque canonical cursor、after/through anchor、page digest 和 cursor 篡改拒绝。
- [x] 前端运维台展示告警 lifecycle 历史；完整导出逐页复核 export identity、database time、through、连续 sequence、has-more/cursor 和终点 digest，通过后才下载脱敏 JSON bundle。
- [x] PostgreSQL 17.10 两个独立 engine 同时采样同一 ready drift，两条 metrics audit 合法提交但恰好一条 opened notification；冻结 export 后并发追加的新 audit 不进入旧 export，新 export head 精确增加 1。
- [x] 阶段 61 默认后端全量 `396 passed, 12 skipped, 1 warning`（408 collected）；Ruff、mypy 140 个生产源码/生产+两项门禁 142 文件、`uv lock --check` 全通过；前端 17 文件/134 项测试、type-check 和 build 全部通过。
- [x] 新增 `doc/61-运行时告警通知与Audit冻结导出.md`；记录 lifecycle、双链绑定、冻结 cursor/export、前端完整性复核、通知 transport 边界和下一阶段真实条件业务图入口。
- [x] 新增结构化 `disk_pressure_guarded_file_move`：API 规范化源/目标并从目标父目录派生磁盘检查路径，阈值限制为 0～100；图固定为 `inspect_capacity -> move_file/confirm_deferred` 两个条件分支，客户端不能自定义边或 outcome。
- [x] 新增受信 disk-pressure resolver；只接受持久化成功的 `computer.disk_usage@1.0.0` terminal event、确定性 call ID、精确 contract digest/schema/resolved path，以 Tool 输出摘要、event identity、阈值、比较符和 outcome 形成 evidence digest，模型不参与分支。
- [x] v2 preparer 改为按节点不可变 Tool Contract 记录 risk/idempotency；R0 磁盘节点精确 local-user scope 自动允许，move 分支先提交 branch decision/ready proof 再创建唯一 R1 一次性审批，defer 分支无写 call/审批且源文件保持原位。
- [x] Dispatcher 新增默认关闭的 decision barrier；业务图在 decision 后先返回外层建立选中节点 Tool/policy ledger，再进入 admission/claim，通用 dispatcher 行为不变。
- [x] 前端新增条件图表单、0～100 阈值校验、`effect.branch.decided`/skip 明确标签和任务快照分支摘要；浏览器只展示服务端数据库事件，不重新计算 outcome。
- [x] 阶段 62 后端第二次全量 `399 passed, 12 skipped, 1 warning`（411 collected）；Ruff、mypy 140 个生产源码、Alembic head/check、`uv lock --check` 全通过；前端 17 文件/136 项测试、type-check 和 build 全部通过。
- [x] 新增 `doc/62-磁盘压力保护文件移动条件业务图.md`；记录固定图、受信证据、混合 R0/R1 账本、decision barrier、前端演示、安全边界和知识库下一入口。
- [x] 阶段 63 新增本地知识库最小只读闭环：显式单文件 `.md/.txt` 导入、1 MiB/UTF-8/普通文件边界、内容寻址 Artifact/Chunk、来源版本重验证、确定性本地检索和 Citation/result proof。
- [x] 新增 Alembic `0027_local_knowledge` 与受保护 `sources/import/search` API；完整 Chunk 集合、parser/chunker 版本、manifest 或来源版本不一致时 fail closed，绝不返回部分旧结果。
- [x] 前端启用知识库导航、路径导入、来源列表、行 locator/证明命中和 stale 提示；浏览器不扫描磁盘或自行推断证据。
- [x] 新增 `doc/63-本地知识库最小只读闭环.md`；记录来源、解析、证明、API、前端和下一阶段 MCP 信任边界。
- [x] 阶段 63 默认后端全量 `403 passed, 12 skipped, 1 warning`（415 collected）；Ruff、mypy 144 个生产源码、Alembic head/check、`uv lock --check` 全通过；前端 18 文件/137 项测试、type-check 和 build 全部通过。
- [x] 阶段 64 实现 MCP `2025-11-25` stdio lifecycle：固定内置 Server、默认禁用、显式 enable/disable、initialize/initialized、tools/list/call、严格版本/能力/Schema/structured content 校验和分级超时回收。
- [x] MCP Host 使用短生命周期进程、isolated Python、临时 cwd、最小无凭据环境和 bundle SHA-256 启动前复核；不提供 roots/sampling/elicitation/tasks，Server annotations 不能覆盖本地 R0 风险下限。
- [x] 新增 Alembic `0028_controlled_mcp`，持久化 manifest-bound enable state 和 enabled/disabled/tool_called/tool_failed hash-chain 审计；原始文本、异常正文和绝对命令不入审计，chain/head 损坏 fail closed。
- [x] 前端启用“Agent 与 MCP”控制台，展示固定命令、协议、网络/roots/client capabilities、风险/Schema/bundle digest、显式启用、只读调用结果与脱敏审计。
- [x] 新增 `doc/64-受控MCP-stdio最小闭环.md`，记录官方协议映射、信任边界、生命周期、审计、强隔离缺口和评测下一入口。
- [x] 阶段 64 默认后端全量 `409 passed, 12 skipped, 1 warning`（421 collected）；Ruff、mypy 151 个生产源码、Alembic head/check、`uv lock --check` 全通过；前端 19 文件/138 项测试、type-check 和 build 全部通过。
- [x] 阶段 65 新增严格 `deskpilot.golden-suite.v1` YAML Loader 与三个固定真实场景：MCP stdio structured output、bundle 篡改拒绝、知识来源变化后旧 Citation 失效；不接受上传套件或任意 handler。
- [x] 新增 Alembic `0029_evaluation_traces`：持久化 Run/result manifest、成功/安全计数、耗时、replay 血缘和逐 Case trace hash-chain；manifest、投影或 trace 篡改均 fail closed。
- [x] Replay 重新执行真实场景并比较语义摘要；suite digest 漂移拒绝，耗时不参与语义一致性；预期不匹配稳定归类为 `EXPECTATION_MISMATCH`。
- [x] 前端新增“评测与 Trace”面板，显示成功率、安全通过率、总耗时、replay match、案例 trace 与持久化历史。
- [x] 新增 `doc/65-版本化黄金任务与Trace-Replay.md`，记录套件边界、三场景、record/replay 证明和扩展到 20 任务的入口。
- [x] 阶段 65 默认后端全量 `414 passed, 12 skipped, 1 warning`（426 collected）；Ruff、mypy 156 个生产源码、Alembic head/check、`uv lock --check` 全通过；前端 20 文件/139 项测试、type-check 和 build 全部通过。
- [x] 阶段 66 将内置套件升级为 version 2 的 20 个固定案例，覆盖 MCP/知识基础行为、429 retry budget、Runner 单/双崩溃恢复、WebSocket disconnect 和三类 MCP 协议异常；十一项为 safety case。
- [x] 新增 `deskpilot.evaluation-report.v1`：从已验证 Run/Trace 计算跨运行趋势、运行与 Case p50/p95、失败分类和稳定 report digest；任一历史证据损坏时整份报告 fail closed。
- [x] 新增报告查询/JSON 下载 API；前端显示报告摘要、分位数、失败聚合、趋势并支持鉴权下载，不在浏览器重算证据。
- [x] 新增 `doc/66-二十黄金任务与版本化趋势报告.md`，记录固定场景、故障注入真实性边界、nearest-rank 统计和 OpenTelemetry 下一入口。
- [x] 阶段 66 默认后端全量 `414 passed, 12 skipped, 1 warning`（426 collected）；Ruff、mypy 160 个生产源码、Alembic head/check、migration 回归与 `uv lock --check` 全通过；前端 20 文件/139 项测试、type-check 和 build 全部通过。
- [x] 阶段 67 引入 OpenTelemetry API/SDK 与 `deskpilot.telemetry-schema.v1` 默认拒绝属性注册表；Facade 和本地 exporter 双重过滤，关闭异常正文/stack 自动记录，export/flush/shutdown 失败不进入领域结果。
- [x] 任务、模型、Runner Tool、MCP、Evaluation Run/Case 接入短 span；本地有界 store 支持真实 OTel trace ID 或 `trc_...` TaskCorrelationId 查询，低基数 metric 只使用 category/outcome，不含实例 ID。
- [x] 新增受保护 `GET /telemetry/traces`、`GET /telemetry/metrics`，均为 `no-store`；OTel 数据继续不参与任务事务、授权、恢复、Receipt/Verification 或 Evaluation report digest。
- [x] 新增不可变 `deskpilot.evaluation-baseline.v1`：绑定 suite ID/version/digest，success/safety 均为 100% 硬门禁，Windows run/case p95 上限分别为 30s/10s；`record` 要求显式环境变量、拒绝 CI 和覆盖已有文件。
- [x] 新增 `.github/workflows/phase-67-telemetry-evaluation-gate.yml`：锁定依赖、运行 telemetry/evaluation contract、Ruff/mypy、只读 compare，并以 Git diff guard 防止静默改写基线。
- [x] 新增 `doc/67-脱敏OpenTelemetry与回归基线CI门禁.md`，记录三套真值边界、脱敏注册表、本地查询、阈值、record/compare 和后续远程 OTLP/多 Agent 扩展边界。
- [x] 阶段 68 新增不可变 `deskpilot.agent-contract.v1`、内容寻址 Prompt Package 和严格 Loader；拒绝重复 JSON key、未知字段、类型宽松转换、超限、符号链接、目录逃逸和秘密标记。
- [x] 新增受信启动期 `AgentRegistry`，对 I/O Schema、Prompt digest、Tool 精确版本/digest/风险、Model capability 和 Handoff 反向边/无环图做交叉校验；freeze 后禁止动态注册。
- [x] 固定注册 `builtin.computer_observer`、`builtin.knowledge_researcher`、`builtin.task_synthesizer` 三个 v1 Agent；Supervisor 不是 Agent，三者均禁止 Memory write，模型能力不足时只 disabled 并拒绝绑定。
- [x] 新增脱敏只读 `/api/v1/agents`、精确版本与 Registry snapshot API；只返回 Schema digest/公开 Contract 投影，不返回 Prompt 正文、路径、callable、凭据或动态代码。
- [x] 新增 `AgentPlanBinder`，将不可信 selector 封装为精确 Agent version/Contract digest/Prompt digest/Tool grant/预算；拒绝伪造 digest、越权 Tool、超额预算与同版本漂移。
- [x] 新增阶段 68 Windows CI 门禁：专项验收、Ruff/mypy 与 wheel Prompt 资源打包复核；实现边界见 `doc/68-Agent-Contract与Registry最小闭环.md`。
- [x] 阶段 69 新增严格不可变的 Conversation/Message/TurnInterpretation、Task Contract/Amendment、DraftPlan、Capability Pack、Research/Workspace/Browser Verify 和 ExecutablePlan 领域合同；文本或 Draft 不能携带 approval、凭据、可信摘要、Shell/Python 权限。
- [x] 新增纯确定性 Plan Compiler：精确绑定 Agent/Prompt/Tool/Capability 版本摘要，校验 DAG、Handoff、风险、隐私外发、分节点预算总和和 Acceptance Coverage，并为 Plan/node/binding 生成规范内容摘要。
- [x] 新增 `research.read.v1`、`artifact.html.v1`、`browser.verify.v1` 固定 Catalog 和五节点 `research_to_html` fixture；三项 runtime 在阶段 69 全部禁用，因此计划可编译、可检查但不可执行。
- [x] 新增 `0030_task_contract_plans`：不可变 Contract version、Plan generation 和可变活动指针；内部服务在单事务内执行连续版本链校验、编译、上一代 supersede 与活动代激活，读取时复核 manifest、摘要与 Registry/Catalog 漂移。
- [x] 新增 Capability/Contract/Plan/Planning State 只读 API，均 `Cache-Control: no-store`；无公共规划写入口。新增阶段 69 Windows CI 门禁、专项负向测试、迁移往返和数据库篡改拒绝；实现边界见 `doc/69-Task-Contract与Executable-Plan-Compiler最小闭环.md`。
- [x] 阶段 70 新增持久 Execution Run/Node/Edge、不可变 Handoff、Invocation、Agent Model Turn/Result 和 `0031_agent_research_runtime`；ready/claim/lease/fence 上限为 3，过期 dispatch 进入 `outcome_unknown`，陈旧 fence 被拒绝。
- [x] 新增 `research.read.v1@1.1.0` 显式运行开关和 `builtin.web_researcher`；SearchProvider 与 ModelGateway 分离，SearXNG adapter 与 DNS-pinned PageReader 拒绝非公网/metadata/重定向绕过、非允许 MIME、压缩和超大正文。
- [x] `research_to_html` 前半链可持久化 SearchHit/PageSnapshot/ResearchClaim/CitationEvidence，但 AgentResult 只能是 `candidate`，Run/Node/Claim/Citation 只到 `awaiting_verification`，不会解锁 Builder 或提升 Task 成功。阶段 70 CI 继续执行阶段 67 冻结 evaluation baseline compare；实现边界见 `doc/70-持久Agent-Invocation与只读联网研究最小闭环.md`。
- [x] 阶段 71 新增独立 Citation Verifier、immutable Evidence Snapshot、逐 Claim Verdict 和可信 verified-edge reducer；重验 Result/Claim/Citation/PageSnapshot digest、Task 血缘、locator 原文和来源下限后，只有 `verified` 源节点能解锁后继 `ready`。
- [x] 新增受控 Task Artifact Workspace、内容寻址 blob、immutable ArtifactRevision/PatchReceipt、只消费 verified Claim 的确定性 HTML Builder，以及无 JavaScript/无登录/断网新 profile 的 Chromium/Edge Browser Verifier。
- [x] Final Acceptance 已重验 Claim、Artifact/Browser revision 和零外网不变量，生成 DeliveryManifest 并将 `research_to_html` Task/Run 提升为 `succeeded`。新增 `0032_verified_artifact_delivery`、阶段 71 专项对抗/迁移测试和 CI；实现边界见 `doc/71-独立Verification与Artifact-Browser交付闭环.md`。
- [x] 阶段 72 新增 Conversation/Message、task-scope WorkingMemoryItem、TTL/显式删除和 Task Contract 确定性投影；Agent/网页 adapter 没有 active-memory 写权限。
- [x] 每个实际 Research Model Turn 在 Provider 调用前生成 ContextRequest/ContextManifest，记录精确 scope、允许 source、包含/排除原因、authority/trust/classification、token、Provider location、出境决定和 digest；用户可查询 retained items 与 invocation Manifest。
- [x] PageSnapshot 固定为 `data + untrusted_external_content`，可供研究但不能进入 active Memory；只有独立 Verification 已通过的 Claim 可成为 verified ContextItem。新增 `0033_context_working_memory`、专项对抗/迁移测试和阶段 72 CI；实现边界见 `doc/72-会话工作记忆与ContextManifest.md`。
- [x] 阶段 73 新增受保护 MemoryProposal、版本化 MemoryItem、可见 Conflict、TTL、无明文 tombstone 和实际 Agent/Provider usage ledger；用户事实、Agent 推断和 verified episode 都不能自动 active。
- [x] Context Builder 只召回无冲突且未过期的 active 长期记忆，cloud Provider 排除 sensitive value；前端长期记忆账本已支持来源/版本/TTL/去向展示及 create/confirm/reject/edit/delete/export。新增 `0034_long_term_memory`、阶段 73 CI；实现边界见 `doc/73-长期记忆确认冲突与遗忘.md`。
- [x] 阶段 74 新增确定性 `CompactionSnapshot`、完整 source refs、逐字段 coverage、conflict/stale、parent lineage 和 `0035_context_compaction`；相同输入稳定，rebuild 不形成无来源摘要链。
- [x] Context 超预算时只压缩 coverage 完整的 Working Memory，压缩后重新校验 classification/egress/ModelRequest digest；删除、长期记忆失效、Contract amendment 或证明篡改均 fail closed。实现边界见 `doc/74-可证明上下文压缩与重建.md`。
- [x] 新增并更新 `doc/多Agent后续技术架构讨论总纲.md`，把 Plan Compiler、Agent Model Loop、跨层恢复、部署、可观测、评测、用户控制面、第三方供应链及通用对话/联网研究/Artifact 拆成 D1～D9 讨论队列；阶段 67～69 已完成对应最小闭环，工程断点移至阶段 70 持久 Invocation 与只读联网研究，其余设计项不代表 Runtime 已实现。
- [x] 新增 D1 设计 `doc/Task-Contract与ExecutablePlan-Compiler技术设计.md`，细化不可变 Contract version、Draft/Bound/Executable Plan、纯 Compiler、分层校验、acceptance coverage、原子激活和 Replan generation；阶段 69 已实现其最小闭环，尚未实现的扩展参数继续保持候选。
- [x] 新增 D2 候选设计 `doc/Agent-Model-Loop与Prompt-Package技术设计.md`，细化 Prompt Package/Renderer、Context freeze/delta、持久化 Model Loop、Decision union、`tool_binding_id`、`ModelDispatchAttempt`、Provider unknown、预算和 no-progress；九项关键取舍仍待确认，不能视为已实现。
- [x] 新增 D3 候选设计 `doc/多Agent跨层故障与恢复矩阵技术设计.md`，按 certainty/effect/uncertainty/action/owner 建立 API、Plan、Broker、Scheduler、Model、Tool、Context、Verification、Deliverer 故障矩阵，并提出 lifecycle/risk 双投影与机器可读恢复规范；八项关键取舍仍待确认。
- [x] 新增 D4 候选设计 `doc/多Agent-Scheduler与部署拓扑技术设计.md`，细化 RuntimeWorkItem、短 reducer、资源向量 admission、公平/保留容量、Worker capability、device affinity、Embedded/Desktop/PostgreSQL 部署和 rolling upgrade；九项关键取舍仍待确认。
- [x] 新增 D5 候选设计 `doc/多Agent可观测性技术设计.md`，区分 Domain/Audit、Evaluation Trace 与 OTel 诊断投影，细化 TaskCorrelationId 下多个短 trace episode、异步 links、默认拒绝属性注册表、低基数指标、本地优先导出、采样、保留和 telemetry CI contract；阶段 67 已落地通用最小骨架，多 Agent 专属 episode/link/Verification/Memory 扩展仍待后续阶段实现。
- [x] 新增 D6 候选设计 `doc/多Agent评测与CI门禁技术设计.md`，细化五层 suite、不可变 EvaluationPlan、独立 External Oracle、Verifier mutant/混淆矩阵、false-success 硬门禁、相关性错误、统计非劣效、完整 cohort、显式 record/review/approve/compare、quarantine 与 release attestation；十一项关键取舍仍待确认，且现有二十 case 不被冒充为多 Agent 准确性证明。
- [x] 新增 D7 候选设计 `doc/多Agent用户控制面技术设计.md`，细化服务端 TaskViewProjection/ActionAvailability、持久 UserCommandReceipt、正交 lifecycle/control/outcome/risk/verification、Pause/Cancel 精确语义、Contract/Plan/Approval/Reconciliation 分层、多活动任务、Attention Queue、Evidence/Memory/Context 使用与删除传播；十一项关键取舍仍待确认，且不代表多 Agent 控制面已实现。
- [x] 新增 D8 候选设计 `doc/第三方Agent与插件供应链技术设计.md`，区分声明式 Agent/MCP/可执行 Tool/Provider/UI 风险，细化四层信任、签名 manifest、Publisher/更新元数据、SBOM/provenance、Capability Grant、隔离、版本冻结、撤销、升级与供应链 CI；十二项关键取舍仍待确认，实施继续保持在阶段 75 之后。
- [x] 更新 `doc/多Agent跨文档决策收敛矩阵.md`，完整追踪 D1～D9 的 90 项来源取舍，识别包括“愿景通用但实现路线窄”在内的十三类冲突/兼容债务，并统一 identity、正交状态、digest profile、authoritative truth、ErrorEnvelope/Recovery 术语；当前登记 43 项主决策、12 项参数候选和 15 份 ADR，其中 DM-036/ADR-014 与 DM-037/ADR-015 已接受，其余仍为候选或待定。
- [x] 接受 `doc/ADR-014-图可视化与LangGraph采用边界.md`：核心 Runtime 不采用 LangGraph，避免与现有 PostgreSQL/Effect Ledger/Verification 形成第二真值；用户控制面阶段采用服务端只读 `GraphViewProjection + Vue Flow + ELK.js`，Mermaid 仅作脱敏导出。当前只完成架构决定，未安装依赖、未实现图投影或界面。
- [x] 接受 `doc/ADR-015-通用任务Agent产品边界与首个纵向切片.md`：产品方向调整为本地优先通用任务 Agent，首个纵向切片固定为 `research_to_html`；阶段 70 完成只读联网研究前半链，阶段 71 已完成独立 Verification 与 Artifact/Browser 交付闭环。
- [x] 新增 `doc/通用对话联网研究与Artifact工作区总体架构.md`：定义 Conversation/Turn/Amendment、Capability Pack、Provider-neutral Search/PageSnapshot/Claim/Citation、外部内容不可信边界、Task Workspace/ArtifactRevision/PatchReceipt、HTML v1、隔离 Browser Verifier、故障恢复、UI 与对抗门禁；除 D9-01 外其余 D9 细节仍为候选或参数待定。

当前 API：

| Method | Path | 作用 |
| --- | --- | --- |
| GET | `/api/v1/health` | 健康与版本 |
| GET | `/api/v1/session` | 受信任前端建立进程级本地会话 |
| GET | `/api/v1/agents` | 查询/筛选冻结 Registry 的脱敏 Agent Descriptor |
| GET | `/api/v1/agents/registry-snapshot` | 查询内容寻址 Registry snapshot |
| GET | `/api/v1/agents/{agent_id}/versions/{version}` | 查询精确 Agent 版本，包括不可执行状态 |
| GET | `/api/v1/model-providers` | 查询版本化、无密钥 Provider catalog 与有效健康缓存，不触发探测 |
| GET | `/api/v1/model-providers/audit` | 按 sequence 查询脱敏 Provider 配置审计 |
| GET | `/api/v1/model-providers/routing` | 查询角色路由、预算、EWMA、费用、重试和熔断安全投影 |
| POST | `/api/v1/model-providers` | 使用 ETag 与幂等键创建 Provider |
| PUT | `/api/v1/model-providers/{provider_id}` | 完整替换 ProviderConfig |
| POST | `/api/v1/model-providers/{provider_id}:enable` | 验证凭据并启用 Provider |
| POST | `/api/v1/model-providers/{provider_id}:disable` | 禁用非默认 Provider |
| POST | `/api/v1/model-providers/{provider_id}:make-default` | 切换 enabled 默认 Provider |
| DELETE | `/api/v1/model-providers/{provider_id}` | 删除非默认 Provider 并保留凭据 |
| GET | `/api/v1/model-providers/{provider_id}/health` | 按需执行单 Provider 脱敏健康探测 |
| POST | `/api/v1/tasks` | 创建普通、显式文件移动、有界 saga/DAG 或 `disk_pressure_guarded_file_move` 条件业务任务并启动 TaskProcessor |
| GET | `/api/v1/tasks?status=&limit=&offset=` | 按状态查询有界、稳定倒序的持久化任务历史 |
| GET | `/api/v1/tasks/{task_id}` | 查询任务快照 |
| GET | `/api/v1/tasks/{task_id}/events?after_seq=N` | 补拉事件 |
| GET | `/api/v1/tasks/{task_id}/effect-graph` | 查询版本化 graph/node/edge/attempt/effect/compensation 与 transition 证明 |
| POST | `/api/v1/tasks/{task_id}:pause` | 在安全点暂停运行中任务 |
| POST | `/api/v1/tasks/{task_id}:resume` | 从当前进程或已证明的跨重启检查点恢复 |
| POST | `/api/v1/tasks/{task_id}:cancel` | 取消非终态任务 |
| GET | `/api/v1/approvals?status=&task_id=` | 按状态/任务查询审批 |
| GET | `/api/v1/approvals/{approval_id}` | 读取完整审批预览 |
| POST | `/api/v1/approvals/{approval_id}:approve` | 以精确 `preview_hash` 批准本次调用 |
| POST | `/api/v1/approvals/{approval_id}:reject` | 以精确 `preview_hash` 拒绝本次调用 |
| GET | `/api/v1/reconciliations?status=&task_id=` | 按状态/任务查询 `unknown` 对账记录 |
| GET | `/api/v1/reconciliations/{reconciliation_id}` | 读取调用摘要、人工裁决和后继任务关联 |
| POST | `/api/v1/reconciliations/{reconciliation_id}:refresh-evidence` | 签名查询 Runner journal，并内容寻址追加 receipt/no-receipt/query-failed 证据 |
| POST | `/api/v1/reconciliations/{reconciliation_id}:resolve` | 使用幂等键提交不可改写的人工裁决 |
| POST | `/api/v1/reconciliations/{reconciliation_id}:recover-graph` | 使用幂等键按已裁决事实继续或终止 blocked graph，绝不重放原 call |
| POST | `/api/v1/reconciliations/{reconciliation_id}:create-attempt` | 仅在确认未产生效果后幂等创建全新任务 |
| POST | `/api/v1/reconciliations/{reconciliation_id}:create-compensation` | 仅从已验证 committed `file.move` receipt 幂等派生全新反向审批任务 |
| GET | `/api/v1/operations/effect-runtime?sample_limit=` | 查询四域数据库时间指标、稳定告警和有界脱敏运维样本 |
| GET | `/api/v1/operations/effect-runtime/audit?after_sequence=&limit=` | 验证并分页读取 metrics/retention/DLQ hash-chain 审计 |
| GET | `/api/v1/operations/effect-runtime/alerts?after_sequence=&limit=` | 分页读取 opened/updated/resolved 告警通知 hash-chain |
| GET | `/api/v1/operations/effect-runtime/audit/export?cursor=&limit=` | 按冻结 head 与 opaque cursor 导出完整脱敏 audit |
| POST | `/api/v1/operations/effect-runtime:sample` | 持久化当前脱敏指标 snapshot 和内容摘要 |
| POST | `/api/v1/operations/effect-runtime:run-retention` | 使用幂等键执行一批受保护 retention 并提交清单摘要 |
| POST | `/api/v1/operations/outbox/{message_id}:requeue` | 使用幂等键显式重入一个 DLQ message 并提升 fence |
| GET | `/api/v1/knowledge/sources` | 查询当前内容寻址本地知识来源 |
| POST | `/api/v1/knowledge/sources:import` | 显式只读导入一个 UTF-8 Markdown/文本来源 |
| POST | `/api/v1/knowledge/search` | 来源版本与完整证明复核后的本地关键词检索 |
| GET | `/api/v1/mcp/servers` | 查询固定 MCP manifest、风险下限与 enable 状态 |
| POST | `/api/v1/mcp/servers/{server_id}:enable` | 显式启用 manifest-bound 内置 MCP Server |
| POST | `/api/v1/mcp/servers/{server_id}:disable` | 禁用内置 MCP Server |
| POST | `/api/v1/mcp/servers/{server_id}/tools:call` | 以固定 Schema 调用显式启用的短生命周期 stdio Tool |
| GET | `/api/v1/mcp/audit` | 验证并读取脱敏 MCP 控制面 hash-chain 审计 |
| GET | `/api/v1/evaluations/runs` | 读取并验证持久化黄金任务运行与 trace |
| GET | `/api/v1/evaluations/runs/{run_id}` | 读取单次 Run、指标、manifest 与完整 trace chain |
| POST | `/api/v1/evaluations/golden:run` | 运行仓库内固定只读黄金套件 |
| POST | `/api/v1/evaluations/runs/{run_id}:replay` | 复核 suite digest 后重新执行并比较语义结果 |
| GET | `/api/v1/evaluations/reports/latest` | 从已验证 Run/Trace 计算版本化趋势与 p50/p95 报告 |
| GET | `/api/v1/evaluations/reports/latest:export` | 下载带内容摘要的 `deskpilot.evaluation-report.v1` JSON |
| GET | `/api/v1/telemetry/traces?trace_id=...` | 按真实 OTel trace ID 查询本地有界脱敏 span 投影 |
| GET | `/api/v1/telemetry/traces?task_correlation_id=...` | 按 `trc_...` 聚合 Task 的本地诊断 episode |
| GET | `/api/v1/telemetry/metrics` | 查询 task/model/tool/MCP/evaluation 的低基数进程指标 |
| WS | `/api/v1/ws/tasks/{task_id}?after_seq=N` | 实时事件与断线续传 |

### 2.3 前端

- [x] Vue 3 + TypeScript + Vite 7 工程。
- [x] 深色桌面任务工作台布局和响应式样式。
- [x] 任务目标、隐私模式和创建任务表单。
- [x] 任务状态快照、结构化三步计划。
- [x] 实时事件时间线、结构化 payload 展开。
- [x] WebSocket 断线重连并携带最后事件序号。
- [x] 客户端按事件序号去重和排序。
- [x] 任务结束后显示“事件流已归档”。
- [x] Vite `/api` HTTP/WebSocket 本地代理。
- [x] session token 仅保存在前端内存，REST 自动附加 Bearer token。
- [x] WebSocket 使用版本与认证子协议，4401 后自动重新建立会话。
- [x] 前端解析 Problem Details 并展示安全的 `detail` 信息。
- [x] 侧栏新增“模型与设置”入口，任务工作台与 Provider 控制面保持前端模块分离。
- [x] 新增 Provider Catalog/Config/health/audit 严格 TypeScript 契约和认证 API client。
- [x] GET Catalog 捕获 ETag；Provider 写请求自动携带 `If-Match` 和高熵 UUID `Idempotency-Key`。
- [x] 写请求遇到浏览器网络传输失败时使用原 body、ETag 和幂等键自动重试一次，覆盖“服务端已提交但响应丢失”。
- [x] 支持 Fake/OpenAI-compatible Provider 创建和完整重新配置；隐藏 endpoint/credential reference 不从 API 回填。
- [x] 支持 Provider 启停、默认切换、删除内联二次确认和关联凭据保留提示。
- [x] 支持单 Provider 按需健康探测、能力标签和短期缓存状态展示。
- [x] 支持最近 100 条 secret-free 配置审计倒序时间线，只展示变化字段名。
- [x] `412` ETag 冲突后自动刷新 Catalog，不自动重放管理副作用，并提示用户确认后重试。
- [x] 页面明确不接收 API Key，提供 Windows Credential Manager 安全 CLI 指引。
- [x] 新增加载/空/错误/成功/冲突状态、原生配置对话框和窄屏响应式布局。
- [x] 新增 `doc/24-前端Provider模型设置页实现.md`，记录界面架构、并发/幂等、完整替换和凭据安全取舍。
- [x] 设置页新增角色路由、默认重试/费用预算、Provider EWMA、累计重试/费用和熔断状态展示；刷新运行态不会触发 Provider 网络探测。
- [x] 任务工作台新增 Pause/Resume/Cancel；控件严格遵循服务端状态矩阵，任一命令 pending 时整组禁用。
- [x] Cancel 使用内联二次确认；活动任务期间禁止创建第二任务，避免旧任务失去控制入口。
- [x] 控制命令不做乐观切换；成功后写入完整 Task 快照，`409` 或传输结果未知时先 GET 对账，Resume 不盲目重放。
- [x] Task 快照与 WebSocket 状态按 `last_event_seq` 仲裁，显式处理 `task.cancelled`，旧事件不再覆盖控制响应。
- [x] 事件流新增 connecting/connected/reconnecting/reauthenticating/forbidden/not_found/archived 状态和 1/2/4/8 秒封顶退避。
- [x] generation 隔离旧 session/socket/timer/handler；4401 重认证，4403/4404 永久停止，终态归档后不再重连。
- [x] 接入 Vitest、Vue Test Utils、jsdom，共 9 个测试文件、67 个用例，覆盖任务控制、事件恢复、Provider 安全表单、设置页交互及工具失败/取消/unknown 展示。
- [x] 新增 `doc/26-前端任务控制连接恢复与组件测试.md`，记录状态仲裁、非幂等对账、连接恢复和验收结果。
- [x] 任务工作台支持 `waiting_approval`，收到 `approval.required` 后通过受认证 API 读取完整预览，不以事件摘要代替用户核对材料。
- [x] 新增审批卡，展示风险、工具/用途、资源 operation、capability、后果、可逆性、data egress 和到期时间；批准明确限定“仅为本次”，拒绝支持原因。
- [x] 审批请求 pending 时禁用冲突控制；批准/拒绝不做乐观切换，传输结果不明时读取审批与任务快照对账，stale 时刷新预览并要求重新确认。
- [x] 前端识别 `approval.invalidated`，区分“用户曾批准”与“执行授权已失效”；批准已提交但运行时恢复失败时，只安全重试同一审批继续动作一次。
- [x] 前端测试曾扩展为 11 个测试文件、101 个用例，覆盖审批 API、卡片、状态仲裁、授权失效、运行时恢复、并发禁用、过期/冲突和网络失败对账。
- [x] 任务工作台新增 `useTaskReconciliation`；最新 `tool.unknown` 只触发一次自动定位与证据采集，任务切换通过 generation 忽略旧响应，并支持手动刷新。
- [x] 新增 Runner 证据卡，区分 committed/no-receipt/query-failed，展示无路径版本变化和稳定错误码，始终提示原调用保持 unknown 与人工裁决边界。
- [x] 证据卡在 committed `file.move` 可补偿时提供内联二次确认；网络失败的人工重试复用同一幂等键，成功后切换到新任务并复用完整审批卡。
- [x] 侧栏新增独立“历史与对账”入口；任务历史支持服务端状态筛选/分页，Reconciliation 支持 pending/resolved 筛选、详情、Runner 证据刷新与三类血缘导航。
- [x] 集中裁决、attempt 和 compensation 均要求二次确认，不做乐观更新或自动 POST 重放；相同人工重试复用幂等键，正文变化分配新 key，成功只接收服务端快照。
- [x] 活动任务期间锁定其他历史任务；切换终态历史任务时重建事件、控制、审批与单任务 Reconciliation 上下文。
- [x] 前端测试现为 15 个测试文件、126 个用例；覆盖历史 API、集中列表/筛选、幂等键语义、裁决筛选一致性、二次确认、血缘导航和任务切换锁。

### 2.4 浏览器端到端联调

早期事件主干版本已经完成基础联调。本阶段又对当前 Model Gateway + Runner + 路由韧性 + 任务控制版本执行了真实本地浏览器验收。

- [x] 在真实本地浏览器提交任务。
- [x] 收到 10 个连续事件，序号从 1 开始无缺口。
- [x] 最终任务状态为 `succeeded`，最后事件为 `task.completed`。
- [x] 三步计划正确展示。
- [x] 10 个事件时间均按 Asia/Shanghai 正确显示。
- [x] 浏览器控制台 error/warn 数量为 0。
- [x] 真实浏览器完成 session bootstrap、受认证任务创建和受认证 WebSocket 连接。
- [x] 安全联调收到 10 个连续事件，后端访问日志中没有 session token。
- [x] 运行中任务可暂停，暂停后显示 Resume/Cancel；恢复后最终 `succeeded`。
- [x] 检查点续跑过程中 `tool.requested/started/completed` 各出现一次，没有重复执行真实只读工具。
- [x] Cancel 二次确认生效；确认前无取消事件，确认后以 `task.cancelled` 归档。
- [x] API 暂停时显示重连、退避次数和立即重连；重启后从最后 `after_seq` 继续接收并显示恢复提示。
- [x] 暂停任务跨重启后可从受保护 checkpoint Resume；只有 checkpoint 缺失/无效时才展示运行上下文不可恢复提示，并仍可 Cancel。
- [x] Provider 控制面、Fake health、角色路由/预算/熔断投影和无 API Key 的引用凭据表单通过验收。
- [x] 390×844 窄屏无水平溢出；浏览器控制台 error/warn 为 0。

Policy/Approval 阶段人工浏览器验收已使用 `DESKPILOT_POLICY_REQUIRE_APPROVAL_FOR_R0=true` 完成：

- [x] 批准链严格按 `approval.resolved -> task.status_changed -> tool.started -> tool.completed -> task.completed` 到达，任务最终 `succeeded`。
- [x] 拒绝链严格按 `approval.resolved -> tool.cancelled -> task.cancelled` 到达，且没有 `tool.started`。
- [x] 审批卡展示规范绝对路径、capability、风险、后果、外发与一次性有效期。
- [x] 390×844 下审批卡无水平溢出；浏览器控制台 error/warn 数量为 0。

## 3. 验证结果

### 后端

```text
Ruff:  All checks passed
mypy:  Success, 140 source files
pytest: 414 passed, 12 skipped, 1 warning（426 collected；默认未注入外部 PostgreSQL/RabbitMQ URL）
PostgreSQL 17.10: operations alert dual-engine uniqueness + frozen audit export under concurrent append passed
RabbitMQ 3.13.7: 1 passed, 1 warning（显式 loopback test vhost，验证后已清理容器/卷）
```

已覆盖：健康检查、任务/事件主干、有界历史分页/筛选/排序、数据库迁移、Outbox、安全会话、任务控制、受保护 checkpoint codec/缓冲区清零/身份绑定、结构化写请求跨重启恢复、created/paused/waiting-approval/approved-unconsumed 续跑、密文篡改/事件游标落后 fail-closed 与零派发，Tool/Runner 闭环、Runner 自动换代/退避/熔断/generation 隔离、持久化调用状态、原子 unknown/启动恢复/不重放、人工 reconciliation 状态机、签名 receipt/no-receipt/query-failed 证据、内容寻址重复刷新、receipt 绑定复核、稳定错误脱敏、裁决不可改写、跨重启幂等回执、Tool 幂等键占用、并发单新 attempt、side-effecting call 无请求快照时拒绝 attempt、新任务 lineage、事务回滚与旧数据回填，非重放安全 timeout/cancel、Windows 每调用独立 PID、Low Integrity、受限权限、Job 成员、写保护/子进程拒绝/环境秘密隔离、句柄核验与泄漏检查、broker facts/capability 覆盖、controlled-commit fail-closed、AppContainer 专用内容寻址 bundle、protected capability RX ACL、真实 Python Tool 强制禁网、bundle 篡改拒绝与孤儿 profile 启动回收，`file.move` 无副作用 prepare、父 Runner 单次 no-overwrite commit、source 版本/destination absent 复核、durable receipt、跨 Runner 查询、崩溃后 committed/no-effect 恢复、控制面原子投影、显式结构化任务入口、可信应用计划、动态路径 Policy 窄规则、真实一次性审批与前端双路径表单，Policy 风险矩阵、可信参数资源投影、资源/capability 精确校验、完整动态授权事实、审批 preview hash/幂等/冲突/稳定过期/一次性消费、批准后失效/重启取消、授权证明错配拒绝、敏感失败脱敏和事务回滚，Model Gateway 的结构化输出、隐私/能力/角色/费用路由、重试预算、Retry-After、延迟 EWMA、熔断、流式事件、超时、失败脱敏和任务事件接线，OpenAI-compatible Chat 的 HTTP/SSE/错误/health，多 Provider catalog、credential reference、locality/TLS 策略和密钥隔离，Provider API 的认证、脱敏、TTL、single-flight、取消隔离、全局并发限制、ETag、持久化幂等、动态 registry 和安全路由投影，以及 Windows Credential Manager、DPAPI 运行配置和脱敏审计。

阶段 39 额外覆盖：版本化 Tool effect graph 的 node/edge/attempt/effect/compensation 身份、查询脱敏、transition/event 原子绑定、两个 forward 的独立 call/effect/receipt、后续失败逆序补偿、新审批/新回执/补偿血缘、Policy 拒绝同步终止绑定图、unknown 零补偿阻断，以及连续两次 API 重启精确恢复不同节点。

阶段 40 额外覆盖：lease 竞争/接管/fence 单调递增、旧 owner 写拒绝、启动恢复不跨 live lease、Runner 边界 request/terminal 统一事务回滚、unknown 等待对账、accepted unknown 显式终止、confirmed success 原图续跑且 Tool ledger 保持 unknown，以及前端恢复幂等重试与二次确认。

阶段 41 额外覆盖：数据库时间不受应用时钟污染、双 Outbox publisher 唯一 claim、过期接管与旧 ack fence 拒绝、跨 TaskService Tool 幂等键竞争归一化、跨实例 Reconciliation 唯一新 attempt、v2 DAG 环拒绝、双根并行 claim、崩溃 reclaim、旧 node fence 拒绝和 join 前驱证明。

阶段 42 额外覆盖：双根真实并发执行、1 秒 TTL 下 graph/node 持续续租、join 分轮次调度、失败后依赖 skip、图取消传播与终态 reducer、reverse-DAG 并行补偿波次、Inbox logical-message 去重/清理、Outbox DLQ/显式重新入队、PostgreSQL Outbox/node `SKIP LOCKED + RETURNING` SQL 形态，以及 Alembic `head -> 0015 -> head` 往返与 metadata 无漂移。

阶段 43 额外覆盖：受信 v2 DAG 双根批量审批/真实并发 Runner/join 后续审批、每节点 call/attempt/effect/receipt 完整账本、三次 API 进程间的审批 checkpoint 恢复、后续节点确定失败后的双根新审批/新回执并行补偿、compensation wave barrier，以及 failure/unknown 两种补偿阻断 reducer。

阶段 44 额外覆盖：条件依赖环拒绝、未决分支零 ready、受信 resolver 自动选择、decision 内容寻址与幂等重试、改写冲突、旧 ready proof 失效、未选路径及后代 skip、选中路径执行、跨 TaskService 重启恢复、proof 篡改拒绝，以及 `head -> 0017 -> head` 迁移往返与 metadata 无漂移。

阶段 45 额外覆盖：双根在途调用的 intent-before-IPC 顺序、当前 node fence 精确命中、完整 Task API 到 Runner cancel 集成链、用户 reason 传递、Runner generation 错配发送前拒绝、cancelled/unknown/receipt-proven success 三态归并，以及取消前未 commit 文件保持原位。

阶段 46 额外覆盖：等待图的 round-robin 公平分配、全局单槽双图交替、每 Tool 独立配额、waiter 取消无泄漏、backpressure 期间零 node claim、graph cancel 唤醒、ready v3 三页内容证明与跨页 claim 拒绝，以及 128 根大图的有界 checkpoint 压测。

阶段 47 额外覆盖：双 FastAPI 实例从 requester API 到远端 live owner/Runner 的完整取消链、requester 零 runtime/零 Runner IPC、intent-before-IPC、目标 graph fence 与当前 node fence 精确绑定、重复请求单次应用、stale target 重新路由、旧消息 ack fence 拒绝、claim 过期接管和 requester 退出后的无 owner lease takeover。

阶段 48 额外覆盖：双 Database/controller 共享严格全局容量、每 Tool 独立配额与 work-conserving 放行、双 dispatcher 跨 graph 交替 grant、backpressure cancel 零 claim/零 Runner、过期 permit 回收与单调 fence、旧续租/释放拒绝、node claim admission proof 事务拒绝、permit-loss 取消受守护 work，以及 live 配置不一致 fail closed/空闲切换。

阶段 49 额外覆盖：512 根宽图禁用全图 loader 后的 v4 分页/claim/下一页、直接后继计数的局部更新、branch selected/rejected 投影、缺失投影单次重建、state event 漂移修复、行 proof 篡改拒绝，以及 ready v3 既有跨页/崩溃 reclaim/dispatcher backpressure/集群 admission/补偿回归。

阶段 50 额外覆盖：operations API 未认证 401/缺可信写来源 403、snapshot 目标/payload/error 脱敏、ready rebuild 次数/耗时与漂移告警、metrics audit sequence/hash-chain 连续和详情篡改拒绝、DLQ requeue 幂等/敏感错误清除/fence 提升、retention 清理安全终态记录并保留 DLQ/TaskEvent/blocked graph，以及原 control/admission/ready/Outbox/Runner 语义回归。

阶段 51 额外覆盖：ready v5 三页 content-addressed cursor/ordinal 链、跨图 cursor 拒绝、checkpoint ID/digest/ordinal 篡改拒绝、claim 当页重算、PostgreSQL 方言 keyset 含 `ordinal >`/有界 sentinel/无 `OFFSET`，以及默认安全 skip 的专用 PostgreSQL 大图 EXPLAIN/双引擎 claim/连接池丢失接管/审计行锁门禁。

阶段 52 额外覆盖：PostgreSQL URL/二次确认/driver/测试库 token 的共享 fail-closed guard、未提交 claim 事务的 backend terminate 全回滚与零幽灵 fence、graph 行锁 `lock_timeout` 的 SQLSTATE `55P03`/零部分 claim，以及 retention 同键多主首次并发归一为同一 hash-chain 审计结果；阶段 54 已在 Docker PostgreSQL 17.10 的专用测试库实际复验通过。

阶段 53 额外覆盖：operations 五类 API 的认证/query/幂等 header、四域 snapshot/audit 读取、metrics sample 合并、retention 网络失败人工同键重试、DLQ 当前样本与响应 message ID 双重复核、retention/requeue 二次确认和提交前零 mutation，以及 App 独立运维导航和 payload/error 原文不渲染。

阶段 54 额外覆盖：PostgreSQL 17.10 空库/旧 `VARCHAR(32)` Alembic version table 迁移兼容、1000-node v5 keyset EXPLAIN/BUFFERS 实际返回 101 行、dual-engine 单获胜、engine-pool drop TTL/fence 接管、未提交 claim backend terminate 全回滚、graph lock timeout/SQLSTATE `55P03`、retention 多主同键幂等，以及 PostgreSQL 原生 `UPDATE RETURNING` identity-map 隔离。

阶段 55 当前额外覆盖：PostgreSQL JSON plan v1 指标提取、参数化 SQL shape 摘要、完整 raw plan 原子写入、PostgreSQL 版本/rows/scan rows/buffer hit-read/index 记录、结构与阈值回归报告、畸形 plan 拒绝、PostgreSQL 17.10 真实 record 后使用新 graph 默认 compare；API claimant 在 node claim commit/Runner dispatch 前的实际 OS 进程强杀、数据库 TTL takeover、零幽灵 claim/fence、零 Tool call、旧 graph/node fence 分类拒绝；Runner 在 prepared/committing/外部效果已落地边界的真实 OS 进程强杀、确定 no-effect、durable receipt 恢复、unknown/reconciliation 单次收敛和零透明重放。最终启用 Docker PostgreSQL 17.10 的 379 项后端完整回归无 skip 通过，故障辅助进程零残留；容器 restart、statement timeout/deadlock/连接中断仍待后续完成。

阶段 59 额外覆盖：四个独立 PostgreSQL engine 同时领取四个不同 admission shard，global/per-graph/per-Tool 三层容量无超发，两轮后四 graph 均获服务；数据库 TTL 后同 graph replacement 获得更大 sequence，旧 owner/fence 的 release/renew 全拒绝；grant 期间 global state revision 不变。16000-ticket plan 严格保持 shard index、101 scan rows 和无 Sort，专项 record 后 compare 并连续复跑 5 次通过。

阶段 60 额外覆盖：四个独立 PostgreSQL engine 对 12 个 owner/fence 定向 graph control 完成无重复批量 claim；claim 数据库 TTL 到期后旧 ack/renew/retry 即刻拒绝，同 owner 与新 graph owner 依次以更大 claim fence 接管。16000-control plan 严格保持 101 root rows、303 scan rows、零 rows removed、route/control PK/graph PK 索引和无 Sort/Bitmap/Seq Scan；RabbitMQ 不参与该控制正确性链。

阶段 61 额外覆盖：稳定 alert 的 opened/updated/resolved lifecycle、未变化零重复、notification/audit 双 hash-chain 绑定、通知和 cursor 篡改拒绝、普通 audit head 冻结、并发 append 下 export through 不漂移；PostgreSQL 双 engine 同故障恰好一条 opened，前端完整下载前复核 export identity/连续序号/终点 digest。通知是数据库 durable feed，RabbitMQ 不参与正确性链。

阶段 62 额外覆盖：真实 Runner 先执行目标磁盘 R0 检查；阈值 100 选择 move、未选 defer skip、建立唯一 R1 审批并提交签名 file-move receipt，阈值 0 选择 defer、未选 move skip、只读复核且源文件保持原位。branch decision 绑定 Tool terminal event/contract/result digest/阈值证据，选中节点必须跨 decision barrier 建立 ledger 后才能 admission/claim；精确 local-user disk scope 允许而模型 actor 拒绝。前端覆盖固定请求、阈值校验、分支摘要和事件标签。

阶段 63 额外覆盖：显式 UTF-8 Markdown/文本导入复用内容寻址 Artifact；检索逐次重验源文件版本、Artifact manifest 和完整 Chunk 集合，来源变化只报告 stale，数据库证明缺失或篡改返回 `KNOWLEDGE_PROOF_REJECTED`。前端展示来源、行 locator 和 retrieval proof，不调用模型、embedding 或外部服务。

阶段 64 额外覆盖：固定内置 MCP Server 默认禁用，显式启用后以真实 `2025-11-25` stdio lifecycle 调用只读文本指标；完整工具清单/Schema/structured content 受本地 manifest 约束，未知 Tool、超时、bundle 变化、审计 chain/head 篡改 fail closed，原文与 API 环境凭据不进入持久化审计或子进程环境。

阶段 65 额外覆盖：三个固定 YAML 场景运行全绿，真实 MCP/知识安全行为形成内容寻址 trace；replay 重新执行后语义 digest 一致，Suite 漂移、trace/manifest/计数投影篡改拒绝，预期不匹配稳定分类。前端只展示服务端复核后的成功率、安全率、耗时和 trace。

阶段 66 额外覆盖：二十个固定 version 2 案例完整运行和 Replay；429 恢复/预算耗尽、Runner generation 恢复、WebSocket disconnect、错误 MCP 版本/ID/JSON 均走真实内置组件边界。报告在全部 Run/Trace 复核后生成确定性趋势、nearest-rank p50/p95、失败聚合和摘要，任一 Trace 篡改使报告整体拒绝。前端提供鉴权 JSON 下载并只展示服务端指标。

阶段 67 额外覆盖：默认拒绝属性注册表和 exporter 二次过滤阻断 Prompt/Authorization/URL/异常正文金丝雀；exporter 故障与安全日志 contract、五类 span、TaskCorrelationId/真实 trace ID 查询、低基数 metrics、1 Run + 20 Case parent/child 拓扑均通过。版本化 baseline 严格 Schema、不可覆盖、CI 禁止 record，并能分别拒绝 success/safety/run p95/case p95 回归；只读 CLI compare 实际通过且不修改评测证据。

阶段 68 额外覆盖：三个固定 Agent 启动冻结、Supervisor 排除、Descriptor/Schema digest 脱敏；Contract/Prompt 严格 JSON、重复 key、秘密标记、I/O Schema、Prompt/Tool digest、Handoff cycle 和 Model capability 负向用例；Binder 精确版本/双 digest/Tool grant/预算封装、伪造字段与漂移拒绝，以及无关 Registry 扩展不使旧绑定失效。wheel 实际包含固定 Prompt manifest/正文资源。

Alembic：当前单一 head 为 `0035_context_compaction`；空库、重复升级、metadata check 与阶段 63～74 逐阶段往返均有自动化覆盖。PostgreSQL 的既有外部门禁库最后验证到 `0026`，需在下一次显式外部门禁运行时升级验证 `0027～0035`。

当前测试存在 1 条第三方弃用警告：FastAPI/Starlette TestClient 提示未来从 `httpx` 迁移到 `httpx2`。不影响运行，下一次依赖维护时处理。阶段 68 默认后端为 441 collected、429 passed、12 个显式外部门禁 skip；全量回归耗时 883.26 秒。Ruff 全仓、mypy 176 个生产源码、Alembic check、`uv lock --check`、wheel Prompt 资源复核和只读 evaluation baseline compare 全部通过。前端沿用阶段 66 的 20 文件/139 项测试、type-check 和 build 结果，本阶段未改前端。

阶段 69 新增 5 项 Task Contract/Plan Compiler 专项用例；与 Agent Registry、`0030` migration 和 Reconciliation 重启用例合跑 15 项通过。Ruff 全仓、mypy 182 个生产源码、`uv lock --check`、Workflow YAML 解析、`git diff --check` 和只读 evaluation baseline compare（`golden-resilience-v2.windows-v1`）通过。完整后端回归收集 447 项，结果为 434 passed、12 skipped、1 failed：唯一失败是既有 `test_reconciliation_receipts_survive_restart_and_never_blindly_replay` 在全套高负载下超过其 3 秒等待窗口、停在 `classifying`；该精确用例随后 8.3 秒内单独复跑通过，未发现阶段 69 确定性回归。前端未修改，沿用阶段 66 的验证结果。

阶段 70 新增 11 项 Agent Research Runtime/SSRF/fencing/redirect-DNS 用例和 1 项 `0031` migration 往返用例；与 Registry/Plan Compiler 合跑 25 项通过。Ruff 全仓、mypy 189 个源码、Alembic head/check、`uv lock --check`、Workflow YAML、`git diff --check` 和只读 evaluation baseline compare（`golden-resilience-v2.windows-v1`）全部通过。后端全量结果为 `445 passed, 12 skipped, 1 warning`，耗时 966.17 秒；全量结束后新增的 redirect/mixed-DNS 两项安全用例又随阶段 70 专项 11 项复跑通过。前端未修改，沿用阶段 66 的验证结果。

阶段 71 新增 verified-edge 纵向闭环、Citation 篡改、HTML 外部资源/script 和 `0032` migration 门禁；专项 4 项全通过，包含真实本机 Edge 无登录/断网 profile 渲染与截图证据烟测。后端全量在真实 Browser 烟测加入前为 `451 passed, 12 skipped, 1 warning`，耗时 1329.96 秒；随后阶段 71 专项再次全通过。Ruff 全仓、mypy 193 个源码、Workflow YAML 和只读 evaluation baseline compare 全部通过。前端 20 文件/139 项测试、type-check 和 build 通过。

阶段 72 新增恶意网页注入、task-scope 隔离、TTL/删除、伪造 external memory write、Manifest 篡改 fail-closed 和 `0033` migration 门禁；后端全量 `455 passed, 12 skipped, 1 warning`（467 collected）通过。Ruff 全仓、mypy 195 个生产源码、Alembic 单一 `0033` head/迁移往返、Workflow YAML、diff whitespace 和只读 evaluation baseline compare 通过；前端 20 文件/139 项测试、type-check 和 build 通过。

阶段 73 新增长期记忆确认/冲突/解决、TTL/删除、密文 at-rest、无明文 tombstone、Agent/Delivery proof 与真实 Context usage 门禁；后端全量 `458 passed, 12 skipped, 1 warning`，耗时 886.93 秒。最终加入真实 ModelRequest 内容绑定和存储 digest 重验后，研究/Context/verified-delivery 相关 17 项再次通过。Ruff 全仓、mypy 199 个生产源码、Alembic 单一 `0034` head/迁移往返、Workflow YAML、冻结 evaluation baseline compare 和 diff whitespace 通过；前端 21 文件/141 项测试、type-check 和 production build 通过。新记忆控制台的 finesse 静态检查无发现；未打开浏览器做视觉实机检查。

阶段 74 新增确定性 source-bound 压缩、coverage/conflict/stale、parent chain、预算触发和重建；后端全量 `463 passed, 12 skipped, 1 warning`，耗时 1018.34 秒。Context/Registry/Research/verified-delivery 联合 29 项、阶段 74 专项 4 项和 `0035` migration 往返通过。Ruff 全仓、mypy 202 个生产源码、Alembic 单一 `0035` head、Workflow YAML、冻结 evaluation baseline compare、`uv lock --check` 和 diff whitespace 通过；前端未修改，沿用 21 文件/141 项测试、type-check 和 production build 结果。

### 前端

```text
vitest run: 21 files, 141 passed
vue-tsc --noEmit: passed
vite build: passed
```

### 运行时

| 运行时 | 检测结果 | 处理 |
| --- | --- | --- |
| 工作区 Python | 3.12.13 | 已用于后端环境 |
| 系统 Python | 3.11.5 | 当前未使用 |
| 工作区 Node | 24.19.0 | 已用于前端构建 |
| 系统 Node | 18.20.8 | 不满足 Vite 7；后续升级到 Node 22+ |
| pnpm | 11.19.0 | 已使用 |
| uv | 0.12.3 | 安装于后端虚拟环境 |

## 4. 当前目录摘要

```text
项目根目录/
├── backend/
│   ├── src/deskpilot/
│   │   ├── api/
│   │   ├── application/
│   │   ├── core/
│   │   ├── domain/
│   │   ├── infrastructure/migrations/
│   │   ├── model_providers/
│   │   ├── runner/
│   │   └── tools/
│   ├── tests/
│   ├── alembic.ini
│   ├── pyproject.toml
│   └── uv.lock
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   └── composables/
│   ├── package.json
│   ├── pnpm-lock.yaml
│   ├── vitest.config.ts
│   └── pnpm-workspace.yaml
├── doc/
├── README.md
└── 项目进度.md
```

## 5. 尚未完成/已知边界

- [ ] v2 DAG 已具备条件边、内容寻址 branch-decision、受信 resolver、skip/ready/补偿、重启恢复和事务维护的 ready v6 membership/count keyset 投影；`disk_pressure_guarded_file_move` 已成为首个具体条件业务计划并使用模型外 Tool 证据。通用 `file_move_dag` 仍只允许 success edge，且不会开放客户端自定义条件；ready 仍缺失败直方图或外部指标导出。
- [ ] DAG admission 已由数据库在多 API 间提供严格全局/每图/每 Tool 配额、graph 公平 grant、TTL/fence、node-claim proof，以及受保护查询/retention/告警；PostgreSQL 稳态 grant 已拆为 16 个确定性 shard，并以 SERIALIZABLE SSI 守住跨 shard 容量。当前仍依赖等待者轮询，候选窗口固定 2048；极端 shard 前缀阻塞需后续 graph-head queue/keyset 投影。
- [ ] 并行补偿已消费持久化 wave、执行 barrier 并为每个节点创建新审批/回执；补偿失败/unknown 会安全阻断，但尚无补偿的再补偿、图级人工处置 API 或周期证据采集。
- [ ] 图取消已通过持久化 mailbox 路由到跨 API live graph owner，并保持 intent-before-IPC、prepare/commit/unknown、Runner generation 与 graph/node fence；受保护查询、终态 retention 与告警已完成，当前 transport 仍是数据库轮询，尚无外部 broker 主动唤醒或真实网络分区故障注入。
- [ ] Model Gateway、Fake Provider 与 OpenAI-compatible Chat HTTP adapter 已完成；尚未实现 Responses 或 Ollama 原生 Provider。LangGraph 已由 ADR-014 明确不作为核心 Runtime，因此不再作为“缺失实现”；仅保留隔离只读研究可能性。
- [ ] OpenAI-compatible adapter 已支持启动时多 Provider catalog；默认空 catalog 仍只注册离线 Fake Provider，不会自动连接云端或本地模型。
- [ ] Provider Catalog、DPAPI 密文运行配置、写 API、动态 adapter registry 和审计已完成；健康历史仍只在进程内短期缓存。
- [ ] Provider 管理的同 Idempotency-Key 首次并发写已会重读持久化响应并归一化为 replay；仍需在 PostgreSQL 多主部署上执行真实压力和故障注入。
- [ ] 管理 API 当前使用 PUT 完整替换，尚无配置详情读取或字段级 PATCH；因此前端重新配置时必须重填 endpoint/credential reference，Fake delay 也无法从公开投影预填。
- [ ] environment 与 Windows Credential Manager backend 已完成；macOS Keychain、Linux Secret Service 尚未实现，Windows 自动化测试也不会操作真实用户凭据库。
- [ ] 角色级路由、费用/重试预算、延迟 EWMA 与熔断已完成；策略由受信任启动配置加载，运行态和任务费用台账仍是单 API 进程内状态，尚未持久化或支持带版本审计的策略写 API。
- [ ] 当前 stream 在发出内容后不自动 retry/fallback，避免重复文本与 usage；跨 Provider 流式接续仍需更高层显式协议。
- [ ] Policy/Approval 应用层主干、本地知识库、固定内置 MCP 和版本化 Agent Registry 已完成；尚未实现可管理的策略写 API、审批中心、第三方 MCP 强沙箱和多格式/语义知识检索。
- [ ] 当前还不是完整多 Agent Runtime：阶段 70～74 已有持久 Handoff/Invocation/Model Turn、独立 Completion Verification、首个可验证交付 reducer、短期/长期 Memory、ContextManifest 和可证明压缩链，但尚未接入现有 Task Processor，也没有阶段 75 发布级多 Agent 对抗证明。
- [ ] 当前还不是完整通用任务 Agent：`research_to_html` 已具备 Claim/Citation Verification、Workspace/Revision/PatchReceipt、HTML Builder、隔离 Browser Verifier 和 DeliveryManifest；真实联网仍默认关闭，且统一前端工作台、用户路径导出与多类产物仍未实现。
- [ ] `unknown` Reconciliation API、持久化幂等回执、受限显式新 attempt、任务历史与集中对账/人工裁决 UI 已完成；Reconciliation 列表仍是无游标的有界全量查询，尚无后台周期证据采集。
- [ ] Windows 每调用 Job Object、受限令牌、Low Integrity、精确句柄继承和强制进程回收已完成；非 Windows 兼容路径只提供进程分离，生产默认会拒绝该降级。
- [ ] 控制面到常驻 Runner 的 bootstrap 仍使用私有 stdin 首帧交付会话密钥；每调用 worker 已使用句柄继承白名单且不接收该密钥。桌面发布版仍需代码签名、安装目录 ACL 和更强的同用户进程身份绑定。
- [ ] `filesystem.metadata.read` 已映射到父 Runner 句柄核验和不可变 broker facts；内容寻址 Python worker bundle、专用 capability RX ACL、强制 AppContainer 禁网与孤儿 profile reaper 已完成。bundle 仍由当前用户维护，桌面安装版还需代码签名、可信更新链和安装根目录 ACL 来抵御同用户恶意进程篡改。
- [ ] `file.move` prepare/commit/receipt、显式单文件任务、受信有界 DAG、动态路径 Policy 和审批入口已完成；当前刻意不从自然语言或模型输出提取写路径，也不支持目录、覆盖、跨卷或无界批量操作。
- [ ] timeout/cancel 已按 commit boundary 区分确定未提交、committing unknown 和 receipt-proven success；任务页与集中中心可保存/展示签名 receipt 查询证据，但刻意不自动写入人工 Reconciliation 裁决，也不会后台周期采集。
- [ ] `file.move` 已支持版本化多步 effect graph、逆序 Saga 补偿、重新版本校验与每次新审批；补偿失败/unknown 会终止，尚未实现补偿的再补偿。
- [ ] Reconciliation graph recovery 已有显式动作、持久化幂等回执和跨 TaskService 并发冲突归一化；尚无后台周期证据采集和多主故障注入。
- [ ] 系统 Node 需要升级；当前开发验证使用工作区 Node 24。
- [ ] Outbox 已有 delivery ID、Inbox、有界重试/DLQ、受保护且提升 fence 的幂等 requeue、retention scheduler、脱敏指标/告警/hash-chain 审计、前端运维 UI 和 RabbitMQ 真实 confirm/ack 连接丢失门禁；尚无 audit 完整游标/导出、通知通道、quorum queue、broker restart/failover 与真实网络分区注入。
- [ ] 开发阶段会话引导信任白名单 Origin/同源 Fetch Metadata；桌面发布版需由 Tauri 壳通过安全 IPC 交付 token 并启用严格 CSP。
- [ ] Provider 配置已有脱敏审计模型；尚未实现全系统安全审计、API 速率限制和并发连接上限。
- [ ] Runner 边界的 Tool ledger、graph transition 与 protected checkpoint 已统一事务；Policy/Approval 准备阶段和外部 Runner/filesystem commit 仍是独立边界，外部效果只能由签名 receipt 证明。
- [ ] PostgreSQL 运行驱动与 Outbox/node 原生 `SKIP LOCKED/RETURNING` 批量 claim 路径已实现并通过方言编译验证；共享安全 guard 下的 1000-node EXPLAIN/dual-engine claim/engine-pool drop 接管/audit 行锁、backend terminate 回滚、graph lock timeout/SQLSTATE `55P03`、retention 多主同键幂等、版本化 JSON plan、API claim-after-commit 和 Runner prepare/commit 进程强杀门禁已实际通过。仍需扩展 PostgreSQL server restart/failover、statement timeout、连接中断和多行死锁故障注入。
- [ ] pending 或 approved-but-unconsumed 审批在 checkpoint/event/task/call/policy/approval 全部绑定可证明时可跨重启续跑；缺失、损坏或错配时仍安全失效，不会猜测或派发 Tool。
- [ ] 前端已有持久化任务历史与终态任务切换，但活动执行上下文仍只保留一个快照；活动任务期间禁止创建或切换到第二任务，尚无并行多任务控制入口。

## 6. 下一步建议（按顺序）

1. 进入阶段 75：完成通用任务/多 Agent 对抗发布门禁，覆盖 verifier 错误、记忆污染、压缩漂移和 false-success。
2. 完成 Conversation/Research/Artifact 统一前端工作台和精确用户路径导出命令；导出/覆盖继续独立授权，不扩大 Workspace 权限。
3. 第三方 Agent/插件供应链保持在阶段 75 之后。

Model Gateway、Windows AppContainer Runner、receipt-bound Saga、受信 v2 DAG、运行时运维、RabbitMQ 真实重投、本地知识库、固定 MCP、二十黄金任务、脱敏 OpenTelemetry、冻结 Agent Registry、阶段 69 Plan Compiler、阶段 70 持久只读联网研究、阶段 71 verified Artifact/Browser 交付闭环、阶段 72 短期上下文数据面、阶段 73 受保护长期记忆和阶段 74 可证明压缩已完成。下一项是阶段 75 对抗发布门禁；继续禁止任意 Shell、动态 Python、上传评测脚本、用户目录任意写入，或把 Memory/Summary/MCP/OTel/网页文本直接视为授权或正确性证据。

## 7. 下次继续方式

下次开始开发前：

1. 先阅读本文件。
2. 阅读 `doc/11-分阶段开发路线.md` 的阶段 1～2。
3. 运行后端 Ruff、mypy、pytest。
4. 运行前端 test、type-check、build。
5. 阅读 `doc/74-可证明上下文压缩与重建.md`、阶段 75 路线、评测设计和 ADR-015 后，从阶段 75 对抗发布门禁续接。不得让 Memory、Summary 或 Context 绕过 verified edge。

建议给后续任务的指令：

> 继续 DeskPilot 开发，先读取根目录 `项目进度.md`、阶段 74 实现文档、阶段 75 路线、评测设计和 ADR-015，从阶段 75 通用任务与多 Agent 对抗发布门禁开始；保留阶段 71 verified-edge、阶段 72 ContextManifest、阶段 73 Memory 与阶段 74 Compaction 真值边界，完成后更新进度文件并运行全部验证。

## 8. 常用命令

后端（位于 `backend/`）：

```powershell
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
.\.venv\Scripts\ruff.exe check .
.\.venv\Scripts\mypy.exe src
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\alembic.exe current
.\.venv\Scripts\alembic.exe check
.\.venv\Scripts\python.exe -m uvicorn deskpilot.main:app --reload
```

前端要求 Node 20.19+，推荐 Node 22+（位于 `frontend/`）：

```powershell
pnpm install
pnpm test
pnpm type-check
pnpm build
pnpm dev
```
