# DeskPilot 项目进度

> 本文件是跨会话续接入口。每次完成开发任务后都应更新“已完成、验证结果、遗留问题、下一步”四部分。

## 1. 当前状态

| 项目 | 内容 |
| --- | --- |
| 最后更新 | 2026-08-10 |
| 当前阶段 | 阶段 2：安全 Tool MVP 闭环（已含首个受控写） |
| 阶段状态 | 阶段 1 已完成；阶段 2 进行中，Model Gateway、Provider 安全配置与角色级韧性路由、前端控制面、Policy/Approval、持久化工具调用账本、`unknown` 人工对账/受限显式新 attempt/Runner 回执证据、Windows 每调用进程隔离、AppContainer 专用 runtime/强制禁网，以及 `file.move` prepare/commit/receipt、显式单文件任务/审批、receipt-driven 补偿、任务历史/集中 Reconciliation 中心和受保护跨重启检查点已完成 |
| 总体完成度（粗估） | 约 45%；安全执行主干已较深，但阶段 5～7 的知识库、MCP/评测可观测和发布打磨尚未开始 |
| 后端目录 | `backend/` |
| 前端目录 | `frontend/` |
| 设计文档 | `doc/` |
| 当前处理器 | 磁盘容量任务仍从离线 Fake Provider 派发；结构化 `file_move` 与 receipt-bound compensation 使用受信任应用计划模板、精确资源版本与 R1 一次性审批；受保护 checkpoint 可证明恢复 created/pre-dispatch running/paused/waiting-approval，已派发的 running Tool 仍严禁自动重放并转 unknown/Reconciliation |

## 2. 本次已完成

### 2.1 工程与环境

- [x] 创建独立的 `backend/` 与 `frontend/` 目录。
- [x] 使用工作区 Python 3.12.13 建立 `backend/.venv`。
- [x] 后端生成 `uv.lock`，锁定 Python 依赖解析结果。
- [x] 前端生成 `pnpm-lock.yaml`。
- [x] pnpm 供应链策略只允许 Vite 必需的 `esbuild` 运行安装脚本。
- [x] 创建根目录 `.gitignore`，排除虚拟环境、运行数据库、构建产物和本地密钥。

### 2.2 后端

- [x] FastAPI 应用组合根和配置模型。
- [x] SQLite + SQLAlchemy Async 持久化。
- [x] 引入 Alembic，以 `0001_initial_schema` 管理首版数据库结构。
- [x] 应用启动自动执行 `upgrade head`，不再使用运行时 `create_all`。
- [x] migration 同时支持空库初始化和旧版已有数据库无损接管。
- [x] 新增 `OutboxMessageRecord` 与 `0002_transactional_outbox` migration。
- [x] 任务状态、任务事件和 Outbox 消息在同一事务原子提交。
- [x] `OutboxPublisher` 提交后唤醒，并保留短轮询作为漏通知兜底。
- [x] 发布失败按指数退避重试，错误信息和尝试次数持久化。
- [x] API 重启后自动恢复未发布消息，按任务事件序号投递。
- [x] 使用 at-least-once 语义；WebSocket 继续按 `task_id + seq` 去重。
- [x] `TaskRecord`、`TaskEventRecord` 与任务内单调事件序号。
- [x] 任务创建、任务查询、按 `after_seq` 补拉事件。
- [x] WebSocket 实时事件流，连接后先补历史再接收新事件。
- [x] WebSocket 同时监听 broker 事件和客户端 disconnect；非终态客户端关闭后立即清理等待器与订阅，不阻塞 API 优雅停机。
- [x] 进程内 Event Broker；数据库事件是持久化真值。
- [x] 首版 Fake Task Processor 产生分类、计划、步骤、工具和完成事件，后续已演进为 Model Gateway 驱动的 TaskProcessor。
- [x] 终态和 `task.completed` 在同一事务提交，避免状态/事件竞态。
- [x] SQLite 回读时间统一补为 UTC，消除前后事件 8 小时时差。
- [x] 404 使用稳定错误码 `TASK_NOT_FOUND`。
- [x] 每次 API 启动生成高熵进程级 session token，也支持通过 SecretStr 配置注入。
- [x] 会话引导接口只接受精确白名单 Origin 或可信同源 Fetch Metadata，并禁止缓存响应。
- [x] REST 使用 Bearer token；写请求额外要求可信浏览器来源。
- [x] WebSocket 同时校验精确 Origin 和 `Sec-WebSocket-Protocol` 中的令牌。
- [x] 凭据不进入 WebSocket URL、查询参数或 Uvicorn 访问日志。
- [x] 领域错误、参数错误和框架 HTTP 错误统一为 RFC 9457 风格 Problem Details。
- [x] 定义显式任务状态转换矩阵，终态后禁止继续追加执行事件。
- [x] 新增 `pause/resume/cancel` 命令 API 和可选 `reason` 请求体。
- [x] Pause 只允许 `running -> paused`，在持久化事件之间的安全点生效。
- [x] Resume 从可验证的下一阶段检查点继续，同一 API 进程或跨 API 重启都不重复已完成 Tool 事件。
- [x] Cancel 可终止任意非终态任务，以 `task.cancelled` 结束事件流。
- [x] 重复 pause/cancel 幂等，不增加事件序号或 Outbox 消息。
- [x] 非法转换返回 `409 TASK_TRANSITION_NOT_ALLOWED` 和机器可读状态扩展。
- [x] 密文损坏或无法与持久化真值证明一致的 checkpoint 会 fail closed；进程内也无可用运行时时 Resume 返回 `409 TASK_RUNTIME_UNAVAILABLE`。
- [x] 新增数据化 `ToolContract`，声明版本、风险、副作用、Schema、超时、幂等性、输出上限、资源锁和最小能力。
- [x] Tool Contract 不保存 callable、import path 或命令，只能由受信任应用组合代码注册。
- [x] 新增精确匹配 `name@version` 的 `ToolRegistry`，注册时校验输入/输出 Pydantic 模型与序列化 Contract 一致。
- [x] 新增 `deskpilot.runner.v1` IPC 消息：hello、heartbeat、call、cancel、progress、result。
- [x] IPC 信封使用至少 32 字节会话密钥进行 HMAC-SHA256 签名，完整绑定调用参数、Contract 摘要、时间窗和 Runner 启动 nonce。
- [x] Runner 验证器拒绝未知 key、篡改、旧启动会话、未来签发、过期、超长 TTL 和重复 nonce。
- [x] 新增线程安全的进程内防重放表；命令通过密码学与时间校验后立即消费 nonce。
- [x] 新增严格单帧 NDJSON codec：UTF-8、换行终止、1 MiB 上限、重复 JSON key 和多帧拒绝。
- [x] 新增 Runner 调用授权器：验签后执行精确版本白名单、Contract 摘要、幂等键和输入 Schema 校验。
- [x] 新增 `doc/14-Tool-Contract与Runner-IPC协议.md`，记录信任边界、消息字段、签名范围、错误码、校验顺序和下一阶段接线方案。
- [x] 新增控制面 `RunnerClient`，使用当前 Python 解释器创建隐藏的独立 Runner 子进程。
- [x] 会话密钥、`key_id` 和 `startup_nonce` 每次启动随机生成，只通过 stdin 首个 bootstrap 帧交付，不进入命令行、环境变量或日志。
- [x] Runner 完成签名 hello 握手并持续发送 heartbeat；控制面支持心跳超时、异常退出检测和干净关闭。
- [x] Runner 使用最多 4 个 worker、8 个活动调用的受限并发，并支持 Contract 超时与协作取消。
- [x] 新增静态 `ToolExecutor` handler 白名单；输出在 Runner 和控制面两侧执行 Schema 校验，Runner 额外执行 Contract 大小限制。
- [x] 实现首个真实 R0 工具 `computer.disk_usage@1.0.0`，使用标准库读取现有路径所在磁盘的 total/used/free/百分比。
- [x] 磁盘工具无副作用、禁止网络、Contract capability 为 `filesystem.metadata.read`，默认查询 `DESKPILOT_DISK_USAGE_PATH=.`。
- [x] Fake Processor 调整为 Fake Planner，将 `fake.echo` 替换为独立 Runner 真实调用；任务事件可关联 `call_id`、版本、Contract 摘要、Runner ID 和真实结果。
- [x] 健康接口新增 Runner 状态；正常为 `ready`，恢复中为 `degraded`，并投影 backoff/open/half-open 运行态。
- [x] 覆盖真实独立 PID、heartbeat、磁盘查询、结构化失败、超时、取消、启动失败、意外退出和 API 任务闭环测试。
- [x] 修复 Windows Proactor 子进程 stdout/stderr transport 回收顺序，退出时不遗留管道警告。
- [x] 新增 `doc/15-独立Runner与首个R0工具实现.md`，记录进程生命周期、安全边界、失败语义、配置和验收。
- [x] 新增 `RunnerSupervisor`，每次换代使用全新 `RunnerClient`、HMAC 会话、startup nonce、Runner ID 和冻结 generation lease。
- [x] Runner 启动/退出/心跳/协议故障按指数退避自动恢复；连续失败进入 open，恢复窗口后只创建一个 half-open 代际，稳定窗口后才清零。
- [x] 初次 Runner 启动失败不再阻止 API 启动；健康接口投影 state、generation、失败数、重启次数、下一次重试和稳定错误码。
- [x] 旧代在途调用绝不透明重放；expected runner ID 不一致时快速拒绝并安全收敛。
- [x] Alembic `0006_tool_call_persistence` 新增 `tool_calls`，持久化 call/task/step/attempt、Contract/参数摘要、幂等策略、Runner ID、状态和终态事件关联。
- [x] 原始 arguments 和 idempotency key 只计算 SHA-256，不进入调用账本、TaskEvent 或 Outbox。
- [x] `tool.requested`、`tool.started`、工具终态、任务失败和 Outbox 按调用阶段原子提交；终态重复或迟到结果为 no-op。
- [x] 启动恢复将遗留 requested 收敛为确定失败，将 running 收敛为 `unknown`；重复恢复不增加事件。
- [x] 非重放安全工具执行中 timeout/cancel 返回 `unknown`，不把可能已产生的副作用伪装成普通失败。
- [x] 新增 `doc/27-Runner故障恢复与unknown调用持久化.md`，记录状态机、事务、恢复和剩余安全边界。
- [x] 新增 Provider-neutral `ModelRequest/ModelResponse/ModelUsage/ModelStreamEvent/ProviderHealth` 领域契约。
- [x] 新增不可变 Provider descriptor 和 capability requirements，描述 structured output、strict schema、streaming、tool calling、vision、embeddings 与上下文上限。
- [x] 用 `typing.Protocol` 定义 `ModelProvider` 的 complete、stream、health 和 descriptor port，Processor 不依赖供应商 SDK。
- [x] 实现 Model Gateway Provider 注册、重复/缺失默认配置拒绝、显式 Provider hint 和响应身份校验。
- [x] 实现隐私路由：`local_only` 只走本地；`local_preferred` 未批准云回退时也禁止静默上云。
- [x] 实现能力路由、统一 timeout、连续流式序号与 completed 语义检查。
- [x] 定义认证、限流、不可用、超时、内容过滤、响应无效和流无效等稳定模型错误码。
- [x] 实现无需网络/API Key 的 `FakeModelProvider`，支持分类、计划、流式事件、usage、health、延迟和失败注入。
- [x] 新增严格 `TaskClassification/TaskPlan/PlanStep` Schema，校验步骤唯一性、依赖顺序和工具名称/版本配对。
- [x] TaskProcessor 的 `intent` 与 `planner` 阶段改由 Gateway 结构化输出驱动，不再在 Processor 内写死计划字典。
- [x] 当前只读切片额外限制模型计划必须且只能引用 `computer.disk_usage@1.0.0`；模型输出仍不是工具授权。
- [x] 持久化 `model.started/model.usage/model.failed/task.classified`，记录 Provider/model/角色/usage/延迟但不保存完整 prompt。
- [x] Provider 未知异常转换为稳定、脱敏 `MODEL_PROVIDER_UNAVAILABLE`，原始错误正文不进入任务事件。
- [x] `create_app` 支持从组合根注入 Model Provider，健康接口公开当前默认 Provider ID。
- [x] 新增 `doc/16-Model-Gateway与Fake-Provider实现.md`，记录实际接口、路由、安全边界、错误和下一步。
- [x] 将 `httpx` 提升为后端运行时依赖，实现 `OpenAICompatibleChatProvider`，业务层与 Processor 不接触 HTTP/供应商类型。
- [x] 将统一 messages、temperature、Token 上限和 strict JSON Schema 映射到 `/chat/completions` 共同子集。
- [x] 非流式响应校验字节上限、单 choice、模型身份、refusal、finish reason、usage 合计、JSON object 和应用 Pydantic Schema。
- [x] 实现有界 UTF-8 SSE 解析，可处理任意网络拆包，要求稳定 id/model、最终 usage、finish reason 与 `[DONE]`。
- [x] 将 401/403、普通 429、额度类 429、408/504、5xx、其他 4xx、连接失败和 timeout 映射为稳定脱敏错误。
- [x] 新增 `MODEL_QUOTA_EXCEEDED` 与 `MODEL_REQUEST_REJECTED`，避免把不可恢复额度问题错误标记为可重试限流。
- [x] Provider 使用 `SecretStr` 保存 API Key；默认禁止重定向和环境代理继承，base URL 拒绝内嵌凭据、query 与 fragment。
- [x] 请求不外发 DeskPilot task/request ID、隐私状态、内部 metadata、Tool Contract 或 Runner/session 数据。
- [x] 实现脱敏 `GET /models` Provider health 探测；后续已通过单 Provider 按需公共 API 接入，仍不会在总健康接口中自动探测全部 Provider。
- [x] 新增 17 个完全基于 `httpx.MockTransport` 的 adapter/任务闭环测试，不进行真实 DNS、网络或付费调用。
- [x] 新增 `doc/17-OpenAI-Compatible-Chat-Provider实现.md`，记录 HTTP/SSE 映射、安全边界、错误语义和后续配置方案。
- [x] 新增 discriminated `ProviderConfig` catalog，支持 Fake 与 OpenAI-compatible Chat 配置，最多 32 个且 Provider ID 唯一。
- [x] 支持同一 Gateway 同时注册默认 Fake、本地 Ollama/OpenAI-compatible endpoint 和可选云端 Chat Provider。
- [x] 空 catalog 保持 legacy Fake 配置兼容，不会因升级自动联网或要求 API Key。
- [x] 新增 `CredentialReference` 与 `CredentialResolver` port，配置只能引用 `DESKPILOT_CREDENTIAL_*`，不能保存明文 `api_key`。
- [x] 实现 environment credential backend，返回 `SecretStr`；缺失/空白密钥以脱敏 `CREDENTIAL_NOT_FOUND` fail fast。
- [x] Provider factory 只允许静态 adapter 类型，不支持 import path、动态 Python 或模型生成的 Provider 类。
- [x] 云端 endpoint 强制 HTTPS 和 credential reference；本地 endpoint 默认只允许 loopback。
- [x] 公网地址不能声明为 local；私网/link-local IP 必须显式 `allow_private_network=true`，DNS hostname 不参与本地推断。
- [x] disabled Provider 不解析 credential；默认 Provider 缺失或禁用时在数据库/Runner 启动前失败。
- [x] 新增 16 个 Provider 配置、凭据隔离、endpoint 策略和多 Provider 组合测试，全程不访问模型网络。
- [x] 新增 `doc/18-Provider配置与凭据引用实现.md`，记录 catalog、凭据边界、环境配置和下一步。
- [x] 新增受认证 `GET /api/v1/model-providers`，显示所有 enabled/disabled Provider 的无密钥 descriptor、默认状态和有效缓存，列表本身不触发网络探测。
- [x] 新增 `GET /api/v1/model-providers/{provider_id}/health`，只在用户明确请求时探测单个 enabled Provider。
- [x] 公共健康快照在领域类型层移除上游 `detail`，catalog/API 均不返回 endpoint、credential reference、API Key 或异常正文。
- [x] Provider 健康探测加入默认 15 秒 TTL；成功、降级和不可用结果都会短期缓存，避免故障时形成重试风暴。
- [x] 同 Provider 并发请求通过 single-flight 共享一个内部 Task；单个调用者取消不会取消其他等待者的探测。
- [x] 不同 Provider 探测使用默认全局 4 并发 Semaphore，并有统一 5 秒 timeout；应用关闭时取消遗留探测。
- [x] disabled Provider 无需凭据即可出现在 catalog，health 返回 `409 MODEL_PROVIDER_DISABLED` 且不访问网络；未知 ID 返回 `404 MODEL_PROVIDER_NOT_FOUND`。
- [x] 新增 11 项 Provider API、认证、脱敏、零探测列表、TTL、single-flight、取消隔离、并发限制和 timeout 测试。
- [x] 新增 `doc/19-Provider只读API与健康探测缓存实现.md`，记录接口契约、缓存语义、安全边界、配置与下一步。
- [x] 新增 `0003_provider_catalog` migration，创建 `model_provider_catalog_state` 与 `model_provider_catalog_entries` 两张表。
- [x] 新增严格 `ProviderCatalogDefinition/PersistedProviderCatalog`，持久化入口在类型层只接受公开 descriptor、enabled/default，不接受原始 Settings 或 ProviderConfig。
- [x] 新增 `ProviderCatalogStore` port 与 SQLAlchemy repository，API 的 catalog 和 health 前置检查改为每次读取持久化公开投影。
- [x] 应用启动在 Runner 之前幂等导入公开 catalog；相同内容保持版本与时间，公开内容变化时事务内替换条目并将版本单调递增。
- [x] 导入前按 Provider ID 稳定排序并计算 canonical JSON SHA-256，输入数组顺序变化不会产生虚假版本。
- [x] repository 支持 `expected_version` 条件更新和 `MODEL_PROVIDER_CATALOG_VERSION_CONFLICT`，为未来 ETag/`If-Match` 写接口保留乐观并发基础。
- [x] Provider API 新增 `catalog_version` 和 `imported_at`，前端可判断 catalog 是否在重启或配置导入后发生变化。
- [x] SQLite catalog 不保存 `base_url`、credential identifier、API Key 或 health detail；自动化测试直接扫描数据库文件验证敏感字节不存在。
- [x] 新增 5 项 catalog 持久化、幂等导入、版本冲突、重启续读和密钥隔离测试，并更新迁移/API 断言。
- [x] 新增 `doc/20-Provider-Catalog持久化与启动导入实现.md`，记录公开投影决策、数据模型、版本语义、安全边界和下一步。
- [x] `CredentialReference` 新增 `windows_credential_manager` backend；environment 与 Windows identifier 分别使用独立严格命名空间。
- [x] 新增 `CompositeCredentialResolver`，只按显式 backend 分发，禁止 Windows/environment 之间静默 fallback。
- [x] 新增 ctypes `Win32CredentialApi`，使用 Unicode `CredReadW/CredWriteW/CredDeleteW/CredFree`，不增加第三方依赖。
- [x] Windows 凭据固定使用 `CRED_TYPE_GENERIC`、`CRED_PERSIST_LOCAL_MACHINE` 和 `DeskPilot/ModelProvider/` target namespace。
- [x] 实现 Windows Credential Manager resolve/store/delete；删除不存在条目返回 false，保持幂等。
- [x] Generic credential 按 UTF-8 编码并限制 2560 字节；读取始终 `CredFree`，Python 侧读写 `bytearray` 在 finally 中零覆盖。
- [x] 新增 `CREDENTIAL_BACKEND_UNAVAILABLE/CREDENTIAL_INVALID/CREDENTIAL_BACKEND_OPERATION_FAILED`，错误仅保留稳定 operation/backend/OS code，不泄露 target 或密钥。
- [x] Windows 默认组合同时注册 environment 与 Credential Manager；Provider Factory 和业务层继续只依赖原有 `CredentialResolver` port。
- [x] 新增安全本地 CLI：隐藏输入两次保存、只显示存在状态、删除要求 `--yes`，不支持命令行 secret 参数。
- [x] 新增 14 项 backend 命名、CRUD、字节边界、缓冲区清零、错误脱敏、无 fallback、Factory 接线和 CLI 测试；未操作真实用户凭据库。
- [x] 新增 `doc/21-Windows-Credential-Manager实现.md`，记录官方 API 依据、Win32 映射、CLI、安全边界和下一步。
- [x] 新增严格 `ProviderRuntimeConfigBundle`，统一封装完整 ProviderConfig、payload schema 和固定 `retain` 凭据删除策略；内外 Provider ID 必须一致。
- [x] 新增 `RuntimeConfigProtector` port 与 `ProviderRuntimeConfigCodec`，使用 canonical JSON、256 KiB 明文上限和编解码工作 `bytearray` 清零。
- [x] 新增 ctypes `WindowsDpapiProtector`，使用 current-user `CryptProtectData/CryptUnprotectData`、`CRYPTPROTECT_UI_FORBIDDEN`、Provider 绑定 entropy 和 `LocalFree`。
- [x] DPAPI 不使用 `CRYPTPROTECT_LOCAL_MACHINE`，运行配置通常只能由同一 Windows 用户在同机解密；错误只保留 operation 和 OS code。
- [x] 新增 `0004_provider_runtime_config` migration，创建 `model_provider_runtime_configs` 密文表与 `model_provider_config_audit_events` 审计表。
- [x] 新增 `SqlAlchemyProviderRuntimeConfigRepository`，支持 create/update/get/delete、revision 条件更新、相同内容幂等和审计 sequence 分页。
- [x] 密文变化与审计追加在同一 SQLite 事务；审计只保存字段名和枚举结果，不保存 endpoint、credential identifier、密钥或修改前后 JSON。
- [x] 明确删除联动边界：删除 Provider 配置不会自动删除 environment/Windows 凭据，审计记录 `provider_deleted_credential_retained`；后续凭据删除必须独立确认并检查引用。
- [x] 新增 8 项运行配置/DPAPI/仓储/审计测试；Windows 真实 DPAPI 测试只做内存往返，不写 Credential Manager 或外部文件。
- [x] 新增 `doc/22-Provider运行配置保护与审计模型实现.md`，记录分层、DPAPI 依据、数据模型、并发语义、删除策略和下一步接线方案。
- [x] 新增统一 `ProviderManagementStore` port、`ProviderManagementService` 和 SQLAlchemy 原子仓储，统一协调公开 Catalog、密文运行配置、审计和幂等回执。
- [x] Provider 启动配置改为首次 seed；存在密文配置后，数据库成为 adapter 启动真值，重启不再被 `.env`/Settings 静默覆盖。
- [x] 新增 `0005_provider_management` migration 与 `model_provider_idempotency_records`，只保存 key digest、keyed request fingerprint、公开回执和 24 小时过期时间。
- [x] GET Catalog 新增配置 ETag 和 `Cache-Control: no-store`；写请求强制严格 `If-Match` 与 16～128 位 `Idempotency-Key`。
- [x] 新增 Provider 创建、PUT 完整更新、启用、禁用、默认切换、删除和审计分页 API。
- [x] stale ETag 返回 `412 MODEL_PROVIDER_CATALOG_VERSION_CONFLICT`；幂等成功重试优先于 ETag 检查，返回原回执并标记 `replayed=true`。
- [x] 同 key 不同请求返回 `409 IDEMPOTENCY_KEY_REUSED`；raw key、endpoint、credential identifier 和 API Key 不进入数据库回执。
- [x] 写入前构造完整候选 adapter registry；credential 缺失/配置无效时不提交 Catalog、密文、审计或幂等记录。
- [x] 新增 `ModelGateway.reconfigure` 和 health cache/probe invalidation；提交后新请求立即使用新 Provider/default，进行中的旧调用可继续完成。
- [x] endpoint 等隐藏字段变化也递增聚合 Catalog version；删除默认/最后 Provider、禁用默认 Provider 均被拒绝。
- [x] 新增 8 项写 API、ETag、幂等、动态 Gateway、凭据失败回滚、重启真值、凭据保留和 SQLite 明文扫描测试。
- [x] 新增 `doc/23-Provider管理服务与写API实现.md`，记录事务顺序、API 契约、并发/幂等语义、启动真值和安全边界。
- [x] CORS 显式暴露 `ETag` 响应头，前端使用独立 API Origin 时也能读取 Catalog 并发条件。
- [x] 新增 `ModelGatewayPolicy`、角色级有序 Provider allowlist 和 `priority/latency_aware` 两种策略；隐私、能力与费用硬约束始终先于延迟排序。
- [x] 新增请求级 `ModelExecutionBudget`；应用默认最多 2 次尝试、累计等待 2 秒，整个 fallback 链共享单一 request timeout。
- [x] OpenAI-compatible adapter 解析 429/5xx `Retry-After` delta-seconds/HTTP-date 并限制为 3600 秒；有备用时立即切换，无备用时只在预算内等待。
- [x] 新增整数微美元 Provider 价格、调用前保守费用预留、任务累计结算和可负担 fallback；有限预算下缺少价格时安全拒绝。
- [x] 新增 Provider 延迟 EWMA；延迟感知策略先采样未知候选，再按最低 EWMA 调度。
- [x] 新增 closed/open/half-open 熔断，连续 timeout/不可达/非法响应达到阈值后打开，恢复窗口后仅放行一个半开探针；429 使用独立冷却窗口。
- [x] 新增 `GET /api/v1/model-providers/routing`，只返回角色策略、预算、EWMA、费用、重试和熔断状态，不返回 endpoint、凭据、请求内容或任务 ID。
- [x] 新增 12 项路由/配置/adapter/API 测试，使用虚拟时钟、Fake Provider 和 MockTransport 覆盖角色选择、费用预算、Retry-After、EWMA、熔断、半开单探针与脱敏投影。
- [x] 新增 `doc/25-角色级Provider路由与韧性预算实现.md`，记录排序顺序、计价、重试、状态机、配置和已知边界。
- [x] 新增确定性的 `BuiltinPolicyEngine` 和结构化 `ToolAuthorizationRequest/PolicyDecision`；策略不调用模型，只消费受信任 Contract、规范化参数摘要、capability 和资源事实。
- [x] 建立默认风险矩阵：R0 自动允许；R1/R2 要求一次性审批；R3 默认拒绝，显式启用后仍要求审批；R4 项目级拒绝。
- [x] 新增有效风险上调、builtin 来源限制、capability allowlist、资源 operation/capability 完全绑定和精确资源范围校验，任一事实不匹配均 fail closed。
- [x] 新增 `waiting_approval` 状态，以及 `policy.evaluated`、`approval.required`、`approval.resolved`、`approval.expired`、`approval.invalidated` 事件。
- [x] 新增 `0007_policy_approvals` migration 和持久化 `approvals`；工具调用账本同步保存策略 decision/revision/effect、资源范围摘要、策略事件和 authorization ID。
- [x] 审批 `preview_hash` 精确绑定 task/step/call、工具版本、Contract/参数摘要、策略 decision/revision、资源/capability、后果、数据外发和有效期；决策只支持 `scope=once`。
- [x] 相同审批决定可幂等重放，冲突决定、旧预览和过期审批返回稳定 409；批准、拒绝、过期、任务取消和调用状态都使用 CAS/任务锁收敛。
- [x] 策略/审批、任务与工具调用状态、TaskEvent 和 Outbox 在同一数据库事务原子提交；提交失败不会留下孤立授权或事件。
- [x] 一次性审批仅在 `requested -> running`、Runner IPC 下发前原子写入 `consumed_at`；已消费、过期或绑定不一致的授权不能再次执行。
- [x] Runner IPC 新增 `ToolAuthorizationGrant`，完整绑定 task/step/call、actor/origin、工具/Contract、策略依据、参数/资源、capability、网络/外发、副作用、交互/批量和一次性审批；Runner 在 handler 前拒绝缺失、错配、降级或过期证明。
- [x] Tool Registry 要求可信资源 projector；控制面和 Runner 分别从同一份已验证参数重算规范资源，参数与授权资源不一致时在 handler 前 fail closed。
- [x] 用户 `decision` 与当前授权 `status` 分离；批准后未消费的授权在取消、dispatch 前过期或启动恢复时显式失效，同时保留原批准人、时间和原因审计。
- [x] API 启动时只为通过持久化证明的 pending/approved-but-unconsumed 审批保留续跑资格；缺失或无效 checkpoint 的遗留状态仍安全取消，不猜测也不重放已派发 Tool。
- [x] 非交互、批量和数据外发调用在相应预览/预算/目的地绑定完成前 fail closed；审批 API 成功响应统一使用 `Cache-Control: no-store`。
- [x] 新增 `doc/28-Policy-Approval执行前授权主干.md`，记录策略矩阵、精确预览、一次性消费、Runner 证明、重启语义和 OS 隔离边界。
- [x] 将常驻 Runner 收敛为签名 IPC/授权/调度 broker；工具 handler 改为每次调用在全新一次性 worker 进程执行，不跨调用复用内存、句柄或模块状态。
- [x] 新增 `deskpilot.worker.v1` 私有单帧协议；worker 只接收 call ID、精确工具版本、Contract 摘要和已验证参数，不接收 Runner HMAC 密钥或完整授权证明。
- [x] worker 从受信任固定 factory 重建静态 executor，并再次校验工具版本、Contract 摘要、输入/输出 Schema 与输出上限；生产 factory 不能由模型、用户或 IPC 指定。
- [x] 新增 ctypes Windows 隔离启动器：`CreateRestrictedToken(DISABLE_MAX_PRIVILEGE | LUA_TOKEN)` 删除最大权限，`SetTokenInformation(TokenIntegrityLevel)` 降为 Low Integrity（RID 4096）。
- [x] 每个调用创建独立 Job Object，启用 kill-on-close、未处理异常终止、进程内存上限和活动进程上限；默认 256 MiB、`ActiveProcessLimit=1`。
- [x] worker 以 `CreateProcessAsUserW + CREATE_SUSPENDED` 创建，先 `AssignProcessToJobObject` 再 `ResumeThread`，消除入 Job 前执行/派生的竞态。
- [x] 使用 `STARTUPINFOEXW + PROC_THREAD_ATTRIBUTE_HANDLE_LIST`，worker 只继承私有 stdin/stdout/stderr；父/子管道副本、令牌、Job、进程和线程句柄在所有成功/失败路径关闭。
- [x] worker 环境从静态白名单重建，排除 session token、Provider credential 和任意应用秘密；为保持 `ActiveProcessLimit=1`，直接启动可信基础解释器而非会再派生进程的 venv 重定向器。
- [x] timeout/cancel 现在可通过 `TerminateJobObject` 强制终止调用进程；非重放安全调用仍按既有规则保守收敛为 `unknown`，不会因强杀而伪造未产生副作用。
- [x] bootstrap 新增强制 Windows 沙箱、worker 内存/进程数限制；签名 hello 回报实际 isolation mode 与每调用隔离，要求不满足时 Runner 代际启动失败而非静默降级。
- [x] Windows 真实集成测试验证不同调用 PID、Low Integrity、权限削减、Job 成员、Medium Integrity 写入拒绝、子进程创建拒绝和父进程秘密隔离。
- [x] 新增 `doc/29-Windows-Runner进程隔离与低完整性实现.md`，记录执行链、Win32 映射、配置、失败语义、已强制边界和剩余资源隔离边界。
- [x] 新增 `0008_tool_reconciliation` migration；持久化 `tool_reconciliations`、Reconciliation API 幂等回执和 `key_required` Tool 幂等键占用回执。
- [x] `finish_tool_call(UNKNOWN)` 与启动恢复在 Tool 账本、TaskEvent、任务终态和 Outbox 的原事务中创建 pending reconciliation；回滚不留孤立记录。
- [x] 人工裁决支持 `confirmed_succeeded / confirmed_failed / confirmed_no_effect / accepted_unknown`，必须携带证据摘要，resolved 后不可改写。
- [x] 原 `tool_calls.status=unknown` 永不改写；人工裁决不伪装成 Runner 结果，也不向原 failed task 追加事件。
- [x] 只有 `confirmed_no_effect` 允许创建一个显式新 attempt；其他 outcome 全部 fail closed，区分“失败”与“可证未产生效果”。
- [x] 新 attempt 使用全新 Task/call ID，仅复制 goal/conversation/privacy/constraints，重新规划、Policy/Approval 和 Runner 授权；不复用旧参数、幂等键、预览、审批或 lease。
- [x] `task.created` 写入 `retry_of` lineage，精确关联 reconciliation、原 task/call 和 source attempt；Reconciliation 最多绑定一个直接后继任务。
- [x] Reconciliation 写 API 强制 16～128 位 `Idempotency-Key`；只保存键摘要与规范请求 fingerprint，支持跨 API 重启语义重放与同键异请求 409。
- [x] `key_required` Tool 在 requested 事务内持久化 tool/version/key digest 占用回执；跨任务重用在 Runner 前被拒绝，原始 key/参数不落库。
- [x] 旧 schema 中已有 `unknown` 自动回填 pending reconciliation，旧 `key_required` 调用回填最早占用者；空库、重复升级和保留数据均通过。
- [x] 新增受认证 Reconciliation 列表/详情/裁决/新 attempt API；写接口强制可信 Origin，成功响应统一 `Cache-Control: no-store`。
- [x] 并发同键 create-attempt 只创建一个任务；数据库已提交而响应丢失时，同键重试恢复原 task，仅在它仍为 `created` 且本进程无 runtime 时启动。
- [x] 新增 `doc/30-unknown人工对账与显式新attempt实现.md`，记录状态机、幂等语义、事务边界、API、迁移和不重放不变量。
- [x] 新增父 Runner `ToolResourceBroker`；`filesystem.metadata.read` 先以 `CreateFileW(FILE_READ_ATTRIBUTES)` 打开授权路径，再用最终句柄路径核对签名资源 scope。
- [x] broker 通过 `GetVolumePathNameW/GetDiskFreeSpaceExW` 生成不可变容量 facts；一次性 worker 不再根据原始 `path` 打开文件系统。
- [x] `deskpilot.worker.v1` 新增严格 `BrokeredFilesystemMetadata`；worker 要求 resource operations 并集与 allowlisted Contract capabilities 完全相等，缺失/额外能力在 handler 前拒绝。
- [x] `ToolExecutionContract` 新增 `read_only/brokered` commit protocol；声明副作用的 Contract 必须使用 brokered commit，无 provider 时以 `TOOL_CONTROLLED_COMMIT_UNAVAILABLE` 在 handler 前 fail closed。
- [x] Windows 启动器新增每调用 AppContainer 模式，无任何网络 capability，同时保留 Job Object、挂起启动、精确 stdin/stdout/stderr 句柄白名单和环境重建。
- [x] Runner bootstrap/hello 独立声明 `require_network_isolation` 与 `network_isolation_mode`；强制模式预启动真实 worker，解释器或模块在 AppContainer 中不可访问时整代 Runner 启动失败，不静默退回 Low Integrity。
- [x] Windows loopback 真实测试证明无 capability AppContainer 内 `curl.exe` 无法连接父进程监听端口；连续 32 次资源 broker 核验无句柄泄漏，测试后无残留 AppContainer profile。
- [x] 新增 `doc/31-Contract能力Broker受控提交与Windows禁网边界.md`，记录资源句柄/facts、commit 门槛、AppContainer 姿态、平台探针与发布态剩余边界。
- [x] 新增内容寻址 AppContainer worker bundle，只收集 CPython 标准运行时、Pydantic 闭包与 DeskPilot 包；staging 完成 manifest、ACL 和哈希复核后原子发布。
- [x] bundle manifest 精确绑定目标路径、大小、SHA-256、Python 版本和稳定 capability SID；存在额外文件、reparse point、路径穿越或任意内容篡改时 fail closed，不静默覆盖。
- [x] `DeskPilot.workerRuntime.v1` capability 通过 protected DACL 只获得 bundle read/execute；当前用户、SYSTEM 与 Administrators 保留安装维护权限，不再修改开发解释器/venv/源码 ACL。
- [x] AppContainer worker 环境移除开发 `PYTHONPATH/VIRTUAL_ENV` 与用户 PATH；只绑定专用 `PYTHONHOME`、最小系统 PATH、私有三管道和 profile-local TEMP。
- [x] 控制面在 Runner 进程创建前异步准备 bundle，Runner 独立复核已发布内容并预启动真实 worker；Windows 发布 `.env.example` 已切换为强制 AppContainer 禁网。
- [x] 新增 durable AppContainer profile journal；每次调用先登记 exact moniker，删除成功后注销，下一 Runner 启动会在接受调用前回收异常退出遗留 profile。
- [x] 真实 `computer.disk_usage` 已在 `windows_appcontainer/appcontainer` 下端到端成功；自定义 runtime capability 写 bundle 失败且不能连接父进程 loopback。
- [x] 新增 `doc/32-AppContainer专用Worker运行时与Profile回收.md`，记录运行时闭包、内容寻址发布、capability DACL、profile reaper、配置与验收。
- [x] 实现首个真实可逆写 Tool `file.move@1.0.0`；Contract 为 R1 / `key_required` / `brokered`，限定单普通文件、同卷移动且绝不覆盖目标。
- [x] `BrokeredFileMove` 精确绑定 source/destination 角色 capability、source 内容+文件身份版本和 destination absent；AppContainer worker 只返回无副作用 `FileMovePrepare`。
- [x] 父 Runner 复核 prepare、authorization ID、一次性 approval preview hash、expected resource versions 和幂等键摘要；Windows commit provider 使用不带 replace flag 的 `MoveFileExW`。
- [x] 新增 Runner 自有 SQLite commit journal，持久化 `prepared/committing/committed/no_effect/unknown`、prepare binding 与 before/after 外部版本；原始幂等键不落库。
- [x] Runner 启动回收可从 source/destination 外部状态证明 committed 或 no-effect；签名 `tool.commit_receipt.get/result` IPC 可跨 Runner 换代查询 receipt 且不重放调用。
- [x] timeout/cancel 改为按共享 commit boundary 裁决：提交前可确定失败/取消，committing 无证据时 `unknown`，已有 durable committed receipt 时返回成功。
- [x] 新增 Alembic `0009_tool_commit_receipts`，将成功 receipt 的无路径版本证据与 Tool 终态/事件原子投影到控制面。
- [x] 新增 `doc/33-file.move受控提交与持久化回执.md`，记录信任边界、资源版本、journal、崩溃恢复、timeout/cancel 与已知限制。
- [x] `TaskCreate.tool_request` 新增 strict `file_move` 结构化入口；API 在建任务前只读规范化 source/destination，拒绝缺失源、目标已存在、跨卷、同路径和非普通文件。
- [x] 文件移动不解析自然语言或模型输出；TaskProcessor 使用可信三步计划模板、`local_user` actor、单次高熵幂等键和 source/destination 精确版本预览。
- [x] Policy 新增窄范围动态路径规则，仅允许精确 `local_user + file.move@1.0.0 + 双角色 capability + R1/local/single-call` 事实，并始终要求一次性审批；模型身份或事实扩张 fail closed。
- [x] 现有审批卡已真实展示规范源/目标路径、source 版本、R1、reversible、capability、no-overwrite 与补偿后果；拒绝/过期/取消时不派发 Runner。
- [x] 前端任务表单新增“移动单个文件”模式、双路径字段、完整性禁用和安全边界提示；批准后真实 API/Runner 端到端只移动一次并返回 committed receipt。
- [x] 新增 `doc/34-file.move显式任务入口与一次性审批.md`，记录请求契约、可信参数来源、Policy 规则、审批/前端流程和恢复边界。
- [x] 新增 Alembic `0010_reconciliation_receipt_evidence` 与追加式 `tool_reconciliation_evidence`；按 reconciliation + 内容摘要去重保存 commit-receipt/no-receipt/query-failed 观察。
- [x] 新增 `POST /reconciliations/{id}:refresh-evidence`；通过冻结 Runner lease 执行签名 journal 查询，不派发 Tool、不重放调用，响应与列表/详情均返回 `Cache-Control: no-store`。
- [x] 发现 receipt 时再次核对 call/tool/version/authorization/idempotency digest，并写入无路径 `tool_commit_receipts` 投影；绑定无效只记录稳定失败码。
- [x] no receipt/query failed 均明确为非结论性证据，不开启新 attempt；committed receipt 也只支持人工判断，原 `tool_calls.status=unknown` 和 pending outcome 保持不变。
- [x] 新增 `doc/35-unknown-Runner回执证据采集与前端展示.md`，记录证据模型、内容寻址幂等、API、安全验证、前端三态解释和剩余边界。
- [x] 新增内部 `FileMoveCompensationRequest` 与无 body `POST /reconciliations/{id}:create-compensation`；反向路径只从原审批和已验证 committed receipt 派生，客户端不能声明路径或版本。
- [x] 创建前复核反向 source 的 receipt 版本和反向 destination absent；阶段 4 在 Tool call/审批前再次投影，关闭证据到审批的竞态窗口。
- [x] 新增 Alembic `0011_file_move_compensation`，持久化 compensation task/receipt/创建时间；每条 Reconciliation 只允许一个直接补偿，且与显式新 attempt 互斥。
- [x] 补偿任务生成新 task/call/高熵 Tool 幂等键/Policy decision/一次性 approval，审批后仍经过 prepare/commit/receipt；原 unknown Tool 账本保持不变。
- [x] 新增 `doc/36-file.move回执驱动显式补偿闭环.md`，记录服务端派生、版本竞态、单补偿血缘、新授权/提交、前端交互与验收。
- [x] 新增 `GET /api/v1/tasks?status=&limit=&offset=`；提供 1～100 条有界分页、状态筛选、稳定倒序、总数与 `Cache-Control: no-store`，不聚合事件 payload 或 Tool 参数。
- [x] 收紧显式 attempt：仅精确 `computer.disk_usage@1.0.0` 可由当前可信处理器确定性重建；没有持久化原始请求的 `file.move` 即使裁决为 no-effect 也不会从 goal 猜测路径。
- [x] 新增 `doc/37-任务历史与集中Reconciliation中心.md`，记录历史 API、集中工作台、幂等交互、血缘导航、重试边界与验收。
- [x] 新增 strict/frozen `TaskCheckpointPayload`，持久化结构化 `file_move`/补偿请求、受信分类与计划、规范化参数/资源、Policy 事实、审批绑定与原始 Tool 幂等键。
- [x] 新增 `TaskCheckpointCodec`；使用 canonical JSON、512 KiB 上限、临时缓冲区清零和 task-specific current-user DPAPI context，原始路径/幂等键不以明文进入 SQLite。
- [x] 新增 Alembic `0012_task_runtime_checkpoints`；每任务最多一个受保护 checkpoint，明文仅投影 schema/stage/event-seq/revision/protection/digest 和时间。
- [x] 初始 checkpoint 与 task/event/outbox 原子创建；之后每个已完成阶段保存确切 `next_stage`，pause/resume/approval resolution 在事务内重绑最新事件游标。
- [x] API 启动可重建 created/classifying/pre-dispatch running/paused/waiting-approval 和 approved-but-unconsumed 运行时；非暂停状态在 Runner 启动后自动续跑。
- [x] 恢复强制核对 task event sequence/status、确定性 call ID、Tool call status、Policy request digest 与 approval task/call/consumption 绑定；任一错配均 fail closed。
- [x] 启动 Tool call 恢复仅跳过可证明尚未派发的 `requested` call；遗留 `running` call 仍转 `unknown` 并创建 Reconciliation，不会透明重放。
- [x] 终态任务自动删除 runtime checkpoint；密文损坏或真值绑定落后时写入稳定脱敏失败码，不泄漏 DPAPI 细节或载荷。
- [x] 新增 `doc/38-结构化Tool请求与可证明跨重启检查点.md`，记录受信 9 阶段图、保护模型、启动顺序、证明规则、不重放边界与验收。

当前 API：

| Method | Path | 作用 |
| --- | --- | --- |
| GET | `/api/v1/health` | 健康与版本 |
| GET | `/api/v1/session` | 受信任前端建立进程级本地会话 |
| GET | `/api/v1/model-providers` | 查询版本化、无密钥 Provider catalog 与有效健康缓存，不触发探测 |
| GET | `/api/v1/model-providers/audit` | 按 sequence 查询脱敏 Provider 配置审计 |
| GET | `/api/v1/model-providers/routing` | 查询角色路由、预算、EWMA、费用、重试和熔断安全投影 |
| POST | `/api/v1/model-providers` | 使用 ETag 与幂等键创建 Provider |
| PUT | `/api/v1/model-providers/{provider_id}` | 完整替换 ProviderConfig |
| POST | `/api/v1/model-providers/{provider_id}:enable` | 验证凭据并启用 Provider |
| POST | `/api/v1/model-providers/{provider_id}:disable` | 禁用非默认 Provider |
| POST | `/api/v1/model-providers/{provider_id}:make-default` | 切换 enabled 默认 Provider |
| DELETE | `/api/v1/model-providers/{provider_id}` | 删除非默认 Provider 并保留凭据 |
| GET | `/api/v1/model-providers/{provider_id}/health` | 按需执行单 Provider 脱敏健康探测 |
| POST | `/api/v1/tasks` | 创建任务并启动 Model Gateway + Runner TaskProcessor |
| GET | `/api/v1/tasks?status=&limit=&offset=` | 按状态查询有界、稳定倒序的持久化任务历史 |
| GET | `/api/v1/tasks/{task_id}` | 查询任务快照 |
| GET | `/api/v1/tasks/{task_id}/events?after_seq=N` | 补拉事件 |
| POST | `/api/v1/tasks/{task_id}:pause` | 在安全点暂停运行中任务 |
| POST | `/api/v1/tasks/{task_id}:resume` | 从当前进程或已证明的跨重启检查点恢复 |
| POST | `/api/v1/tasks/{task_id}:cancel` | 取消非终态任务 |
| GET | `/api/v1/approvals?status=&task_id=` | 按状态/任务查询审批 |
| GET | `/api/v1/approvals/{approval_id}` | 读取完整审批预览 |
| POST | `/api/v1/approvals/{approval_id}:approve` | 以精确 `preview_hash` 批准本次调用 |
| POST | `/api/v1/approvals/{approval_id}:reject` | 以精确 `preview_hash` 拒绝本次调用 |
| GET | `/api/v1/reconciliations?status=&task_id=` | 按状态/任务查询 `unknown` 对账记录 |
| GET | `/api/v1/reconciliations/{reconciliation_id}` | 读取调用摘要、人工裁决和后继任务关联 |
| POST | `/api/v1/reconciliations/{reconciliation_id}:refresh-evidence` | 签名查询 Runner journal，并内容寻址追加 receipt/no-receipt/query-failed 证据 |
| POST | `/api/v1/reconciliations/{reconciliation_id}:resolve` | 使用幂等键提交不可改写的人工裁决 |
| POST | `/api/v1/reconciliations/{reconciliation_id}:create-attempt` | 仅在确认未产生效果后幂等创建全新任务 |
| POST | `/api/v1/reconciliations/{reconciliation_id}:create-compensation` | 仅从已验证 committed `file.move` receipt 幂等派生全新反向审批任务 |
| WS | `/api/v1/ws/tasks/{task_id}?after_seq=N` | 实时事件与断线续传 |

### 2.3 前端

- [x] Vue 3 + TypeScript + Vite 7 工程。
- [x] 深色桌面任务工作台布局和响应式样式。
- [x] 任务目标、隐私模式和创建任务表单。
- [x] 任务状态快照、结构化三步计划。
- [x] 实时事件时间线、结构化 payload 展开。
- [x] WebSocket 断线重连并携带最后事件序号。
- [x] 客户端按事件序号去重和排序。
- [x] 任务结束后显示“事件流已归档”。
- [x] Vite `/api` HTTP/WebSocket 本地代理。
- [x] session token 仅保存在前端内存，REST 自动附加 Bearer token。
- [x] WebSocket 使用版本与认证子协议，4401 后自动重新建立会话。
- [x] 前端解析 Problem Details 并展示安全的 `detail` 信息。
- [x] 侧栏新增“模型与设置”入口，任务工作台与 Provider 控制面保持前端模块分离。
- [x] 新增 Provider Catalog/Config/health/audit 严格 TypeScript 契约和认证 API client。
- [x] GET Catalog 捕获 ETag；Provider 写请求自动携带 `If-Match` 和高熵 UUID `Idempotency-Key`。
- [x] 写请求遇到浏览器网络传输失败时使用原 body、ETag 和幂等键自动重试一次，覆盖“服务端已提交但响应丢失”。
- [x] 支持 Fake/OpenAI-compatible Provider 创建和完整重新配置；隐藏 endpoint/credential reference 不从 API 回填。
- [x] 支持 Provider 启停、默认切换、删除内联二次确认和关联凭据保留提示。
- [x] 支持单 Provider 按需健康探测、能力标签和短期缓存状态展示。
- [x] 支持最近 100 条 secret-free 配置审计倒序时间线，只展示变化字段名。
- [x] `412` ETag 冲突后自动刷新 Catalog，不自动重放管理副作用，并提示用户确认后重试。
- [x] 页面明确不接收 API Key，提供 Windows Credential Manager 安全 CLI 指引。
- [x] 新增加载/空/错误/成功/冲突状态、原生配置对话框和窄屏响应式布局。
- [x] 新增 `doc/24-前端Provider模型设置页实现.md`，记录界面架构、并发/幂等、完整替换和凭据安全取舍。
- [x] 设置页新增角色路由、默认重试/费用预算、Provider EWMA、累计重试/费用和熔断状态展示；刷新运行态不会触发 Provider 网络探测。
- [x] 任务工作台新增 Pause/Resume/Cancel；控件严格遵循服务端状态矩阵，任一命令 pending 时整组禁用。
- [x] Cancel 使用内联二次确认；活动任务期间禁止创建第二任务，避免旧任务失去控制入口。
- [x] 控制命令不做乐观切换；成功后写入完整 Task 快照，`409` 或传输结果未知时先 GET 对账，Resume 不盲目重放。
- [x] Task 快照与 WebSocket 状态按 `last_event_seq` 仲裁，显式处理 `task.cancelled`，旧事件不再覆盖控制响应。
- [x] 事件流新增 connecting/connected/reconnecting/reauthenticating/forbidden/not_found/archived 状态和 1/2/4/8 秒封顶退避。
- [x] generation 隔离旧 session/socket/timer/handler；4401 重认证，4403/4404 永久停止，终态归档后不再重连。
- [x] 接入 Vitest、Vue Test Utils、jsdom，共 9 个测试文件、67 个用例，覆盖任务控制、事件恢复、Provider 安全表单、设置页交互及工具失败/取消/unknown 展示。
- [x] 新增 `doc/26-前端任务控制连接恢复与组件测试.md`，记录状态仲裁、非幂等对账、连接恢复和验收结果。
- [x] 任务工作台支持 `waiting_approval`，收到 `approval.required` 后通过受认证 API 读取完整预览，不以事件摘要代替用户核对材料。
- [x] 新增审批卡，展示风险、工具/用途、资源 operation、capability、后果、可逆性、data egress 和到期时间；批准明确限定“仅为本次”，拒绝支持原因。
- [x] 审批请求 pending 时禁用冲突控制；批准/拒绝不做乐观切换，传输结果不明时读取审批与任务快照对账，stale 时刷新预览并要求重新确认。
- [x] 前端识别 `approval.invalidated`，区分“用户曾批准”与“执行授权已失效”；批准已提交但运行时恢复失败时，只安全重试同一审批继续动作一次。
- [x] 前端测试曾扩展为 11 个测试文件、101 个用例，覆盖审批 API、卡片、状态仲裁、授权失效、运行时恢复、并发禁用、过期/冲突和网络失败对账。
- [x] 任务工作台新增 `useTaskReconciliation`；最新 `tool.unknown` 只触发一次自动定位与证据采集，任务切换通过 generation 忽略旧响应，并支持手动刷新。
- [x] 新增 Runner 证据卡，区分 committed/no-receipt/query-failed，展示无路径版本变化和稳定错误码，始终提示原调用保持 unknown 与人工裁决边界。
- [x] 证据卡在 committed `file.move` 可补偿时提供内联二次确认；网络失败的人工重试复用同一幂等键，成功后切换到新任务并复用完整审批卡。
- [x] 侧栏新增独立“历史与对账”入口；任务历史支持服务端状态筛选/分页，Reconciliation 支持 pending/resolved 筛选、详情、Runner 证据刷新与三类血缘导航。
- [x] 集中裁决、attempt 和 compensation 均要求二次确认，不做乐观更新或自动 POST 重放；相同人工重试复用幂等键，正文变化分配新 key，成功只接收服务端快照。
- [x] 活动任务期间锁定其他历史任务；切换终态历史任务时重建事件、控制、审批与单任务 Reconciliation 上下文。
- [x] 前端测试现为 15 个测试文件、122 个用例；新增覆盖历史 API、集中列表/筛选、幂等键语义、裁决筛选一致性、二次确认、血缘导航和任务切换锁。

### 2.4 浏览器端到端联调

早期事件主干版本已经完成基础联调。本阶段又对当前 Model Gateway + Runner + 路由韧性 + 任务控制版本执行了真实本地浏览器验收。

- [x] 在真实本地浏览器提交任务。
- [x] 收到 10 个连续事件，序号从 1 开始无缺口。
- [x] 最终任务状态为 `succeeded`，最后事件为 `task.completed`。
- [x] 三步计划正确展示。
- [x] 10 个事件时间均按 Asia/Shanghai 正确显示。
- [x] 浏览器控制台 error/warn 数量为 0。
- [x] 真实浏览器完成 session bootstrap、受认证任务创建和受认证 WebSocket 连接。
- [x] 安全联调收到 10 个连续事件，后端访问日志中没有 session token。
- [x] 运行中任务可暂停，暂停后显示 Resume/Cancel；恢复后最终 `succeeded`。
- [x] 检查点续跑过程中 `tool.requested/started/completed` 各出现一次，没有重复执行真实只读工具。
- [x] Cancel 二次确认生效；确认前无取消事件，确认后以 `task.cancelled` 归档。
- [x] API 暂停时显示重连、退避次数和立即重连；重启后从最后 `after_seq` 继续接收并显示恢复提示。
- [x] 暂停任务跨重启后可从受保护 checkpoint Resume；只有 checkpoint 缺失/无效时才展示运行上下文不可恢复提示，并仍可 Cancel。
- [x] Provider 控制面、Fake health、角色路由/预算/熔断投影和无 API Key 的引用凭据表单通过验收。
- [x] 390×844 窄屏无水平溢出；浏览器控制台 error/warn 为 0。

Policy/Approval 阶段人工浏览器验收已使用 `DESKPILOT_POLICY_REQUIRE_APPROVAL_FOR_R0=true` 完成：

- [x] 批准链严格按 `approval.resolved -> task.status_changed -> tool.started -> tool.completed -> task.completed` 到达，任务最终 `succeeded`。
- [x] 拒绝链严格按 `approval.resolved -> tool.cancelled -> task.cancelled` 到达，且没有 `tool.started`。
- [x] 审批卡展示规范绝对路径、capability、风险、后果、外发与一次性有效期。
- [x] 390×844 下审批卡无水平溢出；浏览器控制台 error/warn 数量为 0。

## 3. 验证结果

### 后端

```text
Ruff:  All checks passed
mypy:  Success, 105 source files
pytest: 282 passed
```

已覆盖：健康检查、任务/事件主干、有界历史分页/筛选/排序、数据库迁移、Outbox、安全会话、任务控制、受保护 checkpoint codec/缓冲区清零/身份绑定、结构化写请求跨重启恢复、created/paused/waiting-approval/approved-unconsumed 续跑、密文篡改/事件游标落后 fail-closed 与零派发，Tool/Runner 闭环、Runner 自动换代/退避/熔断/generation 隔离、持久化调用状态、原子 unknown/启动恢复/不重放、人工 reconciliation 状态机、签名 receipt/no-receipt/query-failed 证据、内容寻址重复刷新、receipt 绑定复核、稳定错误脱敏、裁决不可改写、跨重启幂等回执、Tool 幂等键占用、并发单新 attempt、side-effecting call 无请求快照时拒绝 attempt、新任务 lineage、事务回滚与旧数据回填，非重放安全 timeout/cancel、Windows 每调用独立 PID、Low Integrity、受限权限、Job 成员、写保护/子进程拒绝/环境秘密隔离、句柄核验与泄漏检查、broker facts/capability 覆盖、controlled-commit fail-closed、AppContainer 专用内容寻址 bundle、protected capability RX ACL、真实 Python Tool 强制禁网、bundle 篡改拒绝与孤儿 profile 启动回收，`file.move` 无副作用 prepare、父 Runner 单次 no-overwrite commit、source 版本/destination absent 复核、durable receipt、跨 Runner 查询、崩溃后 committed/no-effect 恢复、控制面原子投影、显式结构化任务入口、可信应用计划、动态路径 Policy 窄规则、真实一次性审批与前端双路径表单，Policy 风险矩阵、可信参数资源投影、资源/capability 精确校验、完整动态授权事实、审批 preview hash/幂等/冲突/稳定过期/一次性消费、批准后失效/重启取消、授权证明错配拒绝、敏感失败脱敏和事务回滚，Model Gateway 的结构化输出、隐私/能力/角色/费用路由、重试预算、Retry-After、延迟 EWMA、熔断、流式事件、超时、失败脱敏和任务事件接线，OpenAI-compatible Chat 的 HTTP/SSE/错误/health，多 Provider catalog、credential reference、locality/TLS 策略和密钥隔离，Provider API 的认证、脱敏、TTL、single-flight、取消隔离、全局并发限制、ETag、持久化幂等、动态 registry 和安全路由投影，以及 Windows Credential Manager、DPAPI 运行配置和脱敏审计。

Alembic：当前 head 为 `0012_task_runtime_checkpoints`；空库、重复升级、从旧 schema 保留数据、unknown/key-required 回填、commit receipt/reconciliation evidence、单补偿血缘与受保护 checkpoint 约束均有自动化覆盖。

当前测试存在 1 条第三方弃用警告：FastAPI/Starlette TestClient 提示未来从 `httpx` 迁移到 `httpx2`。不影响运行，下一次依赖维护时处理。

### 前端

```text
vitest run: 15 files, 122 passed
vue-tsc --noEmit: passed
vite build: passed
```

### 运行时

| 运行时 | 检测结果 | 处理 |
| --- | --- | --- |
| 工作区 Python | 3.12.13 | 已用于后端环境 |
| 系统 Python | 3.11.5 | 当前未使用 |
| 工作区 Node | 24.14.0 | 已用于前端构建 |
| 系统 Node | 18.20.8 | 不满足 Vite 7；后续升级到 Node 22+ |
| pnpm | 11.16.0 | 已使用 |
| uv | 0.12.3 | 安装于后端虚拟环境 |

## 4. 当前目录摘要

```text
项目根目录/
├── backend/
│   ├── src/deskpilot/
│   │   ├── api/
│   │   ├── application/
│   │   ├── core/
│   │   ├── domain/
│   │   ├── infrastructure/migrations/
│   │   ├── model_providers/
│   │   ├── runner/
│   │   └── tools/
│   ├── tests/
│   ├── alembic.ini
│   ├── pyproject.toml
│   └── uv.lock
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   └── composables/
│   ├── package.json
│   ├── pnpm-lock.yaml
│   ├── vitest.config.ts
│   └── pnpm-workspace.yaml
├── doc/
├── README.md
└── 项目进度.md
```

## 5. 尚未完成/已知边界

- [ ] API 重启已能自动恢复通过证明的单 Tool 任务；当前仍不支持通用多步 DAG、跨实例 lease 或已派发 running Tool 的透明重放。
- [ ] Model Gateway、Fake Provider 与 OpenAI-compatible Chat HTTP adapter 已完成；尚未实现 Responses、Ollama 原生 Provider 或 LangGraph。
- [ ] OpenAI-compatible adapter 已支持启动时多 Provider catalog；默认空 catalog 仍只注册离线 Fake Provider，不会自动连接云端或本地模型。
- [ ] Provider Catalog、DPAPI 密文运行配置、写 API、动态 adapter registry 和审计已完成；健康历史仍只在进程内短期缓存。
- [ ] Provider 管理目前以单 API 进程为目标；多进程下同 Idempotency-Key 同时首次插入的唯一键竞争尚未归一化为 replay。
- [ ] 管理 API 当前使用 PUT 完整替换，尚无配置详情读取或字段级 PATCH；因此前端重新配置时必须重填 endpoint/credential reference，Fake delay 也无法从公开投影预填。
- [ ] environment 与 Windows Credential Manager backend 已完成；macOS Keychain、Linux Secret Service 尚未实现，Windows 自动化测试也不会操作真实用户凭据库。
- [ ] 角色级路由、费用/重试预算、延迟 EWMA 与熔断已完成；策略由受信任启动配置加载，运行态和任务费用台账仍是单 API 进程内状态，尚未持久化或支持带版本审计的策略写 API。
- [ ] 当前 stream 在发出内容后不自动 retry/fallback，避免重复文本与 usage；跨 Provider 流式接续仍需更高层显式协议。
- [ ] Policy/Approval 应用层主干已完成；尚未实现可管理的策略写 API、审批中心、Agent Registry、MCP 和知识库。
- [ ] `unknown` Reconciliation API、持久化幂等回执、受限显式新 attempt、任务历史与集中对账/人工裁决 UI 已完成；Reconciliation 列表仍是无游标的有界全量查询，尚无后台周期证据采集。
- [ ] Windows 每调用 Job Object、受限令牌、Low Integrity、精确句柄继承和强制进程回收已完成；非 Windows 兼容路径只提供进程分离，生产默认会拒绝该降级。
- [ ] 控制面到常驻 Runner 的 bootstrap 仍使用私有 stdin 首帧交付会话密钥；每调用 worker 已使用句柄继承白名单且不接收该密钥。桌面发布版仍需代码签名、安装目录 ACL 和更强的同用户进程身份绑定。
- [ ] `filesystem.metadata.read` 已映射到父 Runner 句柄核验和不可变 broker facts；内容寻址 Python worker bundle、专用 capability RX ACL、强制 AppContainer 禁网与孤儿 profile reaper 已完成。bundle 仍由当前用户维护，桌面安装版还需代码签名、可信更新链和安装根目录 ACL 来抵御同用户恶意进程篡改。
- [ ] `file.move` prepare/commit/receipt、显式单文件任务、动态路径 Policy 和前端审批入口已完成；当前刻意不从自然语言或模型输出提取写路径，也不支持批量、目录、覆盖或跨卷操作。
- [ ] timeout/cancel 已按 commit boundary 区分确定未提交、committing unknown 和 receipt-proven success；任务页与集中中心可保存/展示签名 receipt 查询证据，但刻意不自动写入人工 Reconciliation 裁决，也不会后台周期采集。
- [ ] `file.move` 已支持基于 committed receipt 的单步显式补偿、重新版本校验与一次性审批；尚未实现多步 Tool effect graph、saga 补偿编排或补偿的再补偿。
- [ ] Reconciliation 幂等在当前单 API 进程内有并发锁与数据库唯一约束；多 API 进程同时首次提交同键时，唯一键竞争尚未统一归一化为 replay。
- [ ] 系统 Node 需要升级；当前开发验证使用工作区 Node 24。
- [ ] 当前 Outbox Publisher 面向单 API 进程；多进程部署前需要增加抢占锁/租约。
- [ ] 当前未实现 Outbox 死信转储与已发布消息定期清理。
- [ ] 开发阶段会话引导信任白名单 Origin/同源 Fetch Metadata；桌面发布版需由 Tauri 壳通过安全 IPC 交付 token 并启用严格 CSP。
- [ ] Provider 配置已有脱敏审计模型；尚未实现全系统安全审计、API 速率限制和并发连接上限。
- [ ] 当前 checkpoint 持久化的是由受信任应用代码定义的固定 9 阶段单 Tool 图；事件与后续 checkpoint 为相邻事务，崩溃窗口会 fail closed，尚未实现版本化多步图和原子节点 transition journal。
- [ ] pending 或 approved-but-unconsumed 审批在 checkpoint/event/task/call/policy/approval 全部绑定可证明时可跨重启续跑；缺失、损坏或错配时仍安全失效，不会猜测或派发 Tool。
- [ ] 前端已有持久化任务历史与终态任务切换，但活动执行上下文仍只保留一个快照；活动任务期间禁止创建或切换到第二任务，尚无并行多任务控制入口。

## 6. 下一步建议（按顺序）

1. 将单 Tool 固定图扩展为版本化、可查询的多步 Tool effect graph，明确 node/edge/attempt/effect/compensation 身份与 unknown 阻塞语义。
2. 使用事件+节点 checkpoint 同事务提交或显式 transition journal，缩小当前相邻事务的安全失败窗口。
3. 在 effect graph 上定义多步 saga 补偿顺序、不可补偿节点与每次新授权/新回执语义。
4. 为任务历史增加服务端游标分页和更细粒度的时间/Tool 筛选，并设计 Reconciliation 后台证据采集策略。

Model Gateway、Provider 安全管理与韧性路由、前端控制面、Runner 自动换代/熔断、持久化调用账本、`unknown` 安全收敛/人工 reconciliation/Runner 回执证据、任务历史/集中对账中心、Policy/Approval、Windows 每调用 Job/Low Integrity、AppContainer 专用 Python worker/强制禁网，以及 `file.move` prepare/commit/receipt、显式单文件任务/审批、receipt-driven 补偿与可证明跨重启 checkpoint 已完成。项目在没有 API Key 时仍能完整运行。下一阶段进入版本化多步 Tool effect graph 与原子节点 transition；继续禁止任意 Shell、动态 Python、模型生成写路径或未受 broker 控制的副作用。

## 7. 下次继续方式

下次开始开发前：

1. 先阅读本文件。
2. 阅读 `doc/11-分阶段开发路线.md` 的阶段 1～2。
3. 运行后端 Ruff、mypy、pytest。
4. 运行前端 test、type-check、build。
5. 阅读 `doc/38-结构化Tool请求与可证明跨重启检查点.md`，从“下一步”继续，并在结束时更新本文件。

建议给后续任务的指令：

> 继续 DeskPilot 开发，先读取根目录 `项目进度.md`，从尚未完成的第一项开始；保持 `backend/`、`frontend/` 分离，完成后更新进度文件并运行全部验证。

## 8. 常用命令

后端（位于 `backend/`）：

```powershell
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
.\.venv\Scripts\ruff.exe check .
.\.venv\Scripts\mypy.exe src
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\alembic.exe current
.\.venv\Scripts\alembic.exe check
.\.venv\Scripts\python.exe -m uvicorn deskpilot.main:app --reload
```

前端要求 Node 20.19+，推荐 Node 22+（位于 `frontend/`）：

```powershell
pnpm install
pnpm test
pnpm type-check
pnpm build
pnpm dev
```
